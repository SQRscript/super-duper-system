-- V18: Protection Center AntiChat/AntiPlayer + AutoCuff respawn stability + UI fixes
do
    -- Cleanly stop a previous SQR instance before starting a new one.
    pcall(function()
        local oldS = _G.SQR
        if type(oldS) == "table" then
            oldS.remoteCommandAlive = false
            for _,value in pairs(oldS) do
                local valueType = typeof(value)
                if valueType == "RBXScriptConnection" then
                    pcall(function() value:Disconnect() end)
                elseif type(value) == "thread" then
                    pcall(function() task.cancel(value) end)
                end
            end
            if oldS.countConnections then
                for _,conn in pairs(oldS.countConnections) do
                    pcall(function() conn:Disconnect() end)
                end
            end
            if oldS.gui and oldS.gui.Parent then oldS.gui:Destroy() end
            if oldS.screenGui and oldS.screenGui.Parent then oldS.screenGui:Destroy() end
            if oldS.protectionGui and oldS.protectionGui.Parent then oldS.protectionGui:Destroy() end
        end
        local pg = game:GetService("Players").LocalPlayer:FindFirstChildOfClass("PlayerGui")
        if pg then
            for _, name in ipairs({"DeltaFloatingButton", "SQR_UI", "CmdBarGUI"}) do
                local g = pg:FindFirstChild(name)
                if g then g:Destroy() end
            end
        end
        local cg = game:GetService("CoreGui")
        for _, name in ipairs({"ProtectionCenterUI", "AntiNVGui"}) do
            local g = cg:FindFirstChild(name)
            if g then g:Destroy() end
        end
    end)

    _G.SQR = {}
    local S = _G.SQR


    S.player = game.Players.LocalPlayer
    S.scriptStartTime = os.time()
    S.Players = game:GetService("Players")
    S.rs = game:GetService("ReplicatedStorage")
    S.Lighting = game:GetService("Lighting")
    S.VIM = game:GetService("VirtualInputManager")
    S.UIS = game:GetService("UserInputService")
    S.TweenService = game:GetService("TweenService")
    S.UserInputService = game:GetService("UserInputService")
    S.RunService = game:GetService("RunService")
    S.Workspace = game:GetService("Workspace")
    S.CoreGui = game:GetService("CoreGui")
    S.LogService = game:GetService("LogService")
    S.SQR_BUTTON_SOUND_ID = "rbxassetid://6026984224"
    
    -- الرتب الجديدة تُحفظ في JsonStorage وتُحدّث عند جميع الأجهزة.
    -- الاسمان التاليان هما المالكان الأصليان ولا يمكن إزالة Owner منهما.
    S.OWNER_LIST = {"nanMeWellDoit", "nanNeverLet"}
    S.VIP_LIST = {}
    S.BAN_LIST = {}
    S.rankState = {owners={}, vips={}, bans={}, vipKicks={}, day=""}
    S.isOwner = false
    S.isVIP = false
    S.isBanned = false
    S.VIP_DAILY_LIMIT = 5
    S.VIP_RESET_DAY = os.date("!%Y-%m-%d")
    
    for _, name in ipairs(S.OWNER_LIST) do
        S.rankState.owners[tostring(name):lower()] = true
    end
    S.isOwner = S.rankState.owners[S.player.Name:lower()] == true
end

-- ============================================================
-- BACKGROUND: TITLE UPDATER
-- ============================================================
do
    local S = _G.SQR
    task.spawn(function()
        pcall(function()
            local Event = S.rs:WaitForChild("UpdateTitleEvent")
            local function sendTitle(text)
                pcall(function()
                    Event:FireServer(text, Color3.new(0, 1, 1))
                end)
            end
            local randomMessages = {
                "Welcome To Script  .. ",
                "Owner Script Mhdi And SQR",
                "Name Script  :  SQR",
                "the vip is too cheap ",
                "만게쿠 샤링안 ",
                "이타치이이이이  ! "
            }
            sendTitle("SQR Scriptt")
            while true do
                task.wait(600)
                sendTitle(randomMessages[math.random(1, #randomMessages)])
            end
        end)
    end)
end

-- ============================================================
-- SCOPE 2: SAFE CANCEL
-- ============================================================
do
    local S = _G.SQR
    function S.safeCancel(thread)
        if not thread then return end
        pcall(function()
            if type(coroutine.close) == "function" then
                coroutine.close(thread)
            end
        end)
    end
end

-- ============================================================
-- ============================================================
-- SCOPE 3: HDADMIN COMMAND SENDER
-- ============================================================
do
    local S=_G.SQR

    S._hdCommandRemote = nil
    S._hdCommandQueue = {}
    S._hdCommandWorkerAlive = false
    S._hdCommandQueueMax = 40

    local function getHDCommandRemote()
        local cached = S._hdCommandRemote
        if cached and cached.Parent then return cached end

        local remote = S.rs:FindFirstChild("HDAdminHDClient")
        if remote then
            local signals = remote:FindFirstChild("Signals")
            local req = signals and signals:FindFirstChild("RequestCommandSilent")
            if req and req:IsA("RemoteFunction") then
                S._hdCommandRemote = req
                return req
            end
        end

        for _,obj in ipairs(S.rs:GetDescendants()) do
            if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
                local name=obj.Name:lower()
                if name:find("command",1,true) or name:find("exec",1,true) or name:find("silent",1,true) then
                    S._hdCommandRemote = obj
                    return obj
                end
            end
        end
        return nil
    end

    function S.sendCommand(cmd)
        cmd=tostring(cmd or "")
        if cmd=="" then return false end
        local ok, result = pcall(function()
            local remote=getHDCommandRemote()
            if not remote or not remote.Parent then return false end
            if remote:IsA("RemoteFunction") then
                remote:InvokeServer(cmd)
            else
                remote:FireServer(cmd)
            end
            return true
        end)
        if not ok then S._hdCommandRemote=nil end
        return ok and result==true
    end

    -- Bounded queue: prevents a new blocked task from accumulating on every spam tick.
    function S.sendCommandAsync(cmd)
        cmd=tostring(cmd or "")
        if cmd=="" then return end
        local q=S._hdCommandQueue
        if #q >= S._hdCommandQueueMax then table.remove(q,1) end
        q[#q+1]=cmd
        if S._hdCommandWorkerAlive then return end
        S._hdCommandWorkerAlive=true
        task.spawn(function()
            while S._hdCommandWorkerAlive do
                local nextCmd=table.remove(q,1)
                if not nextCmd then
                    S._hdCommandWorkerAlive=false
                    break
                end
                pcall(function() S.sendCommand(nextCmd) end)
                task.wait(0.01)
            end
        end)
    end
end

-- ============================================================
-- SCOPE 4: JSONSTORAGE BOOTSTRAP / LIVE RANK STATE
-- ============================================================
do
    local S=_G.SQR
    S.HttpService=game:GetService("HttpService")
    -- Supabase transport: يحفظ نفس سجل SQR داخل public.sqr_state.
    S.SUPABASE_URL="https://elierhbtttlzmuuhswjj.supabase.co"
    local function _sqrProtectedKey()
        -- Obfuscation only: this prevents the key from appearing as one plain string.
        -- It is not equivalent to server-side secret storage.
        local n={122,105,102,119,124,105,115,112,122,111,104,105,115,108,102,102,115,83,60,120,82,85,115,108,80,121,55,109,107,61,63,93,83,92,64,105,72,102,60,97,110,96,107,89,56,123}
        local out={}
        for i=1,#n do out[i]=string.char(n[i]-7) end
        return table.concat(out)
    end
    S.SUPABASE_KEY=_sqrProtectedKey()
    S._sqrProtectedKey=_sqrProtectedKey
    S.SUPABASE_STATE_URL=S.SUPABASE_URL.."/rest/v1/sqr_state"
    S.JSON_POLL=10
    S.SCRIPT_USER_TIMEOUT=120
    S.COMMAND_TIMEOUT=45
    S.VIP_DAILY_LIMIT=5
    S.remoteCommandAlive=true
    S.scriptStartTime=S.scriptStartTime or os.time()
    S.rankState={owners={nanmewelldoit=true,nanneverlet=true},vips={},bans={},vipKicks={},day=os.date("!%Y-%m-%d")}

    local function reqFn()
        if typeof(request)=="function" then return request end
        if typeof(http_request)=="function" then return http_request end
        if typeof(syn)=="table" and typeof(syn.request)=="function" then return syn.request end
    end
    local function sbHeaders()
        return {
            ["apikey"]=S.SUPABASE_KEY,
            ["Authorization"]="Bearer "..S.SUPABASE_KEY,
            ["Content-Type"]="application/json",
            ["Prefer"]="return=representation"
        }
    end
    local function sbRequest(method,url,body)
        local req=reqFn(); if not req then return false,nil,"request function not found" end
        local options={Url=url,Method=method,Headers=sbHeaders()}
        if body then options.Body=body end
        local ok,res=pcall(function() return req(options) end)
        if not ok or not res then return false,nil,tostring(res or "request failed") end
        local code=tonumber(res.StatusCode) or 0
        local responseBody=tostring(res.Body or "")
        if code<200 or code>=300 then return false,responseBody,"HTTP "..tostring(code).." "..responseBody end
        return true,responseBody,nil
    end
    local function get()
        local ok,body=sbRequest("GET",S.SUPABASE_STATE_URL.."?id=eq.1&select=data")
        if not ok then return nil end
        local good,data=pcall(function() return S.HttpService:JSONDecode(body or "") end)
        if good and type(data)=="table" and data[1] and type(data[1].data)=="table" then return data[1].data end
    end
    local function put(record)
        if type(record)~="table" then return false end
        local payload=S.HttpService:JSONEncode({data=record,updated_at=os.date("!%Y-%m-%dT%H:%M:%SZ")})
        local ok=sbRequest("PATCH",S.SUPABASE_STATE_URL.."?id=eq.1",payload)
        return ok
    end
    S.getCloudRecord=get; S.putCloudRecord=put

    local function norm(r)
        if type(r)~="table" then r={} end
        r.activeUsers=type(r.activeUsers)=="table" and r.activeUsers or {}
        r.owners=type(r.owners)=="table" and r.owners or {}
        r.vips=type(r.vips)=="table" and r.vips or {}
        r.bannedUsers=type(r.bannedUsers)=="table" and r.bannedUsers or {}
        r.vipKicks=type(r.vipKicks)=="table" and r.vipKicks or {}
        r.logs=type(r.logs)=="table" and r.logs or {}
        r.commands=type(r.commands)=="table" and r.commands or {}
        r.removedUsers=type(r.removedUsers)=="table" and r.removedUsers or {}
        r.safeUsers=type(r.safeUsers)=="table" and r.safeUsers or {}
        r.day=tostring(r.day or os.date("!%Y-%m-%d"))
        r.owners.nanMeWellDoit=true; r.owners.nanNeverLet=true
        if r.day~=os.date("!%Y-%m-%d") then r.day=os.date("!%Y-%m-%d"); r.vipKicks={} end
        return r
    end
    local function apply(r)
        r=norm(r)
        S.cloudRecord=r
        S.rankState={owners={},vips={},bans={},vipKicks=r.vipKicks,day=r.day}
        S.OWNER_LIST={}; S.VIP_LIST={}; S.BAN_LIST={}
        for n,v in pairs(r.owners) do if v then S.rankState.owners[tostring(n):lower()]=true; table.insert(S.OWNER_LIST,tostring(n)) end end
        for _,n in ipairs({"nanMeWellDoit","nanNeverLet"}) do if not S.rankState.owners[n:lower()] then S.rankState.owners[n:lower()]=true; table.insert(S.OWNER_LIST,n) end end
        for n,v in pairs(r.vips) do if v and not S.rankState.owners[tostring(n):lower()] then S.rankState.vips[tostring(n):lower()]=true; table.insert(S.VIP_LIST,tostring(n)) end end
        for n,v in pairs(r.bannedUsers) do if v then S.rankState.bans[tostring(n):lower()]=true; table.insert(S.BAN_LIST,tostring(n)) end end
        S.isOwner=S.rankState.owners[S.player.Name:lower()]==true
        S.isVIP=not S.isOwner and S.rankState.vips[S.player.Name:lower()]==true
        S.isBanned=S.rankState.bans[S.player.Name:lower()]==true
        return r
    end
    S.applyCloudRecord=apply

    local initial=get()
    if initial then apply(initial) else
        S.cloudRecord={activeUsers={},owners={nanMeWellDoit=true,nanNeverLet=true},vips={},bannedUsers={},safeUsers={},vipKicks={},logs={},day=os.date("!%Y-%m-%d")}
    end

    function S.isPlayerOwner(plr)
        if not plr then return false end
        return (S.rankState.owners and S.rankState.owners[plr.Name:lower()])==true or plr.Name=="nanMeWellDoit" or plr.Name=="nanNeverLet"
    end
    function S.isPlayerVIP(plr)
        if not plr then return false end
        return S.isPlayerOwner(plr) or ((S.rankState.vips and S.rankState.vips[plr.Name:lower()])==true)
    end
    function S.getPlayerRank(plr)
        if S.isPlayerOwner(plr) then return "Owner" end
        if S.isPlayerVIP(plr) then return "VIP" end
        return "Free"
    end
    function S.canTargetPlayer(plr)
        if not plr or plr==S.player then return false end
        if S.isPlayerSafe and S.isPlayerSafe(plr) then return false end
        if S.isPlayerOwner(plr) then return false end
        if S.isVIP and S.isPlayerVIP(plr) then return false end
        return true
    end
end

-- SCOPE 4: MESSAGE FUNCTIONS (تم إزالة رسائل السكربت)
-- ============================================================
do
    local S = _G.SQR
    
    function S.ShowScriptMessage(message, duration)
        -- تم تعطيل هذه الدالة (إزالة رسائل السكربت)
    end
    
    function S.ShowPopupMessage(message, duration, textColor, avatarId)
        -- تم تعطيل هذه الدالة (إزالة رسائل السكربت)
    end
end

-- ============================================================
-- SCOPE 5: BUTTON FEEDBACK
-- ============================================================
do
    local S = _G.SQR
    
    function S.addButtonFeedback(btn)
        if not btn or not btn:IsA("GuiButton") then return end
        if btn:GetAttribute("SQRFeedback") then return end
        btn:SetAttribute("SQRFeedback", true)

        local sound = Instance.new("Sound")
        sound.Name = "SQRButtonClick"
        sound.SoundId = S.SQR_BUTTON_SOUND_ID
        sound.Volume = 0.35
        sound.Parent = btn

        -- Keep one fixed base size. Repeated clicks must never compound the resize.
        local baseSize = btn.Size
        local activeDown, activeUp

        btn.Activated:Connect(function()
            if not btn or not btn.Parent then return end
            pcall(function() sound.SoundId = S.SQR_BUTTON_SOUND_ID; sound:Play() end)

            -- Cancel any previous pulse before starting a new one.
            pcall(function() if activeDown then activeDown:Cancel() end end)
            pcall(function() if activeUp then activeUp:Cancel() end end)
            btn.Size = baseSize

            local pressedSize = UDim2.new(
                baseSize.X.Scale * 0.97, math.floor(baseSize.X.Offset * 0.97),
                baseSize.Y.Scale * 0.92, math.floor(baseSize.Y.Offset * 0.92)
            )

            activeDown = S.TweenService:Create(btn, TweenInfo.new(0.055, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = pressedSize})
            activeUp = S.TweenService:Create(btn, TweenInfo.new(0.11, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = baseSize})
            activeDown:Play()
            activeDown.Completed:Connect(function()
                if btn and btn.Parent and activeUp then
                    activeUp:Play()
                end
            end)
        end)
    end
    
    function S.installButtonFeedback(root)
        if not root then return end
        for _, obj in ipairs(root:GetDescendants()) do
            if obj:IsA("GuiButton") then S.addButtonFeedback(obj) end
        end
        root.DescendantAdded:Connect(function(obj)
            if obj:IsA("GuiButton") then pcall(function() S.addButtonFeedback(obj) end) end
        end)
    end
end

-- ============================================================
-- SCOPE 6: OWNER/BANNED CHECK
-- ============================================================
do
    local S = _G.SQR
    
    if S.isOwner then
        wait(1)
        -- تم إزالة رسالة "You Are Owner The Script"
    end
    
    if S.isBanned then
        S.player:Kick("Reason : Banned from using this script")
        return
    end
end

-- ============================================================
-- SCOPE 7: DANCE AND BANG FUNCTIONS
-- ============================================================
do
    local S = _G.SQR
    
    S.BangActive = false
    S.HeadBangActive = false
    S.bangTask = nil
    S.headbangTask = nil
    S.danceAnimationTrack = nil
    S.BangSpeed = 1
    S.BangDistance = 200
    S.BangHeight = 0
    S.BangOscillation = 1.5
    S.HeadBangSpeed = 1
    S.HeadBangDistance = 200
    S.HeadBangHeight = 0
    S.HeadBangOscillation = 1.5
    S.OriginalPositions = {}
    
    function S.SaveOriginalPosition(name)
        local playerRoot = S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart")
        if playerRoot then S.OriginalPositions[name] = playerRoot.CFrame end
    end
    
    function S.RestoreOriginalPosition(name)
        local position = S.OriginalPositions[name]
        if position then
            local playerRoot = S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart")
            if playerRoot then
                playerRoot.CFrame = position
                S.OriginalPositions[name] = nil
            end
        end
    end
    
    function S.PlayDanceAnimation(speed)
        local humanoid = S.player.Character and S.player.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            local animator = humanoid:FindFirstChildOfClass("Animator")
            if animator then
                if S.danceAnimationTrack then S.danceAnimationTrack:Stop() end
                local animation = Instance.new("Animation")
                animation.AnimationId = "rbxassetid://5918726674"
                S.danceAnimationTrack = animator:LoadAnimation(animation)
                S.danceAnimationTrack:Play()
                S.danceAnimationTrack:AdjustSpeed(speed or 1.0)
            end
        end
    end
    
    function S.StopDanceAnimation()
        if S.danceAnimationTrack then
            S.danceAnimationTrack:Stop()
            S.danceAnimationTrack = nil
        end
    end
    
    function S.calculateBangMovement(targetRoot, playerRoot, oscTime)
        local baseDistance = math.clamp(S.BangDistance, 1, 500) / 100
        local heightOffset = math.clamp(S.BangHeight, 0, 1000) / 100
        local oscillationMultiplier = math.clamp(S.BangOscillation, 1, 10)
        oscTime = (oscTime or 0) + (0.1 * math.max(S.BangSpeed, 0.1))
        local osc = math.sin(oscTime) * baseDistance * oscillationMultiplier * 0.5
        local lookVector = targetRoot.CFrame.LookVector
        local behindPosition = targetRoot.Position - (lookVector * (baseDistance + osc))
        local maxDistance = 10
        if (behindPosition - targetRoot.Position).Magnitude > maxDistance then
            behindPosition = targetRoot.Position - (lookVector * maxDistance)
        end
        local finalPosition = behindPosition + Vector3.new(0, heightOffset, 0)
        playerRoot.CFrame = CFrame.new(finalPosition, targetRoot.Position)
        return oscTime
    end
    
    function S.StartBang(targetPlayer)
        if S.BangActive or not targetPlayer then return end
        S.BangActive = true
        S.SaveOriginalPosition("Bang")
        S.PlayDanceAnimation(S.BangSpeed)
        local oscillationTime = 0
        S.bangTask = task.spawn(function()
            while S.BangActive and targetPlayer and targetPlayer.Character do
                pcall(function()
                    local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                    local playerRoot = S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart")
                    if targetRoot and playerRoot then
                        oscillationTime = S.calculateBangMovement(targetRoot, playerRoot, oscillationTime)
                        if S.danceAnimationTrack then
                            S.danceAnimationTrack:AdjustSpeed(math.clamp(S.BangSpeed * (1 + math.clamp(S.BangOscillation, 1, 10) * 0.2), 1, 15))
                        end
                    end
                end)
                task.wait(0.03)
            end
            S.StopDanceAnimation()
            S.BangActive = false
            S.RestoreOriginalPosition("Bang")
        end)
    end
    
    function S.StartHeadBang(targetPlayer)
        if S.HeadBangActive or not targetPlayer then return end
        S.HeadBangActive = true
        S.SaveOriginalPosition("HeadBang")
        S.PlayDanceAnimation(S.HeadBangSpeed)
        local oscillationTime = 0
        S.headbangTask = task.spawn(function()
            while S.HeadBangActive and targetPlayer and targetPlayer.Character do
                pcall(function()
                    local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                    local targetHead = targetPlayer.Character:FindFirstChild("Head")
                    local playerRoot = S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart")
                    if targetRoot and playerRoot then
                        local baseDistance = math.clamp(S.HeadBangDistance, 1, 1000) / 100
                        local heightOffset = math.clamp(S.HeadBangHeight, 0, 1000) / 100
                        local oscillationMultiplier = math.clamp(S.HeadBangOscillation, 1, 10)
                        oscillationTime = oscillationTime + (0.1 * S.HeadBangSpeed)
                        local oscillation = math.sin(oscillationTime) * baseDistance * oscillationMultiplier * 0.5
                        local headPos = targetHead and targetHead.Position or targetRoot.Position + Vector3.new(0, 1.5, 0)
                        local lookVector = targetRoot.CFrame.LookVector
                        local inFrontPosition = headPos + (lookVector * (baseDistance + oscillation))
                        local maxDistance = 10
                        if (inFrontPosition - headPos).Magnitude > maxDistance then
                            inFrontPosition = headPos + (lookVector * maxDistance)
                        end
                        local finalPosition = inFrontPosition + Vector3.new(0, heightOffset, 0)
                        playerRoot.CFrame = CFrame.new(finalPosition, headPos)
                        if S.danceAnimationTrack then
                            S.danceAnimationTrack:AdjustSpeed(math.clamp(S.HeadBangSpeed * (1 + oscillationMultiplier * 0.2), 1, 15))
                        end
                    end
                end)
                task.wait(0.03)
            end
            S.StopDanceAnimation()
            S.HeadBangActive = false
            S.RestoreOriginalPosition("HeadBang")
        end)
    end
    
    function S.StopBang()
        S.BangActive = false
        S.HeadBangActive = false
        if S.bangTask then pcall(function() task.cancel(S.bangTask) end); S.bangTask = nil end
        if S.headbangTask then pcall(function() task.cancel(S.headbangTask) end); S.headbangTask = nil end
        S.StopDanceAnimation()
        S.RestoreOriginalPosition("Bang")
        S.RestoreOriginalPosition("HeadBang")
    end
    
    S.player.CharacterAdded:Connect(function()
        if S.BangActive or S.HeadBangActive then
            wait(1)
            if S.BangActive then S.PlayDanceAnimation(S.BangSpeed)
            elseif S.HeadBangActive then S.PlayDanceAnimation(S.HeadBangSpeed) end
        end
    end)
end

-- ============================================================
-- SCOPE 8: ANTI AFK AND NEAR PLAYERS
-- ============================================================
do
    local S = _G.SQR
    
    S.AntiAFK = false
    S.afkConnection = nil
    
    function S.EnableAntiAFK()
        if S.AntiAFK then return end
        S.AntiAFK = true
        S.afkConnection = S.player.Idled:Connect(function()
            S.VIM:SendKeyEvent(true, Enum.KeyCode.W, false, game)
            wait(0.1)
            S.VIM:SendKeyEvent(false, Enum.KeyCode.W, false, game)
        end)
        spawn(function()
            while S.AntiAFK do
                wait(10)
                pcall(function()
                    local hum = S.player.Character and S.player.Character:FindFirstChildOfClass("Humanoid")
                    if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
                end)
            end
        end)
    end
    
    function S.DisableAntiAFK()
        S.AntiAFK = false
        if S.afkConnection then
            S.afkConnection:Disconnect()
            S.afkConnection = nil
        end
    end
    
    function S.getNearPlayers()
        local near = {}
        local char = S.player.Character
        if not char then return near end
        local root = char:FindFirstChild("HumanoidRootPart")
        if not root then return near end
        for _, plr in ipairs(S.Players:GetPlayers()) do
            if plr ~= S.player and plr.Character then
                local targetRoot = plr.Character:FindFirstChild("HumanoidRootPart")
                if targetRoot then
                    local dist = (root.Position - targetRoot.Position).Magnitude
                    if dist < 50 then table.insert(near, plr) end
                end
            end
        end
        return near
    end
end

-- ============================================================
-- SCOPE 9: GUI CREATION - FLOATING BUTTON
-- ============================================================
do
    local S = _G.SQR
    
    S.playerGui = S.player:WaitForChild("PlayerGui")
    
    local oldGui = S.playerGui:FindFirstChild("DeltaFloatingButton")
    if oldGui then oldGui:Destroy() end
    
    S.gui = Instance.new("ScreenGui")
    S.gui.Name = "DeltaFloatingButton"
    S.gui.ResetOnSpawn = false
    S.gui.IgnoreGuiInset = true
    S.gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    S.gui.Parent = S.playerGui
    
    S.button = Instance.new("ImageButton")
    S.button.Name = "FloatingButton"
    S.button.Size = UDim2.fromOffset(55, 55)
    S.button.Position = UDim2.new(0.02, 0, 0.4, 0)
    S.button.BackgroundTransparency = 1
    S.button.BorderSizePixel = 0
    S.button.Image = "rbxassetid://105410336254895"
    S.button.ScaleType = Enum.ScaleType.Fit
    S.button.Active = true
    S.button.AutoButtonColor = true
    S.button.ZIndex = 999
    S.button.Parent = S.gui
    
    S.dragging = false
    S.dragStart = nil
    S.startPos = nil
    S.dragInput = nil
    
    S.button.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            S.dragging = true
            S.dragStart = input.Position
            S.startPos = S.button.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then S.dragging = false end
            end)
        end
    end)
    
    S.button.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            S.dragInput = input
        end
    end)
    
    S.UserInputService.InputChanged:Connect(function(input)
        if S.dragging and input == S.dragInput then
            local delta = input.Position - S.dragStart
            S.button.Position = UDim2.new(
                S.startPos.X.Scale,
                S.startPos.X.Offset + delta.X,
                S.startPos.Y.Scale,
                S.startPos.Y.Offset + delta.Y
            )
        end
    end)
end

-- ============================================================
-- SCOPE 10: MAIN UI FRAME (تم تصغير القياس)
-- ============================================================
do
    local S = _G.SQR
    
    S.screenGui = Instance.new("ScreenGui")
    S.screenGui.Name = "SQR_UI"
    S.screenGui.ResetOnSpawn = false
    S.screenGui.Enabled = false
    S.screenGui.Parent = S.playerGui
    
    S.mainFrame = Instance.new("Frame")
    S.mainFrame.Size = UDim2.new(0, 600, 0, 400)
    S.mainFrame.Position = UDim2.new(0.5, -300, 0.5, -200)
    S.mainFrame.BackgroundTransparency = 0
    S.mainFrame.BorderSizePixel = 0
    S.mainFrame.Active = true
    S.mainFrame.Draggable = false
    S.mainFrame.ClipsDescendants = false
    S.mainFrame.Visible = false
    S.mainFrame.Parent = S.screenGui

    -- مقياس الواجهة: يغيّر حجم الواجهة والأزرار وأزرار الأقسام معاً.
    S.uiScale = Instance.new("UIScale")
    S.uiScale.Scale = 1
    S.uiScale.Parent = S.mainFrame
    
    S.bgImage = Instance.new("ImageLabel")
    S.bgImage.Size = UDim2.new(1, 0, 1, 0)
    S.bgImage.BackgroundTransparency = 1
    S.bgImage.BorderSizePixel = 0
    S.bgImage.Image = ""
    S.bgImage.ImageTransparency = 1
    S.bgImage.ScaleType = Enum.ScaleType.Crop
    S.bgImage.Parent = S.mainFrame
    
    -- لا يوجد مربع/طبقة سوداء فوق الخلفية؛ تظهر صورة الخلفية فقط.
    S.bgImage.ZIndex = 0
    
    S.button.Activated:Connect(function()
        S.screenGui.Enabled = not S.screenGui.Enabled
        if S.screenGui.Enabled then
            S.mainFrame.Visible = true
            local savedScale = math.clamp(tonumber(S.settingState.uiScale) or 1, 0.7, 1.3)
            S.mainFrame.Size = UDim2.new(0, 600, 0, 400)
            S.mainFrame.Position = UDim2.new(0.5, -300 * savedScale, 0.5, -200 * savedScale)
            S.uiScale.Scale = savedScale
            S.mainFrame.Visible = true
            S.screenGui.Enabled = true
            S.mainFrame.Position = UDim2.new(0.5, -300 * savedScale, 0.5, -200 * savedScale)
        else
            pcall(function()
                S.TweenService:Create(S.mainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
                    Size = UDim2.new(0, 0, 0, 0),
                    Position = UDim2.new(0.5, -300, 0.5, -200)
                }):Play()
            end)
            wait(0.3)
            S.mainFrame.Visible = false
        end
    end)
    
    S.centerTitle = Instance.new("TextLabel")
    S.centerTitle.Size = UDim2.new(1, 0, 0.06, 0)
    S.centerTitle.Position = UDim2.new(0, 0, 0.02, 0)
    S.centerTitle.BackgroundTransparency = 1
    S.centerTitle.BorderSizePixel = 0
    S.centerTitle.Text = "S Q R"
    S.centerTitle.ZIndex = 4
    S.centerTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.centerTitle.TextScaled = true
    S.centerTitle.Font = Enum.Font.SourceSansBold
    S.centerTitle.TextXAlignment = Enum.TextXAlignment.Center
    S.centerTitle.Parent = S.mainFrame
    
    S.titleLine = Instance.new("Frame")
    S.titleLine.Size = UDim2.new(0.2, 0, 0, 2)
    S.titleLine.Position = UDim2.new(0.4, 0, 0.085, 0)
    S.titleLine.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
    S.titleLine.BorderSizePixel = 0
    S.titleLine.ZIndex = 4
    S.titleLine.Parent = S.mainFrame
    Instance.new("UICorner", S.titleLine).CornerRadius = UDim.new(1, 0)

    -- شريط سحب مستقل: سحب شريط التمرير لا يحرك الواجهة كلها.
    S.dragBar = Instance.new("TextButton")
    S.dragBar.Size = UDim2.new(1, -70, 0, 38)
    S.dragBar.Position = UDim2.new(0, 35, 0, 0)
    S.dragBar.BackgroundTransparency = 1
    S.dragBar.BorderSizePixel = 0
    S.dragBar.Text = ""
    S.dragBar.AutoButtonColor = false
    S.dragBar.ZIndex = 10
    S.dragBar.Parent = S.mainFrame

    local dragMoving = false
    local dragStart = nil
    local dragStartPos = nil
    local dragInput = nil

    S.dragBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragMoving = true
            dragStart = input.Position
            dragStartPos = S.mainFrame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragMoving = false end
            end)
        end
    end)

    S.dragBar.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    S.UserInputService.InputChanged:Connect(function(input)
        if dragMoving and input == dragInput then
            local delta = input.Position - dragStart
            S.mainFrame.Position = UDim2.new(
                dragStartPos.X.Scale, dragStartPos.X.Offset + delta.X,
                dragStartPos.Y.Scale, dragStartPos.Y.Offset + delta.Y
            )
        end
    end)
end

-- ============================================================
-- SCOPE 11: SIDEBAR NAVIGATION
-- ============================================================
do
    local S = _G.SQR
    
    S.sidebar = Instance.new("Frame")
    S.sidebar.Size = UDim2.new(0, 180, 1, -40)
    S.sidebar.Position = UDim2.new(0, 0, 0, 40)
    S.sidebar.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    S.sidebar.BackgroundTransparency = 0.6
    S.sidebar.BorderSizePixel = 0
    S.sidebar.ZIndex = 3
    S.sidebar.Parent = S.mainFrame
    
    -- ترتيب الأقسام: الأقسام الإدارية موجودة دائماً، وتُظهر حسب الصلاحية.
    -- هذا يسمح بظهور VIP/Owner فور الترقية بدون إعادة تشغيل السكربت.
    S.sections = {"Setting", "Target", "HD", "Anti", "Near", "VIP", "Owner", "3dad", "SQR"}
    
    S.sectionButtons = {}
    S.sectionFrames = {}
    S.currentSection = 1
    
    for i, name in ipairs(S.sections) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.88, 0, 0.065, 0)
        btn.Position = UDim2.new(0.06, 0, 0.02 + (i-1) * 0.075, 0)
        btn.BackgroundColor3 = i == 1 and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(30, 30, 45)
        btn.BackgroundTransparency = i == 1 and 0 or 0.4
        btn.BorderSizePixel = 0
        btn.TextColor3 = i == 1 and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(200, 200, 200)
        btn.Text = name
        btn.TextScaled = true
        btn.Font = Enum.Font.SourceSansBold
        btn.Visible = true
        btn.Active = true
        btn.ZIndex = 5
        btn.Parent = S.sidebar
        S.sectionButtons[i] = btn
        
        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 8)
        btnCorner.Parent = btn
        
        local sectionFrame = Instance.new("ScrollingFrame")
        sectionFrame.Size = UDim2.new(1, -195, 1, -50)
        sectionFrame.Position = UDim2.new(0, 185, 0, 45)
        sectionFrame.BackgroundTransparency = 1
        sectionFrame.BorderSizePixel = 0
        sectionFrame.Visible = i == 1
        sectionFrame.ZIndex = 4
        sectionFrame.CanvasSize = UDim2.new(0,0,0,0)
        sectionFrame.ScrollBarThickness = (name == "Anti") and 4 or 5
        sectionFrame.ScrollingDirection = Enum.ScrollingDirection.Y
        sectionFrame.Active = true
        sectionFrame.ScrollingEnabled = true
        sectionFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
        sectionFrame.ClipsDescendants = true
        sectionFrame.ZIndex = 20
        sectionFrame.Parent = S.mainFrame
        S.sectionFrames[i] = sectionFrame
        
        local function selectSection()
            if S.currentSection == i then return end
            S.currentSection = i
            pcall(function() S.normalizeSectionUI(S.sectionFrames[i]) end)
            for j, sf in ipairs(S.sectionFrames) do
                if j == i then
                    sf.Visible = true
                    S.sectionButtons[j].BackgroundColor3 = Color3.fromRGB(0, 200, 0)
                    S.sectionButtons[j].BackgroundTransparency = 0
                    S.sectionButtons[j].TextColor3 = Color3.fromRGB(255, 255, 255)
                else
                    sf.Visible = false
                    S.sectionButtons[j].BackgroundColor3 = Color3.fromRGB(30, 30, 45)
                    S.sectionButtons[j].BackgroundTransparency = 0.4
                    S.sectionButtons[j].TextColor3 = Color3.fromRGB(200, 200, 200)
                end
            end
        end
        btn.Activated:Connect(selectSection)
    end
    local vipI=table.find(S.sections,"VIP")
    local ownerI=table.find(S.sections,"Owner")
    if vipI then S.sectionButtons[vipI].Visible=true; S.sectionFrames[vipI].Visible=false end
    if ownerI then S.sectionButtons[ownerI].Visible=true; S.sectionFrames[ownerI].Visible=false end
end


-- V32 FIX: restore the S reference before using sectionFrames.
-- The previous V31 block was outside the navigation scope, so `S` was nil/stale.
local S = _G.SQR

-- V35 UI NORMALIZER: every section uses the same rendering/input rules.
-- Only controls explicitly marked SQRKeepHidden/SQRDeferred may stay hidden.
function S.normalizeSectionUI(sf)
    if not sf then return end
    sf.Visible = true
    sf.Active = true
    sf.ZIndex = 20
    sf.ScrollingEnabled = true
    sf.ScrollingDirection = Enum.ScrollingDirection.Y
    sf.AutomaticCanvasSize = Enum.AutomaticSize.Y
    sf.ScrollBarThickness = (S.sections[table.find(S.sectionFrames, sf)] == "Anti") and 4 or 5
    for _,obj in ipairs(sf:GetDescendants()) do
        if obj:IsA("GuiObject") then
            if obj:GetAttribute("SQRKeepHidden") ~= true and obj:GetAttribute("SQRDeferred") ~= true then
                obj.Visible = true
                if obj:IsA("GuiButton") or obj:IsA("TextBox") then
                    obj.Active = true
                    obj.AutoButtonColor = true
                end
            end
            if obj:IsA("GuiButton") or obj:IsA("TextBox") then
                obj.ZIndex = math.max(obj.ZIndex, 50)
            end
        end
    end
    task.defer(function()
        if sf and sf.Parent then
            sf.CanvasSize = UDim2.new(0, 0, 0, math.max(0, sf.AbsoluteCanvasSize.Y))
        end
    end)
end

for _,sf in ipairs((S and S.sectionFrames) or {}) do
    if sf then pcall(function() S.normalizeSectionUI(sf) end) end
end

-- ============================================================
-- SCOPE 13: TARGET SECTION - UI ELEMENTS (مع سلايدرات محسنة)
-- ============================================================
do
    local S = _G.SQR
    local sf3 = S.sectionFrames[table.find(S.sections, "Target")]
    
    S.searchBox = Instance.new("TextBox")
    S.searchBox.Size = UDim2.new(1, -20, 0.12, 0)
    S.searchBox.Position = UDim2.new(0, 10, 0.02, 0)
    S.searchBox.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    S.searchBox.BorderSizePixel = 0
    S.searchBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.searchBox.PlaceholderText = "Search player..."
    S.searchBox.Text = ""
    S.searchBox.ClearTextOnFocus = false
    S.searchBox.Font = Enum.Font.SourceSans
    S.searchBox.TextSize = 16
    S.searchBox.Active = true
    S.searchBox.ZIndex = 80
    S.searchBox.Parent = sf3
    Instance.new("UICorner", S.searchBox).CornerRadius = UDim.new(0, 10)

    S.targetAvatar = Instance.new("ImageLabel")
    S.targetAvatar.Size = UDim2.fromOffset(54, 54)
    S.targetAvatar.Position = UDim2.new(0.5, -27, 0.145, 0)
    S.targetAvatar.BackgroundTransparency = 1
    S.targetAvatar.BorderSizePixel = 0
    S.targetAvatar.ScaleType = Enum.ScaleType.Fit
    S.targetAvatar.ZIndex = 7
    S.targetAvatar.Visible = false
    S.targetAvatar:SetAttribute("SQRKeepHidden", true)
    S.targetAvatar.Parent = sf3

    S.nameLabel = Instance.new("TextLabel")
    S.nameLabel.Size = UDim2.new(1, 0, 0.05, 0)
    S.nameLabel.Position = UDim2.new(0, 0, 0.29, 0)
    S.nameLabel.BackgroundTransparency = 1
    S.nameLabel.BorderSizePixel = 0
    S.nameLabel.Text = "No player"
    S.nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.nameLabel.TextScaled = true
    S.nameLabel.Font = Enum.Font.SourceSansBold
    S.nameLabel.Parent = sf3

    S.userLabel = Instance.new("TextLabel")
    S.userLabel.Size = UDim2.new(1, 0, 0.04, 0)
    S.userLabel.Position = UDim2.new(0, 0, 0.35, 0)
    S.userLabel.BackgroundTransparency = 1
    S.userLabel.BorderSizePixel = 0
    S.userLabel.Text = ""
    S.userLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    S.userLabel.TextScaled = true
    S.userLabel.Font = Enum.Font.SourceSans
    S.userLabel.Parent = sf3

    function S.createTargetButton(name, xPos, yPos)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.21, 0, 0.065, 0)
        btn.Position = UDim2.new(xPos, 0, yPos, 0)
        btn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        btn.BorderSizePixel = 0
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.Text = name
        btn.TextScaled = true
        btn.Font = Enum.Font.SourceSansBold
        btn.Parent = sf3
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
        return btn
    end

    S.viewButton = S.createTargetButton("View", 0.02, 0.42)
    S.teleportButton = S.createTargetButton("Teleport", 0.25, 0.42)
    S.bangButton = S.createTargetButton("Bang", 0.48, 0.42)
    S.headbangButton = S.createTargetButton("Head", 0.71, 0.42)
    S.nvButton = S.createTargetButton("NV", 0.02, 0.50)
    S.reButton = S.createTargetButton("Re", 0.25, 0.50)
    S.copy1Button = S.createTargetButton("C1", 0.48, 0.50)
    S.copy2Button = S.createTargetButton("C2", 0.71, 0.50)

    -- نفس شكل شرائط Setting: زر قيمة صغير + شريط رفيع + مقبض دائري.
    local function createTargetSlider(valueText, yPos, minValue, maxValue, initialPercent, formatter, onChanged, noDrag)
        local valueButton = Instance.new("TextButton")
        valueButton.Size = UDim2.fromOffset(90, 35)
        valueButton.Position = UDim2.fromOffset(10, yPos)
        valueButton.BackgroundColor3 = Color3.fromRGB(35, 40, 55)
        valueButton.BackgroundTransparency = 0.05
        valueButton.BorderSizePixel = 0
        valueButton.Text = valueText
        valueButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        valueButton.TextSize = 12
        valueButton.Font = Enum.Font.GothamBold
        valueButton.ZIndex = 5
        valueButton.Parent = sf3
        Instance.new("UICorner", valueButton).CornerRadius = UDim.new(0, 9)

        local slider = Instance.new("Frame")
        slider.Size = UDim2.fromOffset(220, 10)
        slider.Position = UDim2.fromOffset(110, yPos + 13)
        slider.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
        slider.BorderSizePixel = 0
        slider.Active = true
        slider.Selectable = true
        slider.ZIndex = 80
        slider.Parent = sf3
        Instance.new("UICorner", slider).CornerRadius = UDim.new(1, 0)

        local fill = Instance.new("Frame")
        fill.Size = UDim2.new(initialPercent, 0, 1, 0)
        fill.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
        fill.BorderSizePixel = 0
        fill.ZIndex = 81
        fill.Parent = slider
        Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)

        local knob = Instance.new("Frame")
        knob.Size = UDim2.fromOffset(18, 18)
        knob.Position = UDim2.new(initialPercent, -9, 0.5, -9)
        knob.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
        knob.BorderSizePixel = 0
        knob.ZIndex = 82
        knob.Active = true
        knob.Parent = slider
        Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

        local function applyPercent(percent)
            percent = math.clamp(percent, 0, 1)
            fill.Size = UDim2.new(percent, 0, 1, 0)
            knob.Position = UDim2.new(percent, -9, 0.5, -9)
            local value = math.floor(minValue + (maxValue - minValue) * percent + 0.5)
            valueButton.Text = formatter(value)
            if onChanged then onChanged(value) end
        end

        if not noDrag then
            local dragging = false
            local function setFromInput(input)
                local left = slider.AbsolutePosition.X
                local width = math.max(slider.AbsoluteSize.X, 1)
                applyPercent(math.clamp((input.Position.X - left) / width, 0, 1))
            end
            slider.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = true
                    setFromInput(input)
                end
            end)

            S.UserInputService.InputChanged:Connect(function(input)
                if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    setFromInput(input)
                end
            end)

            S.UserInputService.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = false
                end
            end)
        end

        return valueButton, slider, fill, knob
    end

    S.distanceLabel, S.distanceSlider, S.distanceFill, S.distanceKnob =
        createTargetSlider("Distance: 200cm", 205, 1, 500, 199/499, function(value)
            return "Distance: " .. value .. "cm"
        end, function(value)
            S.BangDistance = value
            S.HeadBangDistance = value
        end)

    S.BangDistance = 200
    S.HeadBangDistance = 200
    S.distanceFill.Size = UDim2.new(199/499, 0, 1, 0)
    S.distanceKnob.Position = UDim2.new(199/499, -9, 0.5, -9)
    S.distanceLabel.Text = "Distance: 200cm"

    local speedPoints = {0.166, 0.5, 0.833}

    local function applyTargetSpeedFromPercent(percent)
        local nearest = speedPoints[1]
        local nearestDist = math.abs(percent - nearest)
        for i = 2, #speedPoints do
            local dist = math.abs(percent - speedPoints[i])
            if dist < nearestDist then
                nearestDist = dist
                nearest = speedPoints[i]
            end
        end
        local speed = 1
        if nearest >= 0.75 then speed = 3 elseif nearest >= 0.35 then speed = 2 end
        S.speedFill.Size = UDim2.new(nearest,0,1,0)
        S.speedKnob.Position = UDim2.new(nearest,-9,0.5,-9)
        S.speedLabel.Text = "Speed: x"..speed
        S.BangSpeed = speed
        S.HeadBangSpeed = speed
        if S.BangActive and S.danceAnimationTrack then pcall(function() S.danceAnimationTrack:AdjustSpeed(speed) end) end
        if S.HeadBangActive and S.danceAnimationTrack then pcall(function() S.danceAnimationTrack:AdjustSpeed(speed) end) end
    end

    S.speedLabel, S.speedSlider, S.speedFill, S.speedKnob =
        createTargetSlider("Speed: x1", 255, 1, 3, speedPoints[1], function(value)
            local percent = (value-1)/2
            applyTargetSpeedFromPercent(percent)
            return "Speed: x"..value
        end)

    S.speedSlider.Active=true
    S.speedSlider.ZIndex=80
    S.speedFill.ZIndex=81
    S.speedKnob.ZIndex=82
    S.speedLabel.ZIndex=83
    S.speedFill.Size=UDim2.new(speedPoints[1],0,1,0)
    S.speedKnob.Position=UDim2.new(speedPoints[1],-9,0.5,-9)
    S.speedLabel.Text="Speed: x1"


    -- أزرار الكلبشه المطلوبة: تستهدف اللاعب المحدد فقط.
    S.cuffButton = S.createTargetButton("كلبشه", 0.02, 0.84)
    S.autoCuffButton = S.createTargetButton("كلبشه تلقائيه", 0.50, 0.84)
    S.autoCuffEnabled = false
    S.autoCuffLoop = nil
    S.lastCuffTime = 0
    S.cuffInProgress = false
    S.cachedCuffTool = nil
    S.lastCuffNotify = 0
    S.autoCuffMonitorLoop = nil
    S.autoCuffMonitorAlive = false

    -- !re can replace the local character. Do not stop Auto Cuff; simply reset
    -- its timer so it resumes with the same selected target after respawn.
    S.cuffCharacterConnection = S.cuffCharacterConnection or S.player.CharacterAdded:Connect(function()
        S.lastCuffTime = 0
        S.cachedCuffTool = nil
    end)

    -- Cuff tool discovery is intentionally generic: use any Tool the user actually owns
    -- whose name indicates a cuff/handcuff, instead of depending on a fixed tool name.
    local function isCuffTool(tool)
        if not tool or not tool:IsA("Tool") then return false end
        local n=tostring(tool.Name or ""):lower():gsub("%s+","")
        return n:sub(1,6)=="كلبشه"
            or n:sub(1,6)=="كلبشة"
            or n:sub(1,5)=="كلبش"
    end

    local function getAllTools()
        local result={}
        local seen={}
        local char=S.player.Character
        local backpack=S.player:FindFirstChildOfClass("Backpack")
        local function add(container)
            if not container then return end
            for _,obj in ipairs(container:GetChildren()) do
                if obj:IsA("Tool") and not seen[obj] then
                    seen[obj]=true
                    result[#result+1]=obj
                end
            end
        end
        add(char)
        add(backpack)
        return result
    end

    local function getCuffTool()
        local cached=S.cachedCuffTool
        if cached and cached.Parent and cached:IsA("Tool") then
            return cached
        end
        for _,tool in ipairs(getAllTools()) do
            if isCuffTool(tool) then
                S.cachedCuffTool=tool
                return tool
            end
            for _,obj in ipairs(tool:GetDescendants()) do
                if obj:IsA("RemoteEvent") then
                    local n=tostring(obj.Name or ""):lower()
                    if n:find("cuff",1,true) or n:find("handcuff",1,true) or n:find("كلب",1,true) or n:find("قيد",1,true) then
                        S.cachedCuffTool=tool
                        return tool
                    end
                end
            end
        end
        S.cachedCuffTool=nil
        return nil
    end

    local function getCuffRemote(tool)
        if not tool then return nil end
        local preferred=tool:FindFirstChild("RemoteEvent",true)
        if preferred and preferred:IsA("RemoteEvent") then return preferred end
        for _,obj in ipairs(tool:GetDescendants()) do
            if obj:IsA("RemoteEvent") then return obj end
        end
        return nil
    end

    local function cuffNotify(text)
        pcall(function() game:GetService("StarterGui"):SetCore("SendNotification",{Title="SQR - الكلبشه",Text=tostring(text),Duration=3}) end)
    end

    local function executeCuff(targetPlayer)
        if S.cuffInProgress then return false end
        if not targetPlayer or targetPlayer==S.player or not targetPlayer.Parent then return false end
        S.cuffInProgress=true
        local success=false
        pcall(function()
            local myChar=S.player.Character
            local humanoid=myChar and myChar:FindFirstChildOfClass("Humanoid")
            local myHRP=myChar and myChar:FindFirstChild("HumanoidRootPart")
            if not humanoid or not myHRP then return end
            local tool=getCuffTool()
            if not tool then cuffNotify("الكلبشه ليست عندك"); return end
            if tool.Parent~=myChar then
                pcall(function() humanoid:EquipTool(tool) end)
                task.wait(0.15)
                tool=getCuffTool() or tool
            end
            if not tool or not tool.Parent then
                S.cachedCuffTool=nil
                tool=getCuffTool()
            end
            local remote=getCuffRemote(tool)
            if not remote then cuffNotify("لم يتم العثور على Remote للكلبشه"); return end
            local targetChar=targetPlayer.Character
            local targetHRP=targetChar and targetChar:FindFirstChild("HumanoidRootPart")
            if not targetHRP then return end
            local oldCFrame=myHRP.CFrame
            local attempts=0
            while attempts<8 and targetPlayer.Parent and targetPlayer.Character and myHRP.Parent do
                attempts+=1
                targetChar=targetPlayer.Character
                targetHRP=targetChar and targetChar:FindFirstChild("HumanoidRootPart")
                if not targetHRP then break end
                local d=(myHRP.Position-targetHRP.Position).Magnitude
                if d>12 then myHRP.CFrame=targetHRP.CFrame*CFrame.new(0,0,0.7) end
                local fired=false
                for _,args in ipairs({{targetHRP},{targetPlayer},{"Cuff",targetHRP},{"Cuff",targetPlayer},{targetPlayer,targetHRP}}) do
                    local ok=pcall(function() remote:FireServer(table.unpack(args)) end)
                    if ok then fired=true end
                    task.wait(0.025)
                end
                if fired then success=true end
                task.wait(0.05)
            end
            if myHRP.Parent then myHRP.CFrame=oldCFrame end
        end)
        S.cuffInProgress=false
        if not success then
            if not getCuffTool() then cuffNotify("الكلبشه ليست عندك") else cuffNotify("تعذر استخدام الكلبشه") end
        end
        return success
    end

    S.cuffButton.Activated:Connect(function()
        if S.selectedPlayer then executeCuff(S.selectedPlayer) end
    end)

    -- Auto Cuff: one worker only. Respawn/Character changes never disable it.
    -- No extra AntiHuman protection/position-restore block is used here.
    local boundAutoCuffTarget=nil
    local function bindAutoCuffTarget(target)
        if S.autoCuffTargetConnection then
            pcall(function() S.autoCuffTargetConnection:Disconnect() end)
            S.autoCuffTargetConnection=nil
        end
        boundAutoCuffTarget=target
        if target and target~=S.player then
            S.autoCuffTargetConnection=target.CharacterAdded:Connect(function()
                S.lastCuffTime=0
            end)
        end
    end

    local function stopAutoCuffWorker()
        S.autoCuffMonitorAlive=false
        if S.autoCuffMonitorLoop then
            pcall(function() task.cancel(S.autoCuffMonitorLoop) end)
        end
        S.autoCuffMonitorLoop=nil
        if S.autoCuffLoop then
            pcall(function() task.cancel(S.autoCuffLoop) end)
        end
        S.autoCuffLoop=nil
    end

    local function startAutoCuffWorker()
        if S.autoCuffMonitorAlive then return end
        S.autoCuffMonitorAlive=true
        S.autoCuffMonitorLoop=task.spawn(function()
            while S.autoCuffMonitorAlive and S.autoCuffEnabled do
                local target=S.selectedPlayer
                if not target or not target.Parent then
                    local all=S.Players:GetPlayers()
                    for _,candidate in ipairs(all) do
                        if candidate~=S.player and (not S.canTargetPlayer or S.canTargetPlayer(candidate)) and candidate.Character then
                            target=candidate
                            S.updateTargetUI(candidate)
                            break
                        end
                    end
                end
                if target~=boundAutoCuffTarget then bindAutoCuffTarget(target) end
                if target and target~=S.player and target.Parent then
                    local targetChar=target.Character
                    local targetHRP=targetChar and targetChar:FindFirstChild("HumanoidRootPart")
                    local myChar=S.player.Character
                    local myHRP=myChar and myChar:FindFirstChild("HumanoidRootPart")
                    if targetHRP and myHRP and not S.cuffInProgress then
                        local distance=(myHRP.Position-targetHRP.Position).Magnitude
                        if distance>18 or (tick()-(S.lastCuffTime or 0)>4) then
                            if executeCuff(target) then S.lastCuffTime=tick() end
                        end
                    end
                end
                task.wait(0.15)
            end
            S.autoCuffMonitorLoop=nil
            S.autoCuffMonitorAlive=false
        end)
        S.autoCuffLoop=S.autoCuffMonitorLoop
    end

    -- Strong supervisor: a transient error/respawn must never leave AutoCuff
    -- without a worker while the toggle is still ON.
    if not S.autoCuffSupervisorLoop then
        S.autoCuffSupervisorLoop=task.spawn(function()
            while S.remoteCommandAlive ~= false do
                task.wait(0.5)
                if S.autoCuffEnabled and not S.autoCuffMonitorAlive then
                    pcall(startAutoCuffWorker)
                end
                if S.autoCuffEnabled then
                    local target=S.selectedPlayer
                    if target and target~=boundAutoCuffTarget then pcall(bindAutoCuffTarget,target) end
                end
            end
        end)
    end

    S.autoCuffButton.Activated:Connect(function()
        S.autoCuffEnabled=not S.autoCuffEnabled
        if S.autoCuffEnabled then
            S.autoCuffButton.Text="إيقاف الكلبشه"
            S.autoCuffButton.BackgroundColor3=Color3.fromRGB(200,0,0)
            S.lastCuffTime=0
            startAutoCuffWorker()
        else
            stopAutoCuffWorker()
            S.autoCuffButton.Text="كلبشه تلقائيه"
            S.autoCuffButton.BackgroundColor3=Color3.fromRGB(0,200,0)
        end
    end)

    -- Reset the retry timer when the selected target respawns.
    S.autoCuffTargetConnection=nil
    bindAutoCuffTarget(S.selectedPlayer)

    for i, txt in ipairs({"x1","x2","x3"}) do
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.fromOffset(35,18)
        lbl.Position = UDim2.new(speedPoints[i],-17,0,280)
        lbl.BackgroundTransparency = 1
        lbl.Text = txt
        lbl.TextColor3 = Color3.fromRGB(210,220,240)
        lbl.TextSize = 12
        lbl.Font = Enum.Font.SourceSansBold
        lbl.TextXAlignment = Enum.TextXAlignment.Center
        lbl.Parent = sf3
    end

    S.selectedPlayer = nil
    S.isViewing = false
    S.isNV = false
    S.isRe = false
    S.isCopy1 = false
    S.isCopy2 = false
    S.nvLoop = nil
    S.reLoop = nil
    S.copy1Loop = nil
    S.copy2Loop = nil
    S.playerLeaveData = {}
    S.leftPlayersData = {}
    S.leavePopup = nil
end

-- ============================================================
-- SCOPE 14: TARGET SECTION - FUNCTIONS
-- ============================================================
do
    local S = _G.SQR
    
    function S.updateTargetUI(plr)
        if plr then
            if S.canTargetPlayer and not S.canTargetPlayer(plr) then
                return
            end
            S.selectedPlayer = plr
            S.nameLabel.Text = plr.DisplayName
            S.userLabel.Text = "@" .. plr.Name
            S.targetAvatar.Visible = true
            pcall(function()
                S.targetAvatar.Image = S.Players:GetUserThumbnailAsync(plr.UserId, Enum.ThumbnailType.AvatarBust, Enum.ThumbnailSize.Size150x150)
            end)
        else
            S.selectedPlayer = nil
            S.nameLabel.Text = "No player"
            S.userLabel.Text = ""
            S.targetAvatar.Image = ""
            S.targetAvatar.Visible = false
        end
    end
    
    function S.ShowLeavePopup(plr, leaveCount)
        if S.leavePopup then S.leavePopup:Destroy() end
        local popupGui = Instance.new("ScreenGui")
        popupGui.Name = "LeavePopup"
        popupGui.ResetOnSpawn = false
        popupGui.Parent = S.playerGui
        S.leavePopup = popupGui
        
        local popupFrame = Instance.new("Frame")
        popupFrame.Size = UDim2.new(0, 200, 0, 70)
        popupFrame.Position = UDim2.new(0.01, 10, 0.15, 0)
        popupFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        popupFrame.BackgroundTransparency = 0.1
        popupFrame.BorderSizePixel = 0
        popupFrame.Parent = popupGui
        
        local avatarImg = Instance.new("ImageLabel")
        avatarImg.Size = UDim2.new(0, 50, 0, 50)
        avatarImg.Position = UDim2.new(0, 10, 0.5, -25)
        avatarImg.BackgroundTransparency = 1
        avatarImg.BorderSizePixel = 0
        avatarImg.Image = S.Players:GetUserThumbnailAsync(plr.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size150x150)
        avatarImg.Parent = popupFrame
        
        local nameText = Instance.new("TextLabel")
        nameText.Size = UDim2.new(1, -70, 0.35, 0)
        nameText.Position = UDim2.new(0, 70, 0.05, 0)
        nameText.BackgroundTransparency = 1
        nameText.BorderSizePixel = 0
        nameText.Text = plr.Name
        nameText.TextColor3 = Color3.fromRGB(0, 0, 0)
        nameText.TextScaled = true
        nameText.Font = Enum.Font.SourceSansBold
        nameText.TextXAlignment = Enum.TextXAlignment.Left
        nameText.Parent = popupFrame
        
        local leaveText = Instance.new("TextLabel")
        leaveText.Size = UDim2.new(1, -70, 0.35, 0)
        leaveText.Position = UDim2.new(0, 70, 0.4, 0)
        leaveText.BackgroundTransparency = 1
        leaveText.BorderSizePixel = 0
        leaveText.Text = "Left " .. leaveCount .. " times"
        leaveText.TextColor3 = Color3.fromRGB(100, 100, 100)
        leaveText.TextScaled = true
        leaveText.Font = Enum.Font.SourceSans
        leaveText.TextXAlignment = Enum.TextXAlignment.Left
        leaveText.Parent = popupFrame
        
        popupFrame.Position = UDim2.new(0.01, -200, 0.15, 0)
        S.TweenService:Create(popupFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Position = UDim2.new(0.01, 10, 0.15, 0)
        }):Play()
        
        delay(5, function()
            S.TweenService:Create(popupFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
                Position = UDim2.new(0.01, -200, 0.15, 0)
            }):Play()
            delay(0.3, function()
                if S.leavePopup == popupGui then
                    popupGui:Destroy()
                    S.leavePopup = nil
                end
            end)
        end)
    end
    
    function S.ResetTargetActions()
        if S.autoCuffEnabled then
            S.autoCuffEnabled = false
            if S.autoCuffLoop then pcall(function() task.cancel(S.autoCuffLoop) end) end
            S.autoCuffLoop = nil
            if S.autoCuffButton and S.autoCuffButton.Parent then
                S.autoCuffButton.Text = "كلبشه تلقائيه"
                S.autoCuffButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            end
        end
        if S.isRe then
            S.isRe = false
            if S.reLoop then S.safeCancel(S.reLoop) end
            S.reButton.Text = "Re"
            S.reButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        end
        if S.isCopy1 then
            S.isCopy1 = false
            if S.copy1Loop then S.safeCancel(S.copy1Loop) end
            S.copy1Button.Text = "C1"
            S.copy1Button.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        end
        if S.isCopy2 then
            S.isCopy2 = false
            if S.copy2Loop then S.safeCancel(S.copy2Loop) end
            S.copy2Button.Text = "C2"
            S.copy2Button.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        end
        if S.BangActive or S.HeadBangActive then
            S.StopBang()
            S.bangButton.Text = "Bang"
            S.bangButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            S.headbangButton.Text = "Head"
            S.headbangButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        end
        if S.isViewing then
            S.isViewing = false
            S.viewButton.Text = "View"
            S.viewButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            pcall(function()
                S.Workspace.CurrentCamera.CameraSubject = S.player.Character
            end)
        end
    end
end

-- ============================================================
-- SCOPE 15: TARGET SECTION - EVENTS
-- ============================================================
do
    local S = _G.SQR
    
    -- Target scanner: rebuild the player list from Players:GetPlayers() on every search,
    -- so PC/Xeno does not depend on a stale player cache.
    local function findTargetMatches(query)
        query=tostring(query or ""):lower():gsub("^%s+",""):gsub("%s+$","")
        local matches={}
        if query=="" then return matches end
        for _,plr in ipairs(S.Players:GetPlayers()) do
            if plr~=S.player and (not S.canTargetPlayer or S.canTargetPlayer(plr)) then
                local name=tostring(plr.Name or ""):lower()
                local display=tostring(plr.DisplayName or ""):lower()
                if name==query or display==query or name:find(query,1,true) or display:find(query,1,true) then
                    table.insert(matches,plr)
                end
            end
        end
        return matches
    end
    S.findTargetMatches=findTargetMatches

    local function scanAndSelectTarget()
        local matches=findTargetMatches(S.searchBox.Text)
        if matches[1] then
            S.updateTargetUI(matches[1])
        elseif tostring(S.searchBox.Text or ""):gsub("%s+","")=="" then
            S.updateTargetUI(nil)
        else
            S.updateTargetUI(nil)
        end
        return matches
    end

    S.searchBox:GetPropertyChangedSignal("Text"):Connect(scanAndSelectTarget)
    S.searchBox.FocusLost:Connect(function() scanAndSelectTarget() end)

    -- Keep the player roster fresh; no stale selected-player lookup.
    S.Players.PlayerAdded:Connect(function()
        task.spawn(function()
            if S.searchBox and tostring(S.searchBox.Text or "")~="" then scanAndSelectTarget() end
        end)
    end)
    
    S.Players.PlayerRemoving:Connect(function(plr)
        -- Remove the user from local/remote presence immediately so Owner UI
        -- does not keep an offline player until the normal timeout.
        pcall(function()
            local uid=tostring(plr.UserId)
            S.cloudRecord.activeUsers=S.cloudRecord.activeUsers or {}
            S.cloudRecord.removedUsers=S.cloudRecord.removedUsers or {}
            S.cloudRecord.activeUsers[uid]=nil
            S.cloudRecord.removedUsers[uid]=true
            S.ownerKnownUsers[uid]=nil
            if S.ownerRefresh then S.ownerRefresh() end
            task.spawn(function() pcall(function() S.putCloudRecord(S.cloudRecord) end) end)
        end)
        if plr == S.selectedPlayer then
            if not S.playerLeaveData[plr] then S.playerLeaveData[plr] = 0 end
            S.playerLeaveData[plr] = S.playerLeaveData[plr] + 1
            S.leftPlayersData[plr] = {
                shouldReselect = true,
                leaveCount = S.playerLeaveData[plr]
            }
            S.ShowLeavePopup(plr, S.playerLeaveData[plr])
            S.ResetTargetActions()
            S.updateTargetUI(nil)
        end
    end)
    
    S.Players.PlayerAdded:Connect(function(plr)
        if S.leftPlayersData[plr] and S.leftPlayersData[plr].shouldReselect then
            wait(0.5)
            if not S.selectedPlayer then S.updateTargetUI(plr) end
            S.leftPlayersData[plr] = nil
        end
    end)
end

-- ============================================================
-- SCOPE 16: TARGET SECTION - BUTTON EVENTS
-- ============================================================
do
    local S = _G.SQR
    
    S.viewButton.Activated:Connect(function()
        if not S.selectedPlayer then return end
        S.isViewing = not S.isViewing
        if S.isViewing then
            S.viewButton.Text = "Unview"
            S.viewButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
            pcall(function() S.Workspace.CurrentCamera.CameraSubject = S.selectedPlayer.Character end)
        else
            S.viewButton.Text = "View"
            S.viewButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            pcall(function() S.Workspace.CurrentCamera.CameraSubject = S.player.Character end)
        end
    end)
    
    S.teleportButton.Activated:Connect(function()
        if not S.selectedPlayer then return end
        if S.player.Character and S.selectedPlayer.Character then
            local root = S.player.Character:FindFirstChild("HumanoidRootPart")
            local targetRoot = S.selectedPlayer.Character:FindFirstChild("HumanoidRootPart")
            if root and targetRoot then
                root.CFrame = targetRoot.CFrame + Vector3.new(0, 2, 0)
            end
        end
    end)
    
    S.bangButton.Activated:Connect(function()
        if not S.selectedPlayer then return end
        if S.BangActive then
            S.StopBang()
            S.bangButton.Text = "Bang"
            S.bangButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        else
            S.StopBang()
            S.StartBang(S.selectedPlayer)
            S.headbangButton.Text = "Head"
            S.headbangButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            S.bangButton.Text = "Stop"
            S.bangButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
        end
    end)
    
    S.headbangButton.Activated:Connect(function()
        if not S.selectedPlayer then return end
        if S.HeadBangActive then
            S.StopBang()
            S.headbangButton.Text = "Head"
            S.headbangButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        else
            S.StopBang()
            S.StartHeadBang(S.selectedPlayer)
            S.bangButton.Text = "Bang"
            S.bangButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            S.headbangButton.Text = "Stop"
            S.headbangButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
        end
    end)
    
    S.nvButton.Activated:Connect(function()
        if not S.selectedPlayer then return end
        S.isNV = not S.isNV
        if S.isNV then
            S.nvButton.Text = "Stop"
            S.nvButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
            local targetName = S.selectedPlayer.Name
            local commands = {"!nv " .. targetName, "!re " .. targetName, "!warp " .. targetName}
            local index = 1
            S.nvLoop = spawn(function()
                while S.isNV and S.selectedPlayer and S.selectedPlayer.Parent do
                    S.sendCommand(commands[index])
                    index = index % #commands + 1
                    task.wait(0.6)
                end
            end)
        else
            S.nvButton.Text = "NV"
            S.nvButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            if S.nvLoop then pcall(function() S.safeCancel(S.nvLoop) end); S.nvLoop = nil end
        end
    end)
    
    S.reButton.Activated:Connect(function()
        if not S.selectedPlayer then return end
        S.isRe = not S.isRe
        if S.isRe then
            S.reButton.Text = "Stop"
            S.reButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
            local targetName = S.selectedPlayer.Name
            S.reLoop = spawn(function()
                while S.isRe do
                    S.sendCommand("!re " .. targetName)
                    task.wait(0.6)
                end
            end)
        else
            S.reButton.Text = "Re"
            S.reButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            if S.reLoop then S.safeCancel(S.reLoop) end
        end
    end)
    
    S.copy1Button.Activated:Connect(function()
        if not S.selectedPlayer then return end
        S.isCopy1 = not S.isCopy1
        if S.isCopy1 then
            S.copy1Button.Text = "Stop"
            S.copy1Button.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
            local targetName = S.selectedPlayer.Name
            local cmds = {
                "!size " .. targetName .. " 3",
                "!thin " .. targetName,
                "!neon " .. targetName,
                "!color " .. targetName .. " pk",
                "!aura " .. targetName,
                "!titlepk " .. targetName .. " mm7wn"
            }
            local index = 1
            S.copy1Loop = spawn(function()
                while S.isCopy1 do
                    S.sendCommand(cmds[index])
                    index = index % #cmds + 1
                    wait(0.4)
                end
            end)
        else
            S.copy1Button.Text = "C1"
            S.copy1Button.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            if S.copy1Loop then S.safeCancel(S.copy1Loop) end
        end
    end)
    
    S.copy2Button.Activated:Connect(function()
        if not S.selectedPlayer then return end
        S.isCopy2 = not S.isCopy2
        if S.isCopy2 then
            S.copy2Button.Text = "Stop"
            S.copy2Button.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
            local targetName = S.selectedPlayer.Name
            local cmds = {
                "!fat " .. targetName,
                "!height " .. targetName .. " 0",
                "!neon " .. targetName,
                "!color " .. targetName .. " pk",
                "!aura " .. targetName
            }
            local index = 1
            S.copy2Loop = spawn(function()
                while S.isCopy2 do
                    S.sendCommand(cmds[index])
                    index = index % #cmds + 1
                    task.wait(0.4)
                end
            end)
        else
            S.copy2Button.Text = "C2"
            S.copy2Button.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            if S.copy2Loop then S.safeCancel(S.copy2Loop) end
        end
    end)
end

-- ============================================================
-- SCOPE 17: HD SECTION
-- ============================================================
do
    local S = _G.SQR
    local sf4 = S.sectionFrames[table.find(S.sections, "HD")]
    if sf4 then sf4.ClipsDescendants=false; sf4.Active=true; sf4.ZIndex=40 end

    S.cmdBox = Instance.new("TextBox")
    S.cmdBox.Size = UDim2.new(1, -20, 0.15, 0)
    S.cmdBox.Position = UDim2.new(0, 10, 0.02, 0)
    S.cmdBox.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    S.cmdBox.BorderSizePixel = 0
    S.cmdBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.cmdBox.PlaceholderText = "Command..."
    S.cmdBox.Text = ""
    S.cmdBox.ClearTextOnFocus = false
    S.cmdBox.Font = Enum.Font.SourceSans
    S.cmdBox.TextSize = 16
    S.cmdBox.Active = true
    S.cmdBox.ZIndex = 50
    S.cmdBox.Parent = sf4
    Instance.new("UICorner", S.cmdBox).CornerRadius = UDim.new(0, 10)

    -- Quick message fill controls requested: type a command here, press + to
    -- put it into every Message field, or - to clear every Message field.
    S.hdMessageCommandBox = Instance.new("TextBox")
    S.hdMessageCommandBox.Size = UDim2.new(0.34, 0, 0.12, 0)
    S.hdMessageCommandBox.Position = UDim2.new(0.27, 0, 0.88, 0)
    S.hdMessageCommandBox.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    S.hdMessageCommandBox.BorderSizePixel = 0
    S.hdMessageCommandBox.TextColor3 = Color3.fromRGB(255,255,255)
    S.hdMessageCommandBox.PlaceholderText = "Message command..."
    S.hdMessageCommandBox.Text = ""
    S.hdMessageCommandBox.ClearTextOnFocus = false
    S.hdMessageCommandBox.Font = Enum.Font.SourceSans
    S.hdMessageCommandBox.TextSize = 15
    S.hdMessageCommandBox.Active = true
    S.hdMessageCommandBox.Selectable = true
    S.hdMessageCommandBox.TextEditable = true
    S.hdMessageCommandBox.ZIndex = 10000
    S.hdMessageCommandBox.Visible = true
    S.hdMessageCommandBox.Active = true
    S.hdMessageCommandBox.Selectable = true
    S.hdMessageCommandBox.Parent = sf4
    S.hdMessageCommandBox.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            task.defer(function()
                if S.hdMessageCommandBox and S.hdMessageCommandBox.Parent then
                    pcall(function() S.hdMessageCommandBox:CaptureFocus() end)
                end
            end)
        end
    end)
    Instance.new("UICorner", S.hdMessageCommandBox).CornerRadius = UDim.new(0, 10)

    S.hdMessageSetButton = Instance.new("TextButton")
    S.hdMessageSetButton.Size = UDim2.new(0.10, 0, 0.12, 0)
    S.hdMessageSetButton.Position = UDim2.new(0.63, 0, 0.88, 0)
    S.hdMessageSetButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    S.hdMessageSetButton.BorderSizePixel = 0
    S.hdMessageSetButton.TextColor3 = Color3.fromRGB(255,255,255)
    S.hdMessageSetButton.Text = "+"
    S.hdMessageSetButton.TextScaled = true
    S.hdMessageSetButton.Font = Enum.Font.SourceSansBold
    S.hdMessageSetButton.Parent = sf4
    Instance.new("UICorner", S.hdMessageSetButton).CornerRadius = UDim.new(0, 10)

    S.hdMessageClearButton = Instance.new("TextButton")
    S.hdMessageClearButton.Size = UDim2.new(0.10, 0, 0.12, 0)
    S.hdMessageClearButton.Position = UDim2.new(0.75, 0, 0.88, 0)
    S.hdMessageClearButton.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
    S.hdMessageClearButton.BorderSizePixel = 0
    S.hdMessageClearButton.TextColor3 = Color3.fromRGB(255,255,255)
    S.hdMessageClearButton.Text = "-"
    S.hdMessageClearButton.TextScaled = true
    S.hdMessageClearButton.Font = Enum.Font.SourceSansBold
    S.hdMessageClearButton.Parent = sf4
    Instance.new("UICorner", S.hdMessageClearButton).CornerRadius = UDim.new(0, 10)

    S.hdSpeedBox = Instance.new("TextBox")
    S.hdSpeedBox.Size = UDim2.new(0.30, 0, 0.12, 0)
    S.hdSpeedBox.Position = UDim2.new(0.05, 0, 0.2, 0)
    S.hdSpeedBox.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    S.hdSpeedBox.BorderSizePixel = 0
    S.hdSpeedBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.hdSpeedBox.PlaceholderText = "Speed (s)"
    S.hdSpeedBox.Text = "0.01"
    S.hdSpeedBox.ClearTextOnFocus = false
    S.hdSpeedBox.Font = Enum.Font.SourceSans
    S.hdSpeedBox.TextSize = 16
    S.hdSpeedBox.Active = true
    S.hdSpeedBox.ZIndex = 60
    S.hdSpeedBox.Visible = true
    S.hdSpeedBox.Parent = sf4
    Instance.new("UICorner", S.hdSpeedBox).CornerRadius = UDim.new(0, 10)

    S.hdSendButton = Instance.new("TextButton")
    S.hdSendButton.Size = UDim2.new(0.30, 0, 0.12, 0)
    S.hdSendButton.Position = UDim2.new(0.35, 0, 0.2, 0)
    S.hdSendButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    S.hdSendButton.BorderSizePixel = 0
    S.hdSendButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.hdSendButton.Text = "Send"
    S.hdSendButton.TextScaled = true
    S.hdSendButton.Font = Enum.Font.SourceSansBold
    S.hdSendButton.Active = true
    S.hdSendButton.AutoButtonColor = true
    S.hdSendButton.ZIndex = 80
    S.hdSendButton.Parent = sf4
    Instance.new("UICorner", S.hdSendButton).CornerRadius = UDim.new(0, 10)

    S.hdSpamButton = Instance.new("TextButton")
    S.hdSpamButton.Size = UDim2.new(0.30, 0, 0.12, 0)
    S.hdSpamButton.Position = UDim2.new(0.68, 0, 0.2, 0)
    S.hdSpamButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    S.hdSpamButton.BorderSizePixel = 0
    S.hdSpamButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.hdSpamButton.Text = "Spam"
    S.hdSpamButton.TextScaled = true
    S.hdSpamButton.Font = Enum.Font.SourceSansBold
    S.hdSpamButton.Active = true
    S.hdSpamButton.AutoButtonColor = true
    S.hdSpamButton.ZIndex = 80
    S.hdSpamButton.Parent = sf4
    Instance.new("UICorner", S.hdSpamButton).CornerRadius = UDim.new(0, 10)

    -- أزرار + و - و Start الخاصة برسائل الـ Spam المتعددة.
    S.hdSpamControls = Instance.new("Frame")
    S.hdSpamControls.Size = UDim2.new(1, -20, 0, 38)
    S.hdSpamControls.Position = UDim2.new(0, 10, 0.34, 0)
    S.hdSpamControls.BackgroundTransparency = 1
    S.hdSpamControls.BorderSizePixel = 0
    S.hdSpamControls.ZIndex = 40
    S.hdSpamControls.Active = true
    S.hdSpamControls.Parent = sf4

    S.hdPlusButton = Instance.new("TextButton")
    S.hdPlusButton.Size = UDim2.fromOffset(55, 35)
    S.hdPlusButton.Position = UDim2.fromOffset(0, 0)
    S.hdPlusButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    S.hdPlusButton.BorderSizePixel = 0
    S.hdPlusButton.TextColor3 = Color3.fromRGB(255,255,255)
    S.hdPlusButton.Text = "+"
    S.hdPlusButton.TextScaled = true
    S.hdPlusButton.Font = Enum.Font.SourceSansBold
    S.hdPlusButton.ZIndex = 80
    S.hdPlusButton.Parent = S.hdSpamControls
    Instance.new("UICorner", S.hdPlusButton).CornerRadius = UDim.new(0,9)

    S.hdMinusButton = Instance.new("TextButton")
    S.hdMinusButton.Size = UDim2.fromOffset(55, 35)
    S.hdMinusButton.Position = UDim2.fromOffset(65, 0)
    S.hdMinusButton.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
    S.hdMinusButton.BorderSizePixel = 0
    S.hdMinusButton.TextColor3 = Color3.fromRGB(255,255,255)
    S.hdMinusButton.Text = "-"
    S.hdMinusButton.TextScaled = true
    S.hdMinusButton.Font = Enum.Font.SourceSansBold
    S.hdMinusButton.ZIndex = 80
    S.hdMinusButton.Parent = S.hdSpamControls
    Instance.new("UICorner", S.hdMinusButton).CornerRadius = UDim.new(0,9)

    S.hdCountBox = Instance.new("TextBox")
    S.hdCountBox.Size = UDim2.fromOffset(75, 35)
    S.hdCountBox.Position = UDim2.fromOffset(255, 0)
    S.hdCountBox.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    S.hdCountBox.BorderSizePixel = 0
    S.hdCountBox.TextColor3 = Color3.fromRGB(255,255,255)
    S.hdCountBox.PlaceholderText = "Count"
    S.hdCountBox.Text = "1"
    S.hdCountBox.ClearTextOnFocus = false
    S.hdCountBox.Font = Enum.Font.SourceSans
    S.hdCountBox.TextSize = 14
    S.hdCountBox.TextXAlignment = Enum.TextXAlignment.Center
    S.hdCountBox.Active = true
    S.hdCountBox.ZIndex = 60
    S.hdCountBox.Parent = S.hdSpamControls
    Instance.new("UICorner", S.hdCountBox).CornerRadius = UDim.new(0,9)

    S.hdStartButton = Instance.new("TextButton")
    S.hdStartButton.Size = UDim2.fromOffset(120, 35)
    S.hdStartButton.Position = UDim2.fromOffset(130, 0)
    S.hdStartButton.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
    S.hdStartButton.BorderSizePixel = 0
    S.hdStartButton.TextColor3 = Color3.fromRGB(0,0,0)
    S.hdStartButton.Text = "Start"
    S.hdStartButton.TextScaled = true
    S.hdStartButton.Font = Enum.Font.SourceSansBold
    S.hdStartButton.ZIndex = 80
    S.hdStartButton.Active = true
    S.hdStartButton.AutoButtonColor = true
    S.hdStartButton.Parent = S.hdSpamControls
    Instance.new("UICorner", S.hdStartButton).CornerRadius = UDim.new(0,9)

    S.hdSpamList = Instance.new("ScrollingFrame")
    S.hdSpamList.Name = "SpamMessageList"
    S.hdSpamList.Size = UDim2.new(1, -20, 0, 145)
    S.hdSpamList.Position = UDim2.new(0, 10, 0.46, 0)
    S.hdSpamList.BackgroundColor3 = Color3.fromRGB(18, 21, 30)
    S.hdSpamList.BackgroundTransparency = 0.15
    S.hdSpamList.BorderSizePixel = 0
    S.hdSpamList.ScrollBarThickness = 7
    S.hdSpamList.ScrollingEnabled = true
    S.hdSpamList.Active = true
    S.hdSpamList.AutomaticCanvasSize = Enum.AutomaticSize.Y
    S.hdSpamList.ClipsDescendants = true
    S.hdSpamList.CanvasSize = UDim2.new(0,0,0,0)
    S.hdSpamList.ZIndex = 20
    S.hdSpamList.Parent = sf4
    Instance.new("UICorner", S.hdSpamList).CornerRadius = UDim.new(0,10)

    S.hdSpamLayout = Instance.new("UIListLayout")
    S.hdSpamLayout.Padding = UDim.new(0, 5)
    S.hdSpamLayout.SortOrder = Enum.SortOrder.LayoutOrder
    S.hdSpamLayout.Parent = S.hdSpamList

    local spamPadding = Instance.new("UIPadding")
    spamPadding.PaddingTop = UDim.new(0,5)
    spamPadding.PaddingBottom = UDim.new(0,5)
    spamPadding.PaddingLeft = UDim.new(0,5)
    spamPadding.PaddingRight = UDim.new(0,5)
    spamPadding.Parent = S.hdSpamList

    S.hdSpamBoxes = {}
    -- Separate states so Command Spam and Message Spam can run together.
    S.hdCommandSpamActive = false
    S.hdCommandSpamLoop = nil
    S.hdMessageSpamActive = false
    S.hdMessageSpamLoop = nil

    local function updateSpamCanvas()
        task.defer(function()
            if S.hdSpamList and S.hdSpamLayout then
                S.hdSpamList.CanvasSize = UDim2.new(0,0,0,S.hdSpamLayout.AbsoluteContentSize.Y + 20)
            end
        end)
    end

    local function createSpamBox(skipUpdate)
        local box = Instance.new("TextBox")
        box.Size = UDim2.new(1, -10, 0, 35)
        box.BackgroundColor3 = Color3.fromRGB(35, 40, 55)
        box.BorderSizePixel = 0
        box.TextColor3 = Color3.fromRGB(255,255,255)
        box.PlaceholderText = "Message " .. tostring(#S.hdSpamBoxes + 1)
        box.Text = ""
        box.ClearTextOnFocus = false
        box.Font = Enum.Font.SourceSans
        box.TextSize = 15
        box.ZIndex = 6
        box.Active = true
        box.ZIndex = 60
        box.Visible = true
        box.Parent = S.hdSpamList
        Instance.new("UICorner", box).CornerRadius = UDim.new(0,9)
        table.insert(S.hdSpamBoxes, box)
        if not skipUpdate then updateSpamCanvas() end
        return box
    end

    local function removeLastSpamBox(skipUpdate)
        local box = table.remove(S.hdSpamBoxes)
        if box then box:Destroy() end
        if not skipUpdate then updateSpamCanvas() end
    end

    local function setSpamBoxCount(targetCount)
        targetCount = math.clamp(math.floor(tonumber(targetCount) or 1), 1, 1000)
        while #S.hdSpamBoxes < targetCount do
            createSpamBox(true)
        end
        while #S.hdSpamBoxes > targetCount do
            removeLastSpamBox(true)
        end
        S.hdCountBox.Text = tostring(targetCount)
        updateSpamCanvas()
    end

    -- يبدأ بصندوق واحد. يمكن كتابة 100 أو 500 في Count لتكوين العدد المطلوب.
    createSpamBox()

    S.hdCountBox.FocusLost:Connect(function()
        local n = tonumber(S.hdCountBox.Text)
        if not n then
            S.hdCountBox.Text = tostring(#S.hdSpamBoxes)
            return
        end
        setSpamBoxCount(n)
    end)

    S.hdMessageSetButton.MouseButton1Click:Connect(function()
        local command = tostring(S.hdMessageCommandBox.Text or "")
        if command == "" then return end
        for _, box in ipairs(S.hdSpamBoxes) do
            if box and box.Parent then box.Text = command end
        end
    end)

    S.hdMessageClearButton.MouseButton1Click:Connect(function()
        for _, box in ipairs(S.hdSpamBoxes) do
            if box and box.Parent then box.Text = "" end
        end
    end)

    S.hdPlusButton.Activated:Connect(function()
        createSpamBox()
        S.hdCountBox.Text = tostring(#S.hdSpamBoxes)
    end)

    S.hdMinusButton.Activated:Connect(function()
        if #S.hdSpamBoxes > 1 then
            removeLastSpamBox()
            S.hdCountBox.Text = tostring(#S.hdSpamBoxes)
        end
    end)

    S.hdSendButton.MouseButton1Click:Connect(function()
        local cmd = S.cmdBox.Text
        if cmd == "" then return end
        S.sendCommand(cmd)
    end)

    -- Command Spam: مستقل تماماً عن Message Spam.
    S.hdSpamButton.MouseButton1Click:Connect(function()
        if S.hdCommandSpamActive then
            S.hdCommandSpamActive = false
            if S.hdCommandSpamLoop then S.safeCancel(S.hdCommandSpamLoop); S.hdCommandSpamLoop = nil end
            S._hdCommandQueue = {}
            S.hdSpamButton.Text = "Spam"
            S.hdSpamButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            return
        end
        local cmd = tostring(S.cmdBox.Text or "")
        if cmd == "" then return end
        local speed = 0.6
        S._hdCommandQueue = {}
        S.hdCommandSpamActive = true
        S.hdSpamButton.Text = "Stop"
        S.hdSpamButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
        S.hdCommandSpamLoop = task.spawn(function()
            while S.hdCommandSpamActive do
                -- اقرأ الخانة في كل دورة حتى يتغير الأمر فوراً ولا يستمر الأمر القديم.
                local liveCmd = tostring(S.cmdBox.Text or "")
                if liveCmd ~= "" then
                    S.sendCommand(liveCmd)
                end
                task.wait(speed)
            end
            -- امسح أي أوامر قديمة بقيت في الطابور حتى لا تظهر بعد تغيير/إيقاف الأمر.
            S._hdCommandQueue = {}
            S.hdCommandSpamLoop = nil
        end)
    end)

    -- Message Spam: مستقل تماماً عن Command Spam، ويمكن تشغيلهما معاً.
    S.hdStartButton.Activated:Connect(function()
        if S.hdMessageSpamActive then
            S.hdMessageSpamActive = false
            if S.hdMessageSpamLoop then S.safeCancel(S.hdMessageSpamLoop); S.hdMessageSpamLoop = nil end
            S.hdStartButton.Text = "Start"
            S.hdStartButton.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
            return
        end

        local messages = {}
        for _, box in ipairs(S.hdSpamBoxes) do
            local text = tostring(box.Text or "")
            if text ~= "" then table.insert(messages, text) end
        end
        if #messages == 0 then return end

        local speed = 0.6
        S.hdMessageSpamActive = true
        S.hdStartButton.Text = "Stop"
        S.hdStartButton.BackgroundColor3 = Color3.fromRGB(200, 160, 0)
        S.hdMessageSpamLoop = task.spawn(function()
            local index = 1
            while S.hdMessageSpamActive do
                local message = messages[index]
                if message and message ~= "" then
                    S.sendCommand(message)
                end
                index = index + 1
                if index > #messages then index = 1 end
                task.wait(speed)
                if index == 1 then
                    messages = {}
                    for _, box in ipairs(S.hdSpamBoxes) do
                        local text = tostring(box.Text or "")
                        if text ~= "" then table.insert(messages, text) end
                    end
                    if #messages == 0 then break end
                    if index > #messages then index = 1 end
                end
            end
            S.hdMessageSpamActive = false
            S.hdMessageSpamLoop = nil
            if S.hdStartButton and S.hdStartButton.Parent then
                S.hdStartButton.Text = "Start"
                S.hdStartButton.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
            end
        end)
    end)

    S.hdSpamLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(updateSpamCanvas)
end

-- SCOPE 18: CMD BAR
-- ============================================================
do
    local S = _G.SQR
    local sf4 = S.sectionFrames[table.find(S.sections, "HD")]
    
    S.cmdBarButton = Instance.new("TextButton")
    S.cmdBarButton.Size = UDim2.new(0.22, 0, 0.12, 0)
    S.cmdBarButton.Position = UDim2.new(0.02, 0, 0.88, 0)
    S.cmdBarButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    S.cmdBarButton.BorderSizePixel = 0
    S.cmdBarButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.cmdBarButton.Text = "CmdBar"
    S.cmdBarButton.TextScaled = true
    S.cmdBarButton.Font = Enum.Font.SourceSansBold
    S.cmdBarButton.Parent = sf4
    Instance.new("UICorner", S.cmdBarButton).CornerRadius = UDim.new(0, 10)
    
    S.cmdBarInstances = {}
    
    S.cmdBarButton.MouseButton1Click:Connect(function()
        local cmdBarGui = Instance.new("ScreenGui")
        cmdBarGui.Name = "CmdBarGUI"
        cmdBarGui.ResetOnSpawn = false
        cmdBarGui.Parent = S.player:WaitForChild("PlayerGui")
        table.insert(S.cmdBarInstances, cmdBarGui)
        
        local f = Instance.new("Frame")
        f.Size = UDim2.new(0, 320, 0, 150)
        f.Position = UDim2.new(0.5, -160, 0.5 + (#S.cmdBarInstances * 0.06), 0)
        f.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        f.BackgroundTransparency = 0.2
        f.BorderSizePixel = 0
        f.Active = true
        f.Draggable = true
        f.Parent = cmdBarGui
        Instance.new("UICorner", f).CornerRadius = UDim.new(0, 12)
        
        local titleCmd = Instance.new("TextLabel")
        titleCmd.Size = UDim2.new(1, 0, 0.12, 0)
        titleCmd.Position = UDim2.new(0, 0, 0, 0)
        titleCmd.BackgroundTransparency = 1
        titleCmd.BorderSizePixel = 0
        titleCmd.Text = "CMD BAR"
        titleCmd.TextColor3 = Color3.fromRGB(0, 200, 0)
        titleCmd.TextScaled = true
        titleCmd.Font = Enum.Font.SourceSansBold
        titleCmd.Parent = f
        
        local txt = Instance.new("TextBox")
        txt.Size = UDim2.new(1, -10, 0.3, 0)
        txt.Position = UDim2.new(0, 5, 0.14, 0)
        txt.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
        txt.BorderSizePixel = 0
        txt.TextColor3 = Color3.fromRGB(255, 255, 255)
        txt.PlaceholderText = "Command (each line = command)"
        txt.Text = ""
        txt.ClearTextOnFocus = false
        txt.Font = Enum.Font.SourceSans
        txt.TextSize = 14
        txt.Active = true
        txt.ZIndex = 50
        txt.Parent = f
        Instance.new("UICorner", txt).CornerRadius = UDim.new(0, 8)
        
        local strengthBox = Instance.new("TextBox")
        strengthBox.Size = UDim2.new(0.3, 0, 0.18, 0)
        strengthBox.Position = UDim2.new(0.05, 0, 0.5, 0)
        strengthBox.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
        strengthBox.BorderSizePixel = 0
        strengthBox.TextColor3 = Color3.fromRGB(255, 255, 255)
        strengthBox.PlaceholderText = "Strength"
        strengthBox.Text = "5"
        strengthBox.ClearTextOnFocus = false
        strengthBox.Font = Enum.Font.SourceSans
        strengthBox.TextSize = 14
        strengthBox.Active = true
        strengthBox.ZIndex = 50
        strengthBox.Parent = f
        Instance.new("UICorner", strengthBox).CornerRadius = UDim.new(0, 8)
        
        local cmdBarBtn = Instance.new("TextButton")
        cmdBarBtn.Size = UDim2.new(0.3, 0, 0.18, 0)
        cmdBarBtn.Position = UDim2.new(0.4, 0, 0.5, 0)
        cmdBarBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        cmdBarBtn.BorderSizePixel = 0
        cmdBarBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        cmdBarBtn.Text = "Send"
        cmdBarBtn.TextScaled = true
        cmdBarBtn.Font = Enum.Font.SourceSansBold
        cmdBarBtn.Parent = f
        Instance.new("UICorner", cmdBarBtn).CornerRadius = UDim.new(0, 8)
        
        local closeBtnCmd = Instance.new("TextButton")
        closeBtnCmd.Size = UDim2.new(0, 25, 0, 20)
        closeBtnCmd.Position = UDim2.new(1, -30, 0, 5)
        closeBtnCmd.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
        closeBtnCmd.BorderSizePixel = 0
        closeBtnCmd.TextColor3 = Color3.fromRGB(255, 255, 255)
        closeBtnCmd.Text = "X"
        closeBtnCmd.TextScaled = true
        closeBtnCmd.Font = Enum.Font.SourceSansBold
        closeBtnCmd.Parent = f
        Instance.new("UICorner", closeBtnCmd).CornerRadius = UDim.new(0, 6)
        
        local isSending = false
        local loopTask = nil
        
        cmdBarBtn.MouseButton1Click:Connect(function()
            local cmd = txt.Text
            if cmd == "" then return end
            local strength = tonumber(strengthBox.Text)
            if not strength or strength < 1 or strength > 5 then
                pcall(function()
                    if S.ShowRankMessage then S.ShowRankMessage("لايمكن تحديد عدد اكثر او اقل من 5-1") end
                end)
                strengthBox.Text = "5"
                return
            end
            strength = math.floor(strength)
            isSending = not isSending
            if isSending then
                cmdBarBtn.Text = "Stop"
                cmdBarBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
                loopTask = task.spawn(function()
                    while isSending do
                        -- Strength 1..5 = عدد الحلقات في كل دورة.
                        -- كل إرسال يفصل عنه 0.001 ثانية.
                        for i = 1, strength do
                            if not isSending then break end
                            for line in cmd:gmatch("[^\r\n]+") do
                                if not isSending then break end
                                S.sendCommand(line)
                                task.wait(0.005)
                            end
                        end
                        task.wait(0.005)
                    end
                end)
            else
                cmdBarBtn.Text = "Send"
                cmdBarBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
                if loopTask then S.safeCancel(loopTask) end
            end
        end)
        
        closeBtnCmd.MouseButton1Click:Connect(function()
            isSending = false
            if loopTask then S.safeCancel(loopTask) end
            cmdBarGui:Destroy()
            for i, inst in ipairs(S.cmdBarInstances) do
                if inst == cmdBarGui then
                    table.remove(S.cmdBarInstances, i)
                    break
                end
            end
        end)
    end)
end

-- ============================================================
-- SCOPE 19: SECTION NAVIGATION HELPER
-- ============================================================
do
    local S = _G.SQR
    
    function S.sectionAllowed(sectionName)
        if sectionName=="VIP" then return true end
        if sectionName=="Owner" then return true end
        return true
    end

    function S.refreshSectionVisibility()
        local currentName=S.sections[S.currentSection] or "Setting"
        if not S.sectionAllowed(currentName) then S.currentSection=1 end
        local slot=0
        for j,sf in ipairs(S.sectionFrames) do
            local name=S.sections[j]
            local allowed=S.sectionAllowed(name)
            local active=allowed and j==S.currentSection
            if sf then sf.Visible=active end
            local btn=S.sectionButtons[j]
            if btn then
                btn.Visible=allowed
                btn.Active=allowed
                if allowed then
                    slot=slot+1
                    btn.Position=UDim2.new(0.06,0,0.02+(slot-1)*0.082,0)
                end
                btn.BackgroundColor3=active and Color3.fromRGB(0,200,0) or Color3.fromRGB(30,30,45)
                btn.BackgroundTransparency=active and 0 or 0.4
                btn.TextColor3=active and Color3.fromRGB(255,255,255) or Color3.fromRGB(200,200,200)
                btn.TextTransparency=0
                btn.ZIndex=5
            end
        end
    end

    function S.switchSectionByName(sectionName)
        for i,name in ipairs(S.sections) do
            if name==sectionName then
                if not S.sectionAllowed(name) then return false end
                S.currentSection=i
                S.refreshSectionVisibility()
                return true
            end
        end
        return false
    end

-- ============================================================
end

-- ============================================================
-- ============================================================
-- SCOPE 20: NV / PROTECTION CENTER + SKIN AUTOMATION
-- ============================================================
do
    local S=_G.SQR
    local Players=S.Players
    local ReplicatedStorage=S.rs
    local CoreGui=S.CoreGui
    local TweenService=S.TweenService
    local LocalPlayer=S.player
    local PlayerGui=S.playerGui or LocalPlayer:WaitForChild("PlayerGui")

    -- ============================================================
    -- TESTED PROTECTION: AntiChat / AntiPlayer
    -- Uses event-driven updates instead of a permanent full scan.
    -- ============================================================
    S.antiChatActive=false
    S.antiPlayerActive=false
    S.antiPlayerConnections=S.antiPlayerConnections or {}
    S.antiChatConnections=S.antiChatConnections or {}

    local function clearConnectionList(list)
        for i=#list,1,-1 do
            pcall(function() list[i]:Disconnect() end)
            list[i]=nil
        end
    end

    local function hidePlayerCharacter(character)
        if not S.antiPlayerActive or not character then return end
        pcall(function()
            local humanoid=character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.DisplayDistanceType=Enum.HumanoidDisplayDistanceType.None
                humanoid.DisplayName=""
            end
        end)
        for _,obj in ipairs(character:GetDescendants()) do
            pcall(function()
                if obj:IsA("BasePart") then
                    obj.LocalTransparencyModifier=1
                    obj.CastShadow=false
                elseif obj:IsA("Decal") or obj:IsA("Texture") then
                    obj.Transparency=1
                elseif obj:IsA("BillboardGui") or obj:IsA("SurfaceGui") then
                    obj.Enabled=false
                elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam")
                    or obj:IsA("Smoke") or obj:IsA("Fire") or obj:IsA("Sparkles")
                    or obj:IsA("Highlight") then
                    obj.Enabled=false
                end
            end)
        end
    end

    local function showPlayerCharacter(character)
        if not character then return end
        for _,obj in ipairs(character:GetDescendants()) do
            pcall(function()
                if obj:IsA("BasePart") then
                    obj.LocalTransparencyModifier=0
                    obj.CastShadow=true
                elseif obj:IsA("Decal") or obj:IsA("Texture") then
                    obj.Transparency=0
                elseif obj:IsA("BillboardGui") or obj:IsA("SurfaceGui") then
                    obj.Enabled=true
                elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam")
                    or obj:IsA("Smoke") or obj:IsA("Fire") or obj:IsA("Sparkles")
                    or obj:IsA("Highlight") then
                    obj.Enabled=true
                end
            end)
        end
        pcall(function()
            local humanoid=character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.DisplayDistanceType=Enum.HumanoidDisplayDistanceType.Viewer
            end
        end)
    end

    local function watchAntiPlayer(player)
        if player==LocalPlayer then return end

        local function watchCharacter(character)
            if not character then return end
            -- CharacterAdded can fire before the character has all of its parts.
            -- Hide immediately, then retry briefly so newly joined players do not remain visible.
            task.spawn(function()
                for i=1,8 do
                    if not S.antiPlayerActive or not character.Parent then break end
                    hidePlayerCharacter(character)
                    task.wait(0.15)
                end
            end)
            table.insert(S.antiPlayerConnections, character.DescendantAdded:Connect(function(obj)
                if not S.antiPlayerActive then return end
                pcall(function()
                    if obj:IsA("BasePart") then obj.LocalTransparencyModifier=1; obj.CastShadow=false
                    elseif obj:IsA("Decal") or obj:IsA("Texture") then obj.Transparency=1
                    elseif obj:IsA("BillboardGui") or obj:IsA("SurfaceGui") then obj.Enabled=false
                    elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam")
                        or obj:IsA("Smoke") or obj:IsA("Fire") or obj:IsA("Sparkles") or obj:IsA("Highlight") then obj.Enabled=false end
                end)
            end))
        end

        table.insert(S.antiPlayerConnections, player.CharacterAdded:Connect(watchCharacter))
        if player.Character then watchCharacter(player.Character) end
    end

    local function setAntiPlayer(state)
        S.antiPlayerActive=state
        clearConnectionList(S.antiPlayerConnections)
        if state then
            for _,plr in ipairs(Players:GetPlayers()) do
                if plr~=LocalPlayer then watchAntiPlayer(plr) end
            end
            table.insert(S.antiPlayerConnections, Players.PlayerAdded:Connect(function(plr)
                if plr~=LocalPlayer and S.antiPlayerActive then watchAntiPlayer(plr) end
            end))
        else
            for _,plr in ipairs(Players:GetPlayers()) do
                if plr~=LocalPlayer and plr.Character then showPlayerCharacter(plr.Character) end
            end
        end
    end

    local chatNameWords={"chat","chatframe","chatwindow","chatbox","chatbar","chatcontainer","chatgui","bubblechat","bubble","messaging","textchat"}
    local function isSQRGuiObject(obj)
        local cur=obj
        for _=1,8 do
            if not cur then break end
            local n=tostring(cur.Name or ""):lower()
            if n=="sqr_ui" or n=="protectioncenterui" or n:find("cmdbar",1,true) then return true end
            cur=cur.Parent
        end
        return false
    end
    local function looksLikeChatName(name)
        name=tostring(name or ""):lower()
        for _,word in ipairs(chatNameWords) do
            if name==word or name:find(word,1,true) then return true end
        end
        return false
    end
    local function setGuiHidden(obj,hidden)
        pcall(function()
            if obj:IsA("ScreenGui") or obj:IsA("BillboardGui") or obj:IsA("SurfaceGui") then
                obj.Enabled=not hidden
            elseif obj:IsA("GuiObject") then
                obj.Visible=not hidden
            end
        end)
    end

    local function hideOwnerMessage(obj)
        pcall(function()
            if isSQRGuiObject(obj) then return end
            if not (obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox")) then return end

            local t=tostring(obj.Text or ""):lower()

            -- ForeverHD's message can contain several separate GUI objects:
            -- header, body text (for example "sdadaasdasd"), and a dark overlay.
            -- Detect the header/body and hide the whole notification card, not just
            -- the single text label.
            local isForeverHeader = t:find("message from foreverhd",1,true)
                or t:find("message from owner",1,true)
                or (t:find("message from",1,true) and (t:find("foreverhd",1,true) or t:find("owner",1,true)))
            local isForeverBody = t:find("settings prevent you from sending",1,true)
                or t:find("your chat settings prevent you from sending",1,true)

            if not (isForeverHeader or isForeverBody) then return end

            -- Walk upward to the notification's main GuiObject. Stop before SQR
            -- so the SQR interface is never hidden.
            local cur=obj
            local target=nil
            local previous=obj

            for _=1,12 do
                cur=cur.Parent
                if not cur then break end

                local n=tostring(cur.Name or ""):lower()
                if n=="sqr_ui" or n=="protectioncenterui" or n:find("cmdbar",1,true) then
                    break
                end

                if cur:IsA("ScreenGui") or cur:IsA("CoreGui") then
                    break
                end

                if cur:IsA("GuiObject") then
                    target=cur
                end

                previous=cur
            end

            -- Prefer the highest GuiObject below the ScreenGui. This removes the
            -- dark transparency, header, body text, buttons, and any companion UI
            -- belonging to the same ForeverHD notification.
            if target and target.Parent then
                target.Visible=false
            else
                obj.Visible=false
            end
            obj.Visible=false
        end)
    end
    local function scanChatContainer(container,hidden)
        if not container then return end
        for _,obj in ipairs(container:GetDescendants()) do
            if not isSQRGuiObject(obj) and looksLikeChatName(obj.Name) then setGuiHidden(obj,hidden) end
        end
        for _,obj in ipairs(container:GetChildren()) do
            if not isSQRGuiObject(obj) and looksLikeChatName(obj.Name) then setGuiHidden(obj,hidden) end
        end
    end
    local function setAntiChat(state)
        S.antiChatActive=state
        pcall(function() TextChatService.ChatWindowConfiguration.Enabled=not state end)
        pcall(function() TextChatService.BubbleChatConfiguration.Enabled=not state end)
        pcall(function() StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.Chat,not state) end)
        scanChatContainer(PlayerGui,state)
        scanChatContainer(CoreGui,state)
        for _,container in ipairs({PlayerGui,CoreGui}) do
            if container then
                for _,obj in ipairs(container:GetDescendants()) do hideOwnerMessage(obj) end
            end
        end
    end
    local function setupAntiChatConnections()
        clearConnectionList(S.antiChatConnections)
        table.insert(S.antiChatConnections, PlayerGui.DescendantAdded:Connect(function(obj)
            hideOwnerMessage(obj)
            if not isSQRGuiObject(obj) and S.antiChatActive and looksLikeChatName(obj.Name) then setGuiHidden(obj,true) end
        end))
        table.insert(S.antiChatConnections, CoreGui.DescendantAdded:Connect(function(obj)
            hideOwnerMessage(obj)
            if not isSQRGuiObject(obj) and S.antiChatActive and looksLikeChatName(obj.Name) then setGuiHidden(obj,true) end
        end))
    end
    setupAntiChatConnections()

    S.protectionGui=nil
    S.protectionFrame=nil
    S.nvActive=false
    S.warpActive=false
    S.protectionConnections=S.protectionConnections or {}
    S.protectionRequested=false

    -- Clean old NV/Protection UI if one exists.
    pcall(function()
        local old=CoreGui:FindFirstChild("ProtectionCenterUI")
        if old then old:Destroy() end
    end)

    -- HD Admin ExecuteClientCommand can appear late or be recreated on PC/Xeno.
    -- Resolve it repeatedly from the actual hierarchy instead of keeping a stale reference.
    local function resolveExecuteClientCommand()
        local folder=ReplicatedStorage:FindFirstChild("HDAdminHDClient")
        local signals=folder and folder:FindFirstChild("Signals")
        local execute=signals and signals:FindFirstChild("ExecuteClientCommand")
        if not execute then
            pcall(function()
                execute=ReplicatedStorage:FindFirstChild("ExecuteClientCommand",true)
            end)
        end
        if execute then
            S.hdFolder=folder or execute.Parent
            S.hdSignals=signals or execute.Parent
            S.executeCmd=execute
            return execute
        end
        return nil
    end

    task.spawn(function()
        for _=1,60 do
            if resolveExecuteClientCommand() then break end
            task.wait(0.25)
        end
    end)

    -- AntiNv/AntiWarp: disable the client-side ExecuteClientCommand listeners while
    -- protection is active. This is the reliable path on executors that support
    -- getconnections, and it is refreshed because Xeno may recreate the signal.
    local function setSignalState(disable)
        local execute=resolveExecuteClientCommand()
        if not execute or typeof(getconnections)~="function" then return false end
        local ok=pcall(function()
            local connections=getconnections(execute.OnClientEvent)
            for _,conn in ipairs(connections) do
                pcall(function()
                    if disable then
                        if conn.Disable then conn:Disable() end
                    else
                        if conn.Enable then conn:Enable() end
                    end
                end)
            end
        end)
        return ok
    end

    -- Also keep the exact firesignal interception from the supplied working code.
    -- It is installed only after ExecuteClientCommand is available.
    local function installNVHook()
        if _G.SQR_NV_FIRE_HOOK_INSTALLED then return end
        local execute=S.executeCmd
        if not execute or typeof(firesignal)~="function" or typeof(hookfunction)~="function" then return end
        local oldFireSignal=firesignal
        local installed=false
        local ok=pcall(function()
            hookfunction(firesignal,function(signal,...)
                local cur=_G.SQR
                if cur and signal==cur.executeCmd then
                    local args={...}
                    local cmdNames={}
                    for i=1,#args do
                        if type(args[i])=="string" then
                            local v=args[i]:lower()
                            cmdNames[v]=true
                        end
                    end
                    if cur.nvActive and (cmdNames["nightvision"] or cmdNames["nv"]) then return nil end
                    if cur.warpActive and cmdNames["warp"] then return nil end
                end
                return oldFireSignal(signal,...)
            end)
            installed=true
        end)
        if ok and installed then
            _G.SQR_NV_FIRE_HOOK_INSTALLED=true
            _G.SQR_NV_OLD_FIRESIGNAL=oldFireSignal
        end
    end

    -- Keep the protection alive without repeatedly scanning/disabling every
    -- connection during command spam. Re-apply only when the signal is replaced,
    -- plus a light fallback check.
    local function refreshProtectionSignal()
        local execute=resolveExecuteClientCommand()
        if execute then
            installNVHook()
            if S.nvActive or S.warpActive then
                setSignalState(true)
            end
        end
        return execute
    end

    task.spawn(function()
        local lastExecute=nil
        while task.wait(3) do
            local execute=resolveExecuteClientCommand()
            if execute~=lastExecute then
                lastExecute=execute
                if execute then
                    installNVHook()
                    if S.nvActive or S.warpActive then
                        setSignalState(true)
                    end
                end
            end
        end
    end)

    local function createProtectionUI()
        S.protectionRequested=true
        if S.protectionGui and S.protectionGui.Parent then
            S.protectionGui.Enabled=true
            if S.protectionFrame and S.protectionFrame.Parent then
                S.protectionFrame.Visible=true
            end
            return
        end

        local screenGui=Instance.new("ScreenGui")
        screenGui.Name="ProtectionCenterUI"
        screenGui.ResetOnSpawn=false
        screenGui.DisplayOrder=100001
        screenGui.IgnoreGuiInset=true
        local success,guiParent=pcall(function() return gethui() end)
        screenGui.Parent=(success and guiParent) or CoreGui
        S.protectionGui=screenGui

        local mainFrame=Instance.new("Frame")
        mainFrame.Parent=screenGui
        mainFrame.Size=UDim2.new(0,220,0,410)
        mainFrame.Position=UDim2.new(0.5,-110,0.4,0)
        mainFrame.BackgroundColor3=Color3.fromRGB(25,25,30)
        mainFrame.Active=true
        mainFrame.Draggable=true
        Instance.new("UICorner",mainFrame).CornerRadius=UDim.new(0,8)
        S.protectionFrame=mainFrame
        mainFrame.Visible=true
        screenGui.Enabled=true

        local title=Instance.new("TextLabel")
        title.Parent=mainFrame
        title.Size=UDim2.new(1,0,0,30)
        title.BackgroundTransparency=1
        title.Text="Protection Center"
        title.TextColor3=Color3.fromRGB(255,255,255)
        title.Font=Enum.Font.SourceSansBold
        title.TextSize=14

        local nvBtn=Instance.new("TextButton")
        nvBtn.Parent=mainFrame
        nvBtn.Size=UDim2.new(0.9,0,0,45)
        nvBtn.Position=UDim2.new(0.05,0,0,42)
        nvBtn.BackgroundColor3=Color3.fromRGB(178,34,34)
        nvBtn.TextColor3=Color3.fromRGB(255,255,255)
        nvBtn.Text=S.nvActive and "AntiNv: ON" or "AntiNv: OFF"
        nvBtn.Font=Enum.Font.SourceSansBold
        nvBtn.TextSize=13
        Instance.new("UICorner",nvBtn).CornerRadius=UDim.new(0,6)

        local warpBtn=Instance.new("TextButton")
        warpBtn.Parent=mainFrame
        warpBtn.Size=UDim2.new(0.9,0,0,45)
        warpBtn.Position=UDim2.new(0.05,0,0,94)
        warpBtn.BackgroundColor3=Color3.fromRGB(178,34,34)
        warpBtn.TextColor3=Color3.fromRGB(255,255,255)
        warpBtn.Text=S.warpActive and "AntiWarp: ON" or "AntiWarp: OFF"
        warpBtn.Font=Enum.Font.SourceSansBold
        warpBtn.TextSize=13
        Instance.new("UICorner",warpBtn).CornerRadius=UDim.new(0,6)

        local antiLagBtn=Instance.new("TextButton")
        antiLagBtn.Parent=mainFrame
        antiLagBtn.Size=UDim2.new(0.9,0,0,40)
        antiLagBtn.Position=UDim2.new(0.05,0,0,198)
        antiLagBtn.BackgroundColor3=Color3.fromRGB(178,34,34)
        antiLagBtn.TextColor3=Color3.fromRGB(255,255,255)
        antiLagBtn.Text="AntiLag: OFF"
        antiLagBtn.Font=Enum.Font.SourceSansBold
        antiLagBtn.TextSize=13
        Instance.new("UICorner",antiLagBtn).CornerRadius=UDim.new(0,6)

        local antiLagEnabled=false
        local antiLagRunning=false
        local antiLagState={
            parts={},
            decals={},
            effects={},
            postEffects={},
            atmospheres={},
            clouds={},
            workspaceClouds={},
            skies={},
            skyConnection=nil,
            terrainDecoration=nil,
            globalShadows=nil,
            environmentDiffuseScale=nil,
            environmentSpecularScale=nil,
            shadowSoftness=nil,
            qualityLevel=nil,
            lightingTechnology=nil,
            waterWaveSize=nil,
            waterWaveSpeed=nil,
            waterReflectance=nil,
            waterTransparency=nil,
            fakeFloor=nil,
            floorConnection=nil,
            applied=false
        }

        local playerCharacterModels={}
        local function rebuildPlayerCharacterModels()
            playerCharacterModels={}
            for _,plr in ipairs(S.Players:GetPlayers()) do
                if plr.Character then playerCharacterModels[plr.Character]=true end
            end
        end
        local function isPlayerCharacterPart(obj)
            local model=obj and obj:FindFirstAncestorOfClass("Model")
            return model and playerCharacterModels[model] == true
        end

        local function antiLagRestore()
            pcall(function()
                if antiLagState.skyConnection then antiLagState.skyConnection:Disconnect(); antiLagState.skyConnection=nil end
                for obj,props in pairs(antiLagState.parts) do
                    if obj and obj.Parent then
                        obj.LocalTransparencyModifier=props.transparency
                        obj.CanCollide=props.canCollide
                        obj.CanTouch=props.canTouch
                        obj.CanQuery=props.canQuery
                        if props.castShadow~=nil then obj.CastShadow=props.castShadow end
                    end
                end
                for obj,value in pairs(antiLagState.decals) do if obj and obj.Parent then obj.Transparency=value end end
                for obj,value in pairs(antiLagState.effects) do if obj and obj.Parent then obj.Enabled=value end end
                for obj,value in pairs(antiLagState.postEffects) do if obj and obj.Parent then obj.Enabled=value end end
                for obj,value in pairs(antiLagState.atmospheres) do if obj and obj.Parent then obj.Density=value end end
                for obj,value in pairs(antiLagState.clouds) do if obj and obj.Parent then obj.Enabled=value end end
                for obj,value in pairs(antiLagState.workspaceClouds) do if obj and obj.Parent then obj.Enabled=value end end
                for obj,parent in pairs(antiLagState.skies) do
                    if obj and obj.Parent==nil then obj.Parent=parent end
                end
                if antiLagState.floorConnection then
                    antiLagState.floorConnection:Disconnect()
                    antiLagState.floorConnection=nil
                end
                if antiLagState.fakeFloor then
                    antiLagState.fakeFloor:Destroy()
                    antiLagState.fakeFloor=nil
                end
                local Lighting=game:GetService("Lighting")
                if antiLagState.globalShadows~=nil then Lighting.GlobalShadows=antiLagState.globalShadows end
                if antiLagState.environmentDiffuseScale~=nil then Lighting.EnvironmentDiffuseScale=antiLagState.environmentDiffuseScale end
                if antiLagState.environmentSpecularScale~=nil then Lighting.EnvironmentSpecularScale=antiLagState.environmentSpecularScale end
                if antiLagState.shadowSoftness~=nil then Lighting.ShadowSoftness=antiLagState.shadowSoftness end
                if antiLagState.lightingTechnology~=nil then pcall(function() Lighting.Technology=antiLagState.lightingTechnology end) end
                if antiLagState.qualityLevel~=nil then
                    pcall(function() settings().Rendering.QualityLevel=antiLagState.qualityLevel end)
                end
                local terrain=workspace:FindFirstChildOfClass("Terrain")
                if terrain then
                    if antiLagState.terrainDecoration~=nil then terrain.Decoration=antiLagState.terrainDecoration end
                    if antiLagState.waterWaveSize~=nil then terrain.WaterWaveSize=antiLagState.waterWaveSize end
                    if antiLagState.waterWaveSpeed~=nil then terrain.WaterWaveSpeed=antiLagState.waterWaveSpeed end
                    if antiLagState.waterReflectance~=nil then terrain.WaterReflectance=antiLagState.waterReflectance end
                    if antiLagState.waterTransparency~=nil then terrain.WaterTransparency=antiLagState.waterTransparency end
                end
            end)
            antiLagState.parts={}
            antiLagState.decals={}
            antiLagState.effects={}
            antiLagState.postEffects={}
            antiLagState.atmospheres={}
            antiLagState.clouds={}
            antiLagState.workspaceClouds={}
            antiLagState.skies={}
            antiLagState.terrainDecoration=nil
            antiLagState.fakeFloor=nil
            antiLagState.floorConnection=nil
            antiLagState.globalShadows=nil
            antiLagState.environmentDiffuseScale=nil
            antiLagState.environmentSpecularScale=nil
            antiLagState.shadowSoftness=nil
            antiLagState.qualityLevel=nil
            antiLagState.lightingTechnology=nil
            antiLagState.waterWaveSize=nil
            antiLagState.waterWaveSpeed=nil
            antiLagState.waterReflectance=nil
            antiLagState.waterTransparency=nil
            antiLagState.applied=false
        end

        local function antiLagApply()
            if antiLagState.applied then return end
            antiLagState.applied=true

            local localChar=S.player.Character
            pcall(function()
                -- Aggressive LOCAL visual quality reduction. The server/map is not
                -- deleted; only the local rendering/collision load is reduced.
                for _,obj in ipairs(workspace:GetDescendants()) do
                    if localChar and obj:IsDescendantOf(localChar) then
                        continue
                    end
                    if obj:IsA("BasePart") then
                        antiLagState.parts[obj]={
                            transparency=obj.LocalTransparencyModifier,
                            canCollide=obj.CanCollide,
                            canTouch=obj.CanTouch,
                            canQuery=obj.CanQuery,
                            castShadow=obj.CastShadow
                        }
                        obj.LocalTransparencyModifier=1
                        obj.CanCollide=false
                        obj.CanTouch=false
                        obj.CanQuery=false
                        pcall(function() obj.CastShadow=false end)
                    elseif obj:IsA("Decal") or obj:IsA("Texture") then
                        antiLagState.decals[obj]=obj.Transparency
                        obj.Transparency=1
                    elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam")
                        or obj:IsA("Smoke") or obj:IsA("Fire") or obj:IsA("Sparkles") then
                        antiLagState.effects[obj]=obj.Enabled
                        obj.Enabled=false
                    elseif obj:IsA("Clouds") then
                        -- Clouds can live under Terrain/Workspace rather than Lighting.
                        antiLagState.workspaceClouds[obj]=obj.Enabled
                        obj.Enabled=false
                    end
                end
            end)

            pcall(function()
                local Lighting=game:GetService("Lighting")
                for _,obj in ipairs(Lighting:GetChildren()) do
                    if obj:IsA("PostEffect") then
                        antiLagState.postEffects[obj]=obj.Enabled
                        obj.Enabled=false
                    elseif obj:IsA("Atmosphere") then
                        antiLagState.atmospheres[obj]=obj.Density
                        obj.Density=0
                    elseif obj:IsA("Clouds") then
                        antiLagState.clouds[obj]=obj.Enabled
                        obj.Enabled=false
                    elseif obj:IsA("Sky") then
                        antiLagState.skies[obj]=obj.Parent
                        obj.Parent=nil
                    end
                end

                antiLagState.globalShadows=Lighting.GlobalShadows
                antiLagState.environmentDiffuseScale=Lighting.EnvironmentDiffuseScale
                antiLagState.environmentSpecularScale=Lighting.EnvironmentSpecularScale
                antiLagState.shadowSoftness=Lighting.ShadowSoftness
                pcall(function() antiLagState.lightingTechnology=Lighting.Technology end)
                Lighting.GlobalShadows=false
                Lighting.EnvironmentDiffuseScale=0
                Lighting.EnvironmentSpecularScale=0
                Lighting.ShadowSoftness=0
                pcall(function() Lighting.Technology=Enum.Technology.Compatibility end)
            end)

            pcall(function()
                local renderSettings=settings().Rendering
                antiLagState.qualityLevel=renderSettings.QualityLevel
                renderSettings.QualityLevel=Enum.QualityLevel.Level01
            end)

            pcall(function()
                local terrain=workspace:FindFirstChildOfClass("Terrain")
                if terrain then
                    antiLagState.terrainDecoration=terrain.Decoration
                    antiLagState.waterWaveSize=terrain.WaterWaveSize
                    antiLagState.waterWaveSpeed=terrain.WaterWaveSpeed
                    antiLagState.waterReflectance=terrain.WaterReflectance
                    antiLagState.waterTransparency=terrain.WaterTransparency
                    terrain.Decoration=false
                    terrain.WaterWaveSize=0
                    terrain.WaterWaveSpeed=0
                    terrain.WaterReflectance=0
                    terrain.WaterTransparency=1
                end
            end)

            -- Local-only floor so aggressive map hiding does not make the player fall.
            pcall(function()
                local floor=Instance.new("Part")
                floor.Name="AntiLag_LocalFloor"
                floor.Anchored=true
                floor.CanCollide=true
                floor.CanTouch=false
                floor.CanQuery=true
                floor.Size=Vector3.new(80,1,80)
                floor.Material=Enum.Material.SmoothPlastic
                floor.Transparency=0
                floor.TopSurface=Enum.SurfaceType.Smooth
                floor.BottomSurface=Enum.SurfaceType.Smooth
                floor.Parent=workspace
                antiLagState.fakeFloor=floor

                local function updateFloor()
                    if not antiLagEnabled or not floor.Parent then return end
                    local char=S.player.Character
                    local root=char and char:FindFirstChild("HumanoidRootPart")
                    if root then
                        floor.CFrame=CFrame.new(root.Position.X,root.Position.Y-3.2,root.Position.Z)
                    end
                end
                updateFloor()
                antiLagState.floorConnection=S.RunService.Heartbeat:Connect(updateFloor)
            end)
        end

        local function runAntiLag()
            if antiLagRunning then return end
            antiLagRunning=true
            task.spawn(function()
                antiLagApply()
                while antiLagEnabled do task.wait(1) end
                antiLagRestore()
                antiLagRunning=false
            end)
        end

        local sysBtn=Instance.new("TextButton")
        sysBtn.Parent=mainFrame
        sysBtn.Size=UDim2.new(0.9,0,0,40)
        sysBtn.Position=UDim2.new(0.05,0,0,146)
        sysBtn.BackgroundColor3=Color3.fromRGB(178,34,34)
        sysBtn.TextColor3=Color3.fromRGB(255,255,255)
        sysBtn.Text=S.systemCleanerProtectionActive and "AntiSys: ON" or "AntiSys: OFF"
        sysBtn.Font=Enum.Font.SourceSansBold
        sysBtn.TextSize=13
        Instance.new("UICorner",sysBtn).CornerRadius=UDim.new(0,6)

        if S.nvActive then nvBtn.BackgroundColor3=Color3.fromRGB(46,139,87) end
        if S.warpActive then warpBtn.BackgroundColor3=Color3.fromRGB(46,139,87) end
        if S.systemCleanerProtectionActive then sysBtn.BackgroundColor3=Color3.fromRGB(46,139,87) end
        if S.warpActive then warpBtn.BackgroundColor3=Color3.fromRGB(46,139,87) end

        local antiChatBtn=Instance.new("TextButton")
        antiChatBtn.Parent=mainFrame
        antiChatBtn.Size=UDim2.new(0.9,0,0,40)
        antiChatBtn.Position=UDim2.new(0.05,0,0,250)
        antiChatBtn.BackgroundColor3=Color3.fromRGB(178,34,34)
        antiChatBtn.TextColor3=Color3.fromRGB(255,255,255)
        antiChatBtn.Text="AntiChat: OFF"
        antiChatBtn.Font=Enum.Font.SourceSansBold
        antiChatBtn.TextSize=13
        Instance.new("UICorner",antiChatBtn).CornerRadius=UDim.new(0,6)

        local antiPlayerBtn=Instance.new("TextButton")
        antiPlayerBtn.Parent=mainFrame
        antiPlayerBtn.Size=UDim2.new(0.9,0,0,40)
        antiPlayerBtn.Position=UDim2.new(0.05,0,0,302)
        antiPlayerBtn.BackgroundColor3=Color3.fromRGB(178,34,34)
        antiPlayerBtn.TextColor3=Color3.fromRGB(255,255,255)
        antiPlayerBtn.Text="AntiPlayer: OFF"
        antiPlayerBtn.Font=Enum.Font.SourceSansBold
        antiPlayerBtn.TextSize=13
        Instance.new("UICorner",antiPlayerBtn).CornerRadius=UDim.new(0,6)

        local fpsLabel=Instance.new("TextLabel")
        fpsLabel.Parent=mainFrame
        fpsLabel.Size=UDim2.new(0.9,0,0,28)
        fpsLabel.Position=UDim2.new(0.05,0,0,350)
        fpsLabel.BackgroundTransparency=1
        fpsLabel.Text="FPS: --"
        fpsLabel.TextColor3=Color3.fromRGB(255,255,255)
        fpsLabel.Font=Enum.Font.SourceSansBold
        fpsLabel.TextSize=14
        fpsLabel.TextXAlignment=Enum.TextXAlignment.Center
        fpsLabel.ZIndex=10

        local fpsFrames=0
        local fpsElapsed=0
        local fpsLast=os.clock()
        S.fpsLabel=fpsLabel
        if S.fpsConnection then pcall(function() S.fpsConnection:Disconnect() end) end
        S.fpsConnection=S.RunService.Heartbeat:Connect(function(dt)
            if not S.remoteCommandAlive or not fpsLabel or not fpsLabel.Parent then
                if S.fpsConnection then pcall(function() S.fpsConnection:Disconnect() end) end
                return
            end
            fpsFrames=fpsFrames+1
            fpsElapsed=fpsElapsed+math.max(0,dt)
            if fpsElapsed>=0.5 then
                local fps=math.floor((fpsFrames/fpsElapsed)+0.5)
                fpsLabel.Text="FPS: "..tostring(fps)
                fpsFrames=0
                fpsElapsed=0
            end
        end)

        S.addButtonFeedback(nvBtn)
        S.addButtonFeedback(warpBtn)
        S.addButtonFeedback(sysBtn)
        S.addButtonFeedback(antiLagBtn)

        antiLagBtn.MouseButton1Click:Connect(function()
            antiLagEnabled=not antiLagEnabled
            antiLagBtn.Text=antiLagEnabled and "AntiLag: ON" or "AntiLag: OFF"
            antiLagBtn.BackgroundColor3=antiLagEnabled and Color3.fromRGB(46,139,87) or Color3.fromRGB(178,34,34)
            if antiLagEnabled then
                runAntiLag()
            else
                antiLagRestore()
            end
        end)

        -- AntiSys: نفس طريقة System Warning Cleaner من الكود المرجعي.
        -- يفحص PlayerGui بالكامل ويحذف أي عنصر اسمه System
        -- أو أي TextLabel/TextButton يحتوي نصه على System.
        local sysEnabled=false

        local function checkAndDeleteSystem(obj)
            if not S.systemCleanerProtectionActive then
                return
            end

            pcall(function()
                local name = string.lower(obj.Name)

                if name:find("system", 1, true) then
                    obj:Destroy()
                    return
                end

                if obj:IsA("TextLabel")
                    or obj:IsA("TextButton") then

                    local text = string.lower(obj.Text or "")

                    if text:find("system", 1, true) then
                        local parent = obj.Parent

                        if parent and parent ~= PlayerGui then
                            parent:Destroy()
                        else
                            obj:Destroy()
                        end
                    end
                end
            end)
        end

        local function scanSystemContainer(container)
            if not container then
                return
            end

            for _, obj in ipairs(container:GetDescendants()) do
                checkAndDeleteSystem(obj)
            end
        end

        local function checkSystemMessages()
            if not S.systemCleanerProtectionActive then
                return
            end

            scanSystemContainer(PlayerGui)
        end

        local function connectSystemCleaner()
            if S.protectionConnections.system then
                pcall(function()
                    S.protectionConnections.system:Disconnect()
                end)
                S.protectionConnections.system=nil
            end

            if S.protectionConnections.systemPlayerGui then
                pcall(function()
                    S.protectionConnections.systemPlayerGui:Disconnect()
                end)
                S.protectionConnections.systemPlayerGui=nil
            end

            local pg=PlayerGui

            if pg then
                S.protectionConnections.systemPlayerGui=pg.DescendantAdded:Connect(function(obj)
                    if S.systemCleanerProtectionActive then
                        task.defer(checkAndDeleteSystem,obj)
                    end
                end)
            end

            -- فحص فوري عند التشغيل.
            checkSystemMessages()
        end

        local function setSystem(state)
            sysEnabled=state
            S.systemCleanerProtectionActive=state

            if state then
                connectSystemCleaner()
            else
                if S.protectionConnections.system then
                    pcall(function()
                        S.protectionConnections.system:Disconnect()
                    end)
                    S.protectionConnections.system=nil
                end

                if S.protectionConnections.systemPlayerGui then
                    pcall(function()
                        S.protectionConnections.systemPlayerGui:Disconnect()
                    end)
                    S.protectionConnections.systemPlayerGui=nil
                end
            end
        end

        sysBtn.MouseButton1Click:Connect(function()
            setSystem(not sysEnabled)
            sysBtn.BackgroundColor3=sysEnabled and Color3.fromRGB(46,139,87) or Color3.fromRGB(178,34,34)
            sysBtn.Text=sysEnabled and "AntiSys: ON" or "AntiSys: OFF"
        end)

        S.addButtonFeedback(antiChatBtn)
        S.addButtonFeedback(antiPlayerBtn)

        antiChatBtn.Activated:Connect(function()
            setAntiChat(not S.antiChatActive)
            antiChatBtn.Text=S.antiChatActive and "AntiChat: ON" or "AntiChat: OFF"
            antiChatBtn.BackgroundColor3=S.antiChatActive and Color3.fromRGB(46,139,87) or Color3.fromRGB(178,34,34)
        end)

        antiPlayerBtn.Activated:Connect(function()
            setAntiPlayer(not S.antiPlayerActive)
            antiPlayerBtn.Text=S.antiPlayerActive and "AntiPlayer: ON" or "AntiPlayer: OFF"
            antiPlayerBtn.BackgroundColor3=S.antiPlayerActive and Color3.fromRGB(46,139,87) or Color3.fromRGB(178,34,34)
        end)

        nvBtn.Activated:Connect(function()
            S.nvActive=not S.nvActive
            if S.nvActive then
                nvBtn.BackgroundColor3=Color3.fromRGB(46,139,87)
                nvBtn.Text="AntiNv: ON"
                -- لا نعتمد على sendCommand هنا حتى لا يتوقف AntiNv عندما يكون
                -- RemoteFunction مشغولاً بسبب HD Spam. الحماية الأساسية تعمل محلياً
                -- عبر اعتراض ExecuteClientCommand وتعطيل مستمعاته.
                if S.executeCmd then setSignalState(true) end
                task.spawn(function() pcall(function() S.sendCommand("!unnv") end) end)
            else
                nvBtn.BackgroundColor3=Color3.fromRGB(178,34,34)
                nvBtn.Text="AntiNv: OFF"
                if S._protectionNvLoop then S.safeCancel(S._protectionNvLoop); S._protectionNvLoop=nil end
            end
        end)

        warpBtn.Activated:Connect(function()
            S.warpActive=not S.warpActive
            if S.warpActive then
                warpBtn.BackgroundColor3=Color3.fromRGB(46,139,87)
                warpBtn.Text="AntiWarp: ON"
                if S.executeCmd then setSignalState(true) end
                task.spawn(function() pcall(function() S.sendCommand("!unwarp") end) end)
            else
                warpBtn.BackgroundColor3=Color3.fromRGB(178,34,34)
                warpBtn.Text="AntiWarp: OFF"
                if S._protectionWarpLoop then S.safeCancel(S._protectionWarpLoop); S._protectionWarpLoop=nil end
            end
        end)
        if screenGui.Destroying then
            screenGui.Destroying:Connect(function() if sysConn then pcall(function() sysConn:Disconnect() end) end end)
        end
    end

    S.createProtectionControlCenter=createProtectionUI

    -- Keep Protection Center alive if another client UI cleaner/executor removes or
    -- disables its ScreenGui. Recreate only after the user has opened it, and preserve
    -- AntiNv/AntiWarp state. This does not create duplicate GUIs.
    S.protectionWatchLoop=task.spawn(function()
        while S.remoteCommandAlive do
            task.wait(0.5)
            if S.protectionRequested then
                local gui=S.protectionGui
                local frame=S.protectionFrame
                if not gui or not gui.Parent or not frame or not frame.Parent then
                    pcall(createProtectionUI)
                else
                    gui.Enabled=true
                    frame.Visible=true
                    frame.Active=true
                end
                if S.fpsLabel and S.fpsLabel.Parent then
                    S.fpsLabel.Visible=true
                end
            end
        end
    end)
end

-- SCOPE 21: ANTI SECTION - UI
-- ============================================================
do
    local S = _G.SQR
    local sf5 = S.sectionFrames[table.find(S.sections, "Anti")]
    
    S.VoidOriginalPos = nil
    S.KillOriginalPos = nil
    S.WifiOriginalPos = nil
    S.AntiBangOriginalPos = nil
    
    function S.clickButton(button)
        if typeof(firesignal) == "function" then
            local success = pcall(function() firesignal(button.MouseButton1Click) end)
            if success then return true end
        end
        if typeof(getconnections) == "function" then
            local success, connections = pcall(function() return getconnections(button.MouseButton1Click) end)
            if success and connections then
                for _, connection in ipairs(connections) do
                    if connection.Function then
                        pcall(function() connection.Function() end)
                        return true
                    end
                end
            end
        end
        return false
    end
    
    function S.sendSkinCommand(name)
        pcall(function()
            local ScreenGui = S.player.PlayerGui:FindFirstChild("ScreenGui")
            if not ScreenGui then return end
            local HiddenCommands = ScreenGui:FindFirstChild("HiddenCommands")
            local InputFrame = ScreenGui:FindFirstChild("Input")
            if HiddenCommands and InputFrame then
                local ScrollingFrame = HiddenCommands:FindFirstChild("ScrollingFrame")
                local Input = InputFrame:FindFirstChild("Input")
                local Confirm = InputFrame:FindFirstChild("ChangeName")
                if ScrollingFrame and Input and Confirm then
                    local ChangeSkin = ScrollingFrame:FindFirstChild("ChangeSkin")
                    if ChangeSkin then
                        S.clickButton(ChangeSkin)
                        wait()
                        Input.Text = name
                        wait()
                        S.clickButton(Confirm)
                        wait()
                        pcall(function()
                            HiddenCommands.Visible = false
                            InputFrame.Visible = false
                        end)
                    end
                end
            end
        end)
    end
    
    S.antiButtonsData = {
        {"Wifi", 0.02, 0.0},
        {"Void", 0.52, 0.0},
        {"Kill", 0.52, 0.15},
        {"TpVoid", 0.02, 0.15},
        {"antiafk", 0.02, 0.30},
        {"antivoid", 0.52, 0.30},
        {"antiBang", 0.02, 0.45},
        {"anticopy", 0.52, 0.45},
        {"AntiEmote", 0.02, 0.60},
        {"AntiHuman", 0.52, 0.60},
        {"AntiNv", 0.28, 0.75}
    }

    -- Anti is a scrolling section: keep the controls explicitly renderable.
    if sf5 and sf5:IsA("ScrollingFrame") then
        sf5.Visible=true
        sf5.Active=true
        sf5.ScrollingEnabled=true
        sf5.AutomaticCanvasSize=Enum.AutomaticSize.Y
        sf5.ScrollBarThickness=4
        sf5.ZIndex=20
    end
end

-- ============================================================
-- SCOPE 22: ANTI BUTTON CREATION
-- ============================================================
do
    local S = _G.SQR
    
    function S.createAntiButton(name, xPos, yPos)
        local sf5 = S.sectionFrames[table.find(S.sections, "Anti")]
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(name == "AntiNv" and 0.9 or 0.45, 0, 0.09, 0)
        btn.Position = UDim2.new(name == "AntiNv" and 0.05 or xPos, 0, name == "AntiNv" and 0.75 or yPos, 0)
        btn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        btn.BorderSizePixel = 0
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.Text = name == "AntiNv" and "Anti-NV" or name
        btn.TextScaled = true
        btn.Font = Enum.Font.SourceSansBold
        btn.Visible = true
        btn.Active = true
        btn.AutoButtonColor = true
        btn.ZIndex = 60
        btn.Parent = sf5
        btn:SetAttribute("SQR_AntiName", name)
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
        
        local isActive = false
        local loopTask = nil
        local antiEmoteCleanup = nil
        
        local function handleAntiCopy()
            -- AntiCopy uses the same direct RemoteEvent method as the working
            -- Auto Skin Switcher. It does not touch the original Skin/Name UI,
            -- so it cannot conflict with AntiNV / AntiWarp GUI handlers.
            local RemoteEvent = S.rs:FindFirstChild("RemoteEvent")
            if not RemoteEvent or not RemoteEvent:IsA("RemoteEvent") then
                return
            end

            local skinNames = {"nanmewelldoit", "nan_fm7"}
            local skinIndex = 1

            loopTask = task.spawn(function()
                while isActive do
                    local skinName = skinNames[skinIndex]
                    if skinName then
                        pcall(function()
                            RemoteEvent:FireServer("ChangeSkinByName", skinName)
                        end)
                    end

                    skinIndex = (skinIndex % #skinNames) + 1
                    task.wait(0.1)
                end
            end)
        end

        local function handleAntiAFK()
            S.EnableAntiAFK()
        end
        
        local function handleTpVoid()
            local voidPos = CFrame.new(2147483648, 944401, -2)
            S.TpVoidOriginalPos = nil
            loopTask = spawn(function()
                while isActive do
                    pcall(function()
                        local char = S.player.Character
                        local root = char and char:FindFirstChild("HumanoidRootPart")
                        if root then
                            if not S.TpVoidOriginalPos then
                                S.TpVoidOriginalPos = root.CFrame
                            end
                            root.CFrame = voidPos
                            wait(0.08)
                            if isActive and root.Parent and S.TpVoidOriginalPos then
                                root.CFrame = S.TpVoidOriginalPos
                            end
                        end
                    end)
                    wait(1)
                end
            end)
        end

        local function handleAntiVoid()
            local safePos = CFrame.new(1801, 21, 624)
            loopTask = spawn(function()
                while isActive do
                    if S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart") then
                        local root = S.player.Character.HumanoidRootPart
                        if root.Position.Y < -50 or root.Position.Y > 1000 or root.Position.Magnitude > 5000 then
                            root.CFrame = safePos
                        end
                    end
                    wait(0.1)
                end
            end)
        end
        
        local function handleAntiEmote()
            local RequestCommandModification
            local ActivateClientCommand
            local hdSignals

            pcall(function()
                local hdClient = S.rs:WaitForChild("HDAdminHDClient", 3)
                hdSignals = hdClient and hdClient:WaitForChild("Signals", 3)

                if hdSignals then
                    RequestCommandModification =
                        hdSignals:FindFirstChild("RequestCommandModification")

                    ActivateClientCommand =
                        hdSignals:FindFirstChild("ActivateClientCommand")
                end
            end)

            if not RequestCommandModification then
                return
            end

            local PlayerGui = S.player:WaitForChild("PlayerGui")
            local watcherConnections = {}

            antiEmoteCleanup = function()
                for _, connection in ipairs(watcherConnections) do
                    pcall(function()
                        connection:Disconnect()
                    end)
                end
                table.clear(watcherConnections)
            end

            local function cancelDanceCommand()
                if not isActive then return end
                task.spawn(function()
                    pcall(function()
                        RequestCommandModification:InvokeServer("!Undance")
                    end)
                end)
            end

            -- الطريقة المباشرة: مراقبة إشارة HD Admin نفسها.
            if ActivateClientCommand and ActivateClientCommand:IsA("RemoteEvent") then
                table.insert(
                    watcherConnections,
                    ActivateClientCommand.OnClientEvent:Connect(function(data)
                        if not isActive then return end

                        local isAura = false

                        if type(data) == "table" then
                            for _, value in pairs(data) do
                                local v = string.lower(tostring(value))
                                if v == "aura"
                                    or v == "dance"
                                    or v == "emote" then
                                    isAura = true
                                    break
                                end
                            end
                        else
                            local v = string.lower(tostring(data))
                            if v == "aura"
                                or v == "dance"
                                or v == "emote" then
                                isAura = true
                            end
                        end

                        if isAura then
                            cancelDanceCommand()
                        end
                    end)
                )
            end

            -- مراقبة واجهة HD كاحتياط إذا وصلت الإشارة بصيغة مختلفة.
            task.spawn(function()
                local clientFolder = PlayerGui:WaitForChild("Client", 5)
                if not clientFolder or not isActive then return end

                table.insert(
                    watcherConnections,
                    clientFolder.DescendantAdded:Connect(function(descendant)
                        if not isActive then return end

                        if descendant:IsA("TextLabel")
                            or descendant:IsA("TextButton") then

                            local value = string.lower(tostring(descendant.Text or ""))

                            if string.find(value, "dance", 1, true)
                                or string.find(value, "emote", 1, true)
                                or string.find(value, "aura", 1, true)
                                or string.find(value, "undance", 1, true) then
                                cancelDanceCommand()
                            end
                        end
                    end)
                )
            end)
        end
        
        local function handleAntiHuman()
            -- Original-style AntiHuman cycle, independent of Auto Cuff.
            -- Auto Cuff has its own monitor and never disables this cycle.
            loopTask = task.spawn(function()
                while isActive do
                    local character = S.player.Character
                    if character and character.Parent then
                        local humanoid = character:FindFirstChildOfClass("Humanoid")
                        local root = character:FindFirstChild("HumanoidRootPart")

                        if humanoid and root and root.Parent then
                            local currentCFrame = root.CFrame
                            local camera = workspace.CurrentCamera
                            local savedCamCFrame = camera and camera.CFrame
                            local savedCameraType = camera and camera.CameraType
                            local savedCameraSubject = camera and camera.CameraSubject

                            pcall(function()
                                if camera and savedCamCFrame then
                                    camera.CameraType = Enum.CameraType.Scriptable
                                    camera.CFrame = savedCamCFrame
                                end

                                root.CFrame = CFrame.new(999999, 999999, 999999)
                                task.wait(0.05)

                                if root.Parent then
                                    root.CFrame = currentCFrame
                                end

                                if camera and savedCamCFrame then
                                    camera.CFrame = savedCamCFrame
                                    camera.CameraType = savedCameraType or Enum.CameraType.Custom
                                    if savedCameraSubject and savedCameraSubject.Parent then
                                        camera.CameraSubject = savedCameraSubject
                                    elseif humanoid.Parent then
                                        camera.CameraSubject = humanoid
                                    end
                                end
                            end)
                        end
                    end

                    task.wait(0.6)
                end
            end)
        end

        local function handleAntiBang()
            local function getRoot(plr)
                return plr and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            end
            local myRoot = getRoot(S.player)
            if myRoot then S.AntiBangOriginalPos = myRoot.CFrame end
            local function looksLikeBang(plr)
                local char = plr and plr.Character
                if not char then return false end
                local humanoid = char:FindFirstChildOfClass("Humanoid")
                local animator = humanoid and humanoid:FindFirstChildOfClass("Animator")
                if animator then
                    for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                        local anim = track.Animation
                        local id = anim and tostring(anim.AnimationId) or ""
                        if id:find("5918726674", 1, true) then return true end
                    end
                end
                return false
            end
            loopTask = spawn(function()
                while isActive do
                    pcall(function()
                        local root = getRoot(S.player)
                        if not root then return end
                        local targets = {}
                        if S.selectedPlayer and S.selectedPlayer ~= S.player and S.selectedPlayer.Parent then
                            table.insert(targets, S.selectedPlayer)
                        else
                            for _, plr in ipairs(S.Players:GetPlayers()) do
                                if plr ~= S.player then table.insert(targets, plr) end
                            end
                        end
                        for _, plr in ipairs(targets) do
                            local targetRoot = getRoot(plr)
                            if targetRoot then
                                local distance = (root.Position - targetRoot.Position).Magnitude
                                if distance < 6 and looksLikeBang(plr) then
                                    if not S.AntiBangOriginalPos then S.AntiBangOriginalPos = root.CFrame end
                                    for _, child in ipairs(root:GetChildren()) do
                                        if child:IsA("Attachment") or child:IsA("AlignPosition") or child:IsA("AlignOrientation") then
                                            pcall(function() child:Destroy() end)
                                        end
                                    end
                                    local safeCFrame = S.AntiBangOriginalPos + Vector3.new(0, 25, 0)
                                    root.CFrame = safeCFrame
                                    root.AssemblyLinearVelocity = Vector3.zero
                                    wait(0.08)
                                    if isActive and root.Parent then
                                        root.CFrame = S.AntiBangOriginalPos
                                        root.AssemblyLinearVelocity = Vector3.zero
                                    end
                                    break
                                end
                            end
                        end
                    end)
                    wait(0.03)
                end
            end)
        end
        
        local function handleWifi()
            local wifiPositions = {
                CFrame.new(2147483648, 944401, -2),
                CFrame.new(1134, -271, 408),
                CFrame.new(1801, 21, 624),
                CFrame.new(-5000, -5000, -5000),
                CFrame.new(5000, 5000, 5000),
                CFrame.new(0, 10000, 0),
                CFrame.new(10000, 0, 10000),
                CFrame.new(-10000, 0, -10000)
            }
            local wifiIndex = 1
            loopTask = spawn(function()
                while isActive do
                    pcall(function()
                        if S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart") then
                            local root = S.player.Character.HumanoidRootPart
                            if not S.WifiOriginalPos then S.WifiOriginalPos = root.CFrame end
                            root.CFrame = wifiPositions[wifiIndex]
                            wifiIndex = wifiIndex + 1
                            if wifiIndex > #wifiPositions then wifiIndex = 1 end
                            local near = S.getNearPlayers()
                            for _, plr in ipairs(near) do
                                S.sendCommand("!re " .. plr.Name)
                                wait(0.05)
                            end
                        end
                    end)
                    wait(0.1)
                end
            end)
        end
        
        local function handleVoid()
            local voidPos = CFrame.new(2147483648, 944401, -2)
            loopTask = spawn(function()
                while isActive do
                    if S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart") then
                        local root = S.player.Character.HumanoidRootPart
                        if not S.VoidOriginalPos then S.VoidOriginalPos = root.CFrame end
                        root.CFrame = voidPos
                    end
                    wait(0.1)
                end
            end)
        end
        
        local function handleKill()
            local killPos = CFrame.new(1134, -271, 408)
            loopTask = spawn(function()
                while isActive do
                    if S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart") then
                        local root = S.player.Character.HumanoidRootPart
                        if not S.KillOriginalPos then S.KillOriginalPos = root.CFrame end
                        root.CFrame = killPos
                    end
                    wait(0.1)
                end
            end)
        end
        
        local function handleAntiNv()
            -- Keep the same protection commands, but use one worker instead of
            -- creating two new threads every 0.001s (which caused severe lag/crashes).
            loopTask = task.spawn(function()
                while isActive do
                    pcall(function() S.sendCommand("!unnv") end)
                    pcall(function() S.sendCommand("!unwarp") end)
                    task.wait(0.05)
                end
            end)
        end
        
        btn.MouseButton1Click:Connect(function()
            isActive = not isActive
            if isActive then
                btn.Text = "Stop"
                btn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
                if name == "anticopy" then handleAntiCopy()
                elseif name == "TpVoid" then handleTpVoid()
                elseif name == "antiafk" then handleAntiAFK()
                elseif name == "antivoid" then handleAntiVoid()
                elseif name == "AntiEmote" then handleAntiEmote()
                elseif name == "antiBang" then handleAntiBang()
                elseif name == "Wifi" then handleWifi()
                elseif name == "Void" then handleVoid()
                elseif name == "Kill" then handleKill()
                elseif name == "AntiHuman" then handleAntiHuman()
                elseif name == "AntiNv" then handleAntiNv() end
            else
                btn.Text = name == "AntiNv" and "Anti-NV" or name
                btn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
                if loopTask then S.safeCancel(loopTask) end
                if name == "anticopy" then
                    pcall(function()
                        local pg=S.player:FindFirstChildOfClass("PlayerGui")
                        if pg then
                            local g=pg:FindFirstChild("ScreenGui")
                            if g and g:IsA("ScreenGui") then g.Enabled=true end
                            for _,x in ipairs(pg:GetChildren()) do
                                local n=string.lower(x.Name)
                                if (n:find("cmd",1,true) or n:find("command",1,true) or n:find("admin",1,true)) and x:IsA("ScreenGui") then x.Enabled=true end
                            end
                        end
                    end)
                end
                if name == "AntiEmote" and antiEmoteCleanup then
                    antiEmoteCleanup()
                    antiEmoteCleanup = nil
                end
                if name == "antiafk" then S.DisableAntiAFK() end
                if name == "TpVoid" and S.TpVoidOriginalPos and S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart") then
                    S.player.Character.HumanoidRootPart.CFrame = S.TpVoidOriginalPos
                    S.TpVoidOriginalPos = nil
                end
                if name == "Void" and S.VoidOriginalPos and S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart") then
                    S.player.Character.HumanoidRootPart.CFrame = S.VoidOriginalPos
                    S.VoidOriginalPos = nil
                end
                if name == "Kill" and S.KillOriginalPos and S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart") then
                    S.player.Character.HumanoidRootPart.CFrame = S.KillOriginalPos
                    S.KillOriginalPos = nil
                end
                if name == "Wifi" and S.WifiOriginalPos and S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart") then
                    S.player.Character.HumanoidRootPart.CFrame = S.WifiOriginalPos
                    S.WifiOriginalPos = nil
                end
                if name == "antiBang" and S.AntiBangOriginalPos and S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart") then
                    S.player.Character.HumanoidRootPart.CFrame = S.AntiBangOriginalPos
                    S.player.Character.HumanoidRootPart.AssemblyLinearVelocity = Vector3.zero
                    S.AntiBangOriginalPos = nil
                end
            end
        end)
        
    end
    
    for _, data in ipairs(S.antiButtonsData) do
        S.createAntiButton(data[1], data[2], data[3])
    end

    -- Explicit NV opener: the requested NV button is always present in Anti.
    if sf5 and not sf5:FindFirstChild("SQR_NV_Open") then
        local nvOpen=Instance.new("TextButton")
        nvOpen.Name="SQR_NV_Open"
        nvOpen.Size=UDim2.new(0.9,0,0,34)
        nvOpen.Position=UDim2.new(0.05,0,0.88,0)
        nvOpen.BackgroundColor3=Color3.fromRGB(0,200,0)
        nvOpen.BorderSizePixel=0
        nvOpen.Text="NV"
        nvOpen.TextColor3=Color3.fromRGB(255,255,255)
        nvOpen.TextScaled=true
        nvOpen.Font=Enum.Font.SourceSansBold
        nvOpen.Active=true
        nvOpen.Visible=true
        nvOpen.ZIndex=100
        nvOpen.Parent=sf5
        Instance.new("UICorner",nvOpen).CornerRadius=UDim.new(0,8)
        S.addButtonFeedback(nvOpen)
        nvOpen.Activated:Connect(function()
            S.protectionRequested=true
            if S.createProtectionControlCenter then S.createProtectionControlCenter() end
        end)
        S.antiNVOpenButton=nvOpen
        nvOpen:SetAttribute("SQRKeepHidden", false)
    end
end

-- ============================================================
-- SCOPE 23: NEAR SECTION
-- ============================================================
do
    local S = _G.SQR
    local sf6 = S.sectionFrames[table.find(S.sections, "Near")]
    
    S.nearCmdBox = Instance.new("TextBox")
    S.nearCmdBox.Size = UDim2.new(1, -20, 0.15, 0)
    S.nearCmdBox.Position = UDim2.new(0, 10, 0.02, 0)
    S.nearCmdBox.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    S.nearCmdBox.BorderSizePixel = 0
    S.nearCmdBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.nearCmdBox.PlaceholderText = "Command (use 'user' for near players)..."
    S.nearCmdBox.Text = ""
    S.nearCmdBox.ClearTextOnFocus = false
    S.nearCmdBox.Font = Enum.Font.SourceSans
    S.nearCmdBox.TextSize = 16
    S.nearCmdBox.Parent = sf6
    Instance.new("UICorner", S.nearCmdBox).CornerRadius = UDim.new(0, 10)
    
    S.nearSpeedBox = Instance.new("TextBox")
    S.nearSpeedBox.Size = UDim2.new(0.35, 0, 0.12, 0)
    S.nearSpeedBox.Position = UDim2.new(0.05, 0, 0.2, 0)
    S.nearSpeedBox.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    S.nearSpeedBox.BorderSizePixel = 0
    S.nearSpeedBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.nearSpeedBox.PlaceholderText = "Speed (s)"
    S.nearSpeedBox.Text = "0.5"
    S.nearSpeedBox.ClearTextOnFocus = false
    S.nearSpeedBox.Font = Enum.Font.SourceSans
    S.nearSpeedBox.TextSize = 16
    S.nearSpeedBox.Parent = sf6
    Instance.new("UICorner", S.nearSpeedBox).CornerRadius = UDim.new(0, 10)
    
    S.nearSendButton = Instance.new("TextButton")
    S.nearSendButton.Size = UDim2.new(0.25, 0, 0.12, 0)
    S.nearSendButton.Position = UDim2.new(0.45, 0, 0.2, 0)
    S.nearSendButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    S.nearSendButton.BorderSizePixel = 0
    S.nearSendButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.nearSendButton.Text = "Send"
    S.nearSendButton.TextScaled = true
    S.nearSendButton.Font = Enum.Font.SourceSansBold
    S.nearSendButton.Parent = sf6
    Instance.new("UICorner", S.nearSendButton).CornerRadius = UDim.new(0, 10)
    
    S.nearSpamButton = Instance.new("TextButton")
    S.nearSpamButton.Size = UDim2.new(0.25, 0, 0.12, 0)
    S.nearSpamButton.Position = UDim2.new(0.72, 0, 0.2, 0)
    S.nearSpamButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    S.nearSpamButton.BorderSizePixel = 0
    S.nearSpamButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.nearSpamButton.Text = "Spam"
    S.nearSpamButton.TextScaled = true
    S.nearSpamButton.Font = Enum.Font.SourceSansBold
    S.nearSpamButton.Parent = sf6
    Instance.new("UICorner", S.nearSpamButton).CornerRadius = UDim.new(0, 10)
    
    S.nearAuraButton = Instance.new("TextButton")
    S.nearAuraButton.Size = UDim2.new(0.25, 0, 0.12, 0)
    S.nearAuraButton.Position = UDim2.new(0.02, 0, 0.38, 0)
    S.nearAuraButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    S.nearAuraButton.BorderSizePixel = 0
    S.nearAuraButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.nearAuraButton.Text = "Aura"
    S.nearAuraButton.TextScaled = true
    S.nearAuraButton.Font = Enum.Font.SourceSansBold
    S.nearAuraButton.Parent = sf6
    Instance.new("UICorner", S.nearAuraButton).CornerRadius = UDim.new(0, 10)
    
    S.nearReButton = Instance.new("TextButton")
    S.nearReButton.Size = UDim2.new(0.25, 0, 0.12, 0)
    S.nearReButton.Position = UDim2.new(0.3, 0, 0.38, 0)
    S.nearReButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    S.nearReButton.BorderSizePixel = 0
    S.nearReButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.nearReButton.Text = "Re"
    S.nearReButton.TextScaled = true
    S.nearReButton.Font = Enum.Font.SourceSansBold
    S.nearReButton.Parent = sf6
    Instance.new("UICorner", S.nearReButton).CornerRadius = UDim.new(0, 10)
    
    S.nearStatus = Instance.new("TextLabel")
    S.nearStatus.Size = UDim2.new(1, 0, 0.08, 0)
    S.nearStatus.Position = UDim2.new(0, 0, 0.55, 0)
    S.nearStatus.BackgroundTransparency = 1
    S.nearStatus.BorderSizePixel = 0
    S.nearStatus.Text = "Stopped"
    S.nearStatus.TextColor3 = Color3.fromRGB(255, 255, 0)
    S.nearStatus.TextScaled = true
    S.nearStatus.Font = Enum.Font.SourceSans
    S.nearStatus.Parent = sf6
    
    S.nearSpamActive = false
    S.nearAuraActive = false
    S.nearReActive = false
    S.nearSpamLoop = nil
    S.nearAuraLoop = nil
    S.nearReLoop = nil
    
    function S.sendToNearPlayers(cmd)
        local near = S.getNearPlayers()
        if #near == 0 then
            return
        end
        for _, plr in ipairs(near) do
            local finalCmd = cmd:gsub("user", plr.Name)
            S.sendCommand(finalCmd)
            wait(0.05)
        end
    end
    
    S.nearSendButton.MouseButton1Click:Connect(function()
        local cmd = S.nearCmdBox.Text
        if cmd == "" then return end
        S.sendToNearPlayers(cmd)
        S.nearStatus.Text = "Sent"
        S.nearStatus.TextColor3 = Color3.fromRGB(0, 255, 0)
        wait(0.5)
        S.nearStatus.Text = "Stopped"
        S.nearStatus.TextColor3 = Color3.fromRGB(255, 255, 0)
    end)
    
    S.nearSpamButton.MouseButton1Click:Connect(function()
        S.nearSpamActive = not S.nearSpamActive
        if S.nearSpamActive then
            local cmd = S.nearCmdBox.Text
            if cmd == "" then S.nearSpamActive = false return end
            local speed = tonumber(S.nearSpeedBox.Text) or 0.5
            if speed < 0.1 then speed = 0.1 end
            if speed > 3 then speed = 3 end
            S.nearSpamButton.Text = "Stop"
            S.nearSpamButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
            S.nearStatus.Text = "Running..."
            S.nearStatus.TextColor3 = Color3.fromRGB(0, 255, 0)
            S.nearSpamLoop = spawn(function()
                while S.nearSpamActive do
                    S.sendToNearPlayers(cmd)
                    wait(speed)
                end
            end)
        else
            S.nearSpamButton.Text = "Spam"
            S.nearSpamButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            S.nearStatus.Text = "Stopped"
            S.nearStatus.TextColor3 = Color3.fromRGB(255, 255, 0)
            if S.nearSpamLoop then S.safeCancel(S.nearSpamLoop) end
            S.nearSpamLoop = nil
        end
    end)
    
    S.nearAuraButton.MouseButton1Click:Connect(function()
        S.nearAuraActive = not S.nearAuraActive
        if S.nearAuraActive then
            S.nearAuraButton.Text = "Stop"
            S.nearAuraButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
            S.nearStatus.Text = "Running..."
            S.nearStatus.TextColor3 = Color3.fromRGB(0, 255, 0)
            S.nearAuraLoop = spawn(function()
                while S.nearAuraActive do
                    S.sendToNearPlayers("!aura user")
                    wait(0.1)
                end
            end)
        else
            S.nearAuraButton.Text = "Aura"
            S.nearAuraButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            S.nearStatus.Text = "Stopped"
            S.nearStatus.TextColor3 = Color3.fromRGB(255, 255, 0)
            if S.nearAuraLoop then S.safeCancel(S.nearAuraLoop) end
            S.nearAuraLoop = nil
        end
    end)
    
    S.nearReButton.MouseButton1Click:Connect(function()
        S.nearReActive = not S.nearReActive
        if S.nearReActive then
            S.nearReButton.Text = "Stop"
            S.nearReButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
            S.nearStatus.Text = "Running..."
            S.nearStatus.TextColor3 = Color3.fromRGB(0, 255, 0)
            S.nearReLoop = spawn(function()
                while S.nearReActive do
                    S.sendToNearPlayers("!re user")
                    wait(0.01)
                end
            end)
        else
            S.nearReButton.Text = "Re"
            S.nearReButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            S.nearStatus.Text = "Stopped"
            S.nearStatus.TextColor3 = Color3.fromRGB(255, 255, 0)
            if S.nearReLoop then S.safeCancel(S.nearReLoop) end
            S.nearReLoop = nil
        end
    end)
end

-- ============================================================
-- SCOPE 24: SQR SECTION
-- ============================================================
do
    local S = _G.SQR
    local sf8 = S.sectionFrames[table.find(S.sections, "SQR")]
    
    S.infoText = Instance.new("TextLabel")
    S.infoText.Size = UDim2.new(1, -20, 0.85, 0)
    S.infoText.Position = UDim2.new(0, 10, 0.01, 0)
    S.infoText.BackgroundTransparency = 1
    S.infoText.BorderSizePixel = 0
    S.infoText.Text = [[Hello, I am Mr. The maker of this script completely, and all rights reserved for the group.

Number of Script Developers :

Mahdi Iraqi

Mr. Iraqi

Group S Q R


The script has many, many features that you can try and benefit from in your gameplay in many things from maps.. I hope you enjoy trying the script.

Dis SQR :
Rob Mr : nanMeWellDoit
Rob Mhdi : nanNeverLet

You can join, unlock loot, pay robux for ranks and activations, and many other features not available in the free script.. What are you waiting for? Unlock loot!]]
    S.infoText.TextColor3 = Color3.fromRGB(200, 200, 255)
    S.infoText.TextTransparency = 0
    S.infoText.Visible = true
    S.infoText.ZIndex = 30
    S.infoText.TextScaled = true
    S.infoText.Font = Enum.Font.SourceSans
    S.infoText.TextWrapped = true
    S.infoText.TextXAlignment = Enum.TextXAlignment.Center
    S.infoText.Parent = sf8
    
    S.discordBtn = Instance.new("TextButton")
    S.discordBtn.Size = UDim2.new(0.3, 0, 0.06, 0)
    S.discordBtn.Position = UDim2.new(0.35, 0, 0.92, 0)
    S.discordBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    S.discordBtn.BorderSizePixel = 0
    S.discordBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.discordBtn.Text = "Join Discord"
    S.discordBtn.TextScaled = true
    S.discordBtn.Font = Enum.Font.SourceSansBold
    S.discordBtn.Parent = sf8
    S.discordBtn.Visible = true
    S.discordBtn.Active = true
    S.discordBtn.ZIndex = 31
    Instance.new("UICorner", S.discordBtn).CornerRadius = UDim.new(0, 10)
    
    S.discordBtn.MouseButton1Click:Connect(function()
        setclipboard("https://discord.gg/KQnY6gRVF")
    end)
end

-- ============================================================
-- SCOPE 25: SETTING SECTION (معدل بالكامل)
-- ============================================================
do
    local S = _G.SQR
    local sfSetting = S.sectionFrames[table.find(S.sections, "Setting")]
    
    if sfSetting then
        sfSetting.ClipsDescendants = true
        sfSetting.ScrollBarThickness = 5
        sfSetting.CanvasSize = UDim2.new(0, 0, 0, 1250)
        
        S.settingsData = {
            {"ايتاشي", "108459072532413"}, {"ال", "76264922206194"},
            {"مايكل و سوكريه", "99042741738776"}, {"بنت انمي", "138417035920584"},
            {"ماكيما", "139118334057714"}, {"باتريك جين", "79497123255834"},
            {"كرت فضي", "131997057882632"}, {"ساسكي", "101118902102194"},
            {"مادارا", "131491969992301"}, {"تايلور", "93724754807413"},
            {"ال باتشينو", "128220812349566"}, {"جيسي", "82862976463067"},
            {"FBI", "84860336404228"}, {"اليوت", "79357712453854"},
            {"ميسي", "113200243925050"}, {"بسيط", "81751567772780"},
            {"ساموراي", "91708553471066"}, {"يوهان البرت", "87085350473257"},
            {"سول", "85257482355164"}, {"ماهون", "73520201667284"},
            {"تي باك", "106212842895961"}, {"والتر وايت", "137208889690869"},
            {"يورو", "113679705314612"}, {"يوكي", "82368191067474"},
            {"هيمينو", "97749880790314"},
        }
S.settingState = {
            version = 4,
            background = nil,
            backgroundId = nil,
            backgroundName = nil,
            transparency = 0,
            uiScale = 1,
            walkSpeed = 16,
            flySpeed = 50,
            frozen = false,
            flying = false,
            cameraFrozen = false,
            hidden = false,
            autoRespawn = false,
        }
        
        S.HttpService = game:GetService("HttpService")
        S.SETTINGS_FILE = "SQRrrr.json"
        S.SETTINGS_GLOBAL_KEY = "SQR_PermanentSettings"
        
        -- نفس طريقة الحفظ التي نجحت في اختبار الحفظ المنفصل.
        function S.saveSettings()
            local okEncode, encoded = pcall(function()
                return S.HttpService:JSONEncode(S.settingState)
            end)
            if not okEncode or not encoded then return false end

            if type(writefile) == "function" then
                local ok = pcall(function()
                    writefile(S.SETTINGS_FILE, encoded)
                end)
                if ok then return true end
            end

            if type(savefile) == "function" then
                local ok = pcall(function()
                    savefile(S.SETTINGS_FILE, encoded)
                end)
                if ok then return true end
            end

            return false
        end

        function S.loadSettings()
            if type(readfile) ~= "function" or type(isfile) ~= "function" then return end

            local exists = false
            pcall(function()
                exists = isfile(S.SETTINGS_FILE)
            end)
            if not exists then return end

            local okRead, content = pcall(function()
                return readfile(S.SETTINGS_FILE)
            end)
            if not okRead or not content or content == "" then return end

            local okDecode, data = pcall(function()
                return S.HttpService:JSONDecode(content)
            end)
            if not okDecode or type(data) ~= "table" then return end

            for k,v in pairs(data) do
                if S.settingState[k] ~= nil then
                    S.settingState[k] = v
                end
            end
        end
        
        S.loadSettings()
        
        S.selectedBgImage = Instance.new("ImageLabel")
        S.selectedBgImage.Name = "SettingsBackground"
        S.selectedBgImage.Size = UDim2.new(1,0,1,0)
        S.selectedBgImage.Position = UDim2.fromOffset(0,0)
        S.selectedBgImage.BackgroundTransparency = 1
        S.selectedBgImage.BorderSizePixel = 0
        S.selectedBgImage.ScaleType = Enum.ScaleType.Crop
        S.selectedBgImage.ImageTransparency = 0
        S.selectedBgImage.ZIndex = 1
        S.selectedBgImage.Active = false
        S.selectedBgImage.Parent = S.mainFrame
        
        S.settingTitle = Instance.new("TextLabel")
        S.settingTitle.Size = UDim2.new(1,-20,0,32)
        S.settingTitle.Position = UDim2.fromOffset(10,0)
        S.settingTitle.BackgroundTransparency = 1
        S.settingTitle.Text = "SETTINGS"
        S.settingTitle.TextColor3 = Color3.fromRGB(255,255,255)
        S.settingTitle.TextSize = 20
        S.settingTitle.Font = Enum.Font.GothamBold
        S.settingTitle.TextXAlignment = Enum.TextXAlignment.Center
        S.settingTitle.ZIndex = 5
        S.settingTitle.Parent = sfSetting
        
        S.avatar = Instance.new("ImageLabel")
        S.avatar.Size = UDim2.fromOffset(60,60)
        S.avatar.Position = UDim2.fromOffset(10,38)
        S.avatar.BackgroundTransparency = 1
        S.avatar.BorderSizePixel = 0
        S.avatar.ScaleType = Enum.ScaleType.Fit
        S.avatar.ZIndex = 5
        S.avatar.Parent = sfSetting
        pcall(function()
            S.avatar.Image = S.Players:GetUserThumbnailAsync(S.player.UserId, Enum.ThumbnailType.AvatarBust, Enum.ThumbnailSize.Size150x150)
        end)
        
        S.playerInfo = Instance.new("TextLabel")
        S.playerInfo.Size = UDim2.new(1,-85,0,60)
        S.playerInfo.Position = UDim2.fromOffset(80,38)
        S.playerInfo.BackgroundTransparency = 1
        S.playerInfo.Text = S.player.DisplayName .. "\n@" .. S.player.Name .. "\nPlayers: " .. #S.Players:GetPlayers()
        S.playerInfo.TextColor3 = Color3.fromRGB(225,235,255)
        S.playerInfo.TextSize = 13
        S.playerInfo.Font = Enum.Font.GothamMedium
        S.playerInfo.TextXAlignment = Enum.TextXAlignment.Left
        S.playerInfo.TextYAlignment = Enum.TextYAlignment.Center
        S.playerInfo.ZIndex = 5
        S.playerInfo.Parent = sfSetting
        
        function S.refreshPlayerInfo()
            S.playerInfo.Text = S.player.DisplayName .. "\n@" .. S.player.Name .. "\nPlayers: " .. #S.Players:GetPlayers()
        end
        S.Players.PlayerAdded:Connect(S.refreshPlayerInfo)
        S.Players.PlayerRemoving:Connect(S.refreshPlayerInfo)
        
        function S.makeBox(text,x,y,w)
            local box = Instance.new("TextBox")
            box.Size = UDim2.fromOffset(w,35)
            box.Position = UDim2.fromOffset(x,y)
            box.BackgroundColor3 = Color3.fromRGB(35,40,55)
            box.BackgroundTransparency = 0.05
            box.BorderSizePixel = 0
            box.TextColor3 = Color3.fromRGB(255,255,255)
            box.PlaceholderText = text
            box.Text = ""
            box.ClearTextOnFocus = false
            box.Font = Enum.Font.Gotham
            box.TextSize = 13
            box.ZIndex = 5
            box.Visible = true
            box.Active = true
            box.Parent = sfSetting
            Instance.new("UICorner",box).CornerRadius = UDim.new(0,9)
            return box
        end
        
        function S.makeButton(text,x,y,w)
            local b = Instance.new("TextButton")
            b.Size = UDim2.fromOffset(w,35)
            b.Position = UDim2.fromOffset(x,y)
            b.BackgroundColor3 = Color3.fromRGB(35,40,55)
            b.BackgroundTransparency = 0.05
            b.BorderSizePixel = 0
            b.Text = text
            b.TextColor3 = Color3.fromRGB(255,255,255)
            b.TextSize = 13
            b.Font = Enum.Font.GothamBold
            b.ZIndex = 5
            b.Visible = true
            b.Active = true
            b.Parent = sfSetting
            Instance.new("UICorner",b).CornerRadius = UDim.new(0,9)
            S.addButtonFeedback(b)
            return b
        end
        
        S.speedBox = S.makeBox("Player Speed 1-1000",10,110,180)
        S.speedBox.Text = tostring(S.settingState.walkSpeed)
        S.freezeBtn = S.makeButton("Freeze",200,110,130)
        
        S.flySpeedBox = S.makeBox("Fly Speed 1-1000",10,152,180)
        S.flySpeedBox.Text = tostring(S.settingState.flySpeed)
        
        S.flyBtn = S.makeButton("Fly",200,152,130)
        
        -- زر إعادة الدخول إلى السيرفر
        S.rejoinBtn = S.makeButton("Rejoin Server",10,196,140)
        
        -- زر الانتقال إلى سيرفر قليل العدد
        S.lowServerBtn = S.makeButton("Low Server",160,196,170)
        
        S.cameraBtn = S.makeButton("Freeze Camera",10,240,180)
        S.hideBtn = S.makeButton("Hide Character",200,240,130)
        S.respawnBtn = S.makeButton("Auto Respawn: OFF",10,282,320)

        -- AutoCliker: زر Setting للتحكم بالـ Mini Auto-Clicker.
        S.autoClickerActive = false
        S.autoClickerGui = nil
        S.autoClickerAlive = false

        local function createAutoClicker()
            if S.autoClickerGui then
                pcall(function() S.autoClickerGui:Destroy() end)
                S.autoClickerGui = nil
            end

            local CoreGui = S.CoreGui
            local UserInputService = S.UserInputService
            local VirtualInputManager = S.VIM
            local TweenService = S.TweenService

            local ScreenGui = Instance.new("ScreenGui")
            ScreenGui.Name = "MiniFuturisticAutoClicker"
            ScreenGui.Parent = CoreGui
            ScreenGui.ResetOnSpawn = false
            S.autoClickerGui = ScreenGui
            S.autoClickerAlive = true

            local CircleBtn = Instance.new("TextButton")
            CircleBtn.Name = "CircleBtn"
            CircleBtn.Parent = ScreenGui
            CircleBtn.Size = UDim2.new(0,40,0,40)
            CircleBtn.Position = UDim2.new(0.5,-20,0.4,-20)
            CircleBtn.BackgroundColor3 = Color3.fromRGB(220,20,60)
            CircleBtn.Text = ""
            CircleBtn.AutoButtonColor = false

            local UICorner = Instance.new("UICorner")
            UICorner.CornerRadius = UDim.new(1,0)
            UICorner.Parent = CircleBtn

            local UIStroke = Instance.new("UIStroke")
            UIStroke.Parent = CircleBtn
            UIStroke.Thickness = 2.5
            UIStroke.Color = Color3.fromRGB(255,80,80)
            UIStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

            local InnerRing = Instance.new("Frame")
            InnerRing.Parent = CircleBtn
            InnerRing.Size = UDim2.new(0.65,0,0.65,0)
            InnerRing.Position = UDim2.new(0.175,0,0.175,0)
            InnerRing.BackgroundTransparency = 1

            local InnerCorner = Instance.new("UICorner")
            InnerCorner.CornerRadius = UDim.new(1,0)
            InnerCorner.Parent = InnerRing

            local InnerStroke = Instance.new("UIStroke")
            InnerStroke.Parent = InnerRing
            InnerStroke.Thickness = 1
            InnerStroke.Color = Color3.fromRGB(255,255,255)
            InnerStroke.Transparency = 0.3

            local CenterIcon = Instance.new("TextLabel")
            CenterIcon.Parent = CircleBtn
            CenterIcon.Size = UDim2.new(1,0,1,0)
            CenterIcon.BackgroundTransparency = 1
            CenterIcon.Text = "⚡"
            CenterIcon.TextColor3 = Color3.fromRGB(255,255,255)
            CenterIcon.TextSize = 13
            CenterIcon.Font = Enum.Font.FredokaOne

            local active = true
            local dragging, dragInput, dragStart, startPos = false, nil, nil, nil
            local dragged = false

            local function update(input)
                if not dragStart or not startPos then return end
                local delta = input.Position - dragStart
                if math.abs(delta.X) > 5 or math.abs(delta.Y) > 5 then
                    dragged = true
                end
                local camera = workspace.CurrentCamera
                local viewport = camera and camera.ViewportSize or Vector2.new(800,600)
                local size = CircleBtn.AbsoluteSize
                local x = math.clamp(startPos.X.Offset + delta.X, 0, math.max(0, viewport.X - size.X))
                local y = math.clamp(startPos.Y.Offset + delta.Y, 0, math.max(0, viewport.Y - size.Y))
                CircleBtn.Position = UDim2.fromOffset(x,y)
            end

            CircleBtn.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = true
                    dragged = false
                    dragStart = input.Position
                    startPos = CircleBtn.Position
                    input.Changed:Connect(function()
                        if input.UserInputState == Enum.UserInputState.End then
                            dragging = false
                        end
                    end)
                end
            end)

            CircleBtn.InputChanged:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
                    dragInput = input
                end
            end)

            local inputChanged = UserInputService.InputChanged:Connect(function(input)
                if input == dragInput and dragging and CircleBtn.Parent then update(input) end
            end)

            -- نبض ثابت: نحرّك UIScale فقط، ولا نغيّر Size الحقيقي للزر
            -- حتى لا يعلق الـTween ولا يصغر الزر نهائياً.
            local PulseScale = Instance.new("UIScale")
            PulseScale.Name = "PulseScale"
            PulseScale.Scale = 1
            PulseScale.Parent = CircleBtn

            local pulseTask = task.spawn(function()
                while active and S.autoClickerAlive and CircleBtn.Parent do
                    local ok1 = pcall(function()
                        local t1 = TweenService:Create(PulseScale,TweenInfo.new(0.55,Enum.EasingStyle.Sine,Enum.EasingDirection.InOut),{Scale=1.08})
                        t1:Play()
                        t1.Completed:Wait()
                    end)
                    if not ok1 or not active or not S.autoClickerAlive or not CircleBtn.Parent then break end
                    local ok2 = pcall(function()
                        local t2 = TweenService:Create(PulseScale,TweenInfo.new(0.55,Enum.EasingStyle.Sine,Enum.EasingDirection.InOut),{Scale=1})
                        t2:Play()
                        t2.Completed:Wait()
                    end)
                    if not ok2 then break end
                    PulseScale.Scale=1
                end
                pcall(function() PulseScale.Scale=1 end)
            end)

            local clickTask = task.spawn(function()
                while active and S.autoClickerAlive and CircleBtn.Parent do
                    local centerPosition = CircleBtn.AbsolutePosition + (CircleBtn.AbsoluteSize / 2)
                    pcall(function()
                        VirtualInputManager:SendMouseButtonEvent(centerPosition.X,centerPosition.Y+36,0,true,game,0)
                        task.wait(0.03)
                        VirtualInputManager:SendMouseButtonEvent(centerPosition.X,centerPosition.Y+36,0,false,game,0)
                    end)
                    task.wait(1)
                end
            end)

            CircleBtn.MouseButton1Click:Connect(function()
                -- السحب لا يعتبر ضغطة إيقاف. الضغط بدون تحريك فقط يوقف الزر.
                if dragged then
                    dragged = false
                    return
                end
                if not active then return end
                active = false
                S.autoClickerActive = false
                pcall(function() inputChanged:Disconnect() end)
                pcall(function() S.autoClickerGui:Destroy() end)
                S.autoClickerGui = nil
                S.autoClickerAlive = false
            end)
        end

        S.autoClickerBtn = S.makeButton("AutoCliker: OFF",10,324,320)
        S.autoClickerBtn.Activated:Connect(function()
            S.autoClickerActive = not S.autoClickerActive
            if S.autoClickerActive then
                S.autoClickerBtn.Text = "AutoCliker: ON"
                createAutoClicker()
            else
                S.autoClickerBtn.Text = "AutoCliker: OFF"
                S.autoClickerAlive = false
                if S.autoClickerGui then
                    pcall(function() S.autoClickerGui:Destroy() end)
                    S.autoClickerGui = nil
                end
            end
        end)
        
        S.rejoinBtn.Activated:Connect(function()
            -- منع AutoCliker من تفعيل Rejoin عن طريق نقرة افتراضية.
            if S.autoClickerActive then return end
            pcall(function()
                game:GetService("TeleportService"):Teleport(game.PlaceId, S.player)
            end)
        end)

        S.lowServerBtn.Activated:Connect(function()
            pcall(function()
                local url = "https://games.roblox.com/v1/games/" .. tostring(game.PlaceId) .. "/servers/Public?sortOrder=Asc&limit=100"
                local data
                local ok, result = pcall(function()
                    return game:HttpGetAsync(url)
                end)
                if ok then
                    data = result
                elseif typeof(request) == "function" then
                    local response = request({Url = url, Method = "GET"})
                    if response and response.Body then data = response.Body end
                elseif typeof(http_request) == "function" then
                    local response = http_request({Url = url, Method = "GET"})
                    if response and response.Body then data = response.Body end
                end
                if not data then return end

                local servers = S.HttpService:JSONDecode(data)
                local targetServer = nil
                local lowestPlayers = math.huge
                for _, server in ipairs(servers.data or {}) do
                    if server.id ~= game.JobId and server.playing < server.maxPlayers and server.playing < lowestPlayers then
                        lowestPlayers = server.playing
                        targetServer = server.id
                    end
                end
                if targetServer then
                    game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, targetServer, S.player)
                end
            end)
        end)

        function S.sectionLabel(text,y)
            local label = Instance.new("TextLabel")
            label.Size = UDim2.fromOffset(320,20)
            label.Position = UDim2.fromOffset(10,y)
            label.BackgroundTransparency = 1
            label.Text = text
            label.TextColor3 = Color3.fromRGB(205,215,235)
            label.TextSize = 12
            label.Font = Enum.Font.GothamBold
            label.TextXAlignment = Enum.TextXAlignment.Left
            label.ZIndex = 5
            label.Parent = sfSetting
            return label
        end
        
        S.sectionLabel("Background",370)
        
        S.bgButton = S.makeButton("Choose Background  ▼",10,395,320)
        S.bgPreview = Instance.new("ImageLabel")
        S.bgPreview.Size = UDim2.fromOffset(25,25)
        S.bgPreview.Position = UDim2.new(1,-30,0.5,-12)
        S.bgPreview.BackgroundTransparency = 1
        S.bgPreview.BorderSizePixel = 0
        S.bgPreview.ScaleType = Enum.ScaleType.Crop
        S.bgPreview.ZIndex = 7
        S.bgPreview.Parent = S.bgButton
        
        S.bgList = Instance.new("ScrollingFrame")
        S.bgList.Name = "BackgroundList"
        S.bgList.Size = UDim2.fromOffset(320,120)
        S.bgList.Position = UDim2.fromOffset(10,435)
        S.bgList.BackgroundColor3 = Color3.fromRGB(18,21,30)
        S.bgList.BackgroundTransparency = 0.05
        S.bgList.BorderSizePixel = 0
        S.bgList.Visible = false
        S.bgList:SetAttribute("SQRKeepHidden", true)
        S.bgList.ScrollBarThickness = 4
        S.bgList.ScrollingEnabled = true
        S.bgList.Active = true
        S.bgList.AutomaticCanvasSize = Enum.AutomaticSize.Y
        S.bgList.CanvasSize = UDim2.new(0,0,0,1)
        S.bgList.ZIndex = 20
        S.bgList.Parent = sfSetting
        Instance.new("UICorner",S.bgList).CornerRadius = UDim.new(0,10)
        
        S.bgLayout = Instance.new("UIListLayout")
        S.bgLayout.Padding = UDim.new(0,4)
        S.bgLayout.Parent = S.bgList
        
        S.selectedBgName = S.settingState.backgroundName
        
        if S.settingState.background then
            S.settingState.background = tostring(S.settingState.background)
        end
        
        -- ============================================================
        -- حفظ الخلفية فقط في ملف مستقل: آخر خلفية تم اختيارها
        -- ============================================================
        S.BACKGROUND_FILE = "SQR_Background_Last.json"

        function S.saveLastBackground(name,id)
            local cleanId = tostring(id or ""):gsub("%s+", "")
            cleanId = cleanId:gsub("rbxassetid://", "")
            if cleanId == "" or not tonumber(cleanId) then return false end

            local payload = {
                background = cleanId,
                backgroundName = tostring(name or "Custom")
            }

            local okEncode, encoded = pcall(function()
                return S.HttpService:JSONEncode(payload)
            end)
            if not okEncode or not encoded then return false end

            if type(writefile) == "function" then
                local ok = pcall(function()
                    writefile(S.BACKGROUND_FILE, encoded)
                end)
                if ok then return true end
            end

            if type(savefile) == "function" then
                local ok = pcall(function()
                    savefile(S.BACKGROUND_FILE, encoded)
                end)
                if ok then return true end
            end

            return false
        end

        function S.loadLastBackground()
            if type(isfile) ~= "function" or type(readfile) ~= "function" then
                return nil,nil
            end

            local exists = false
            pcall(function()
                exists = isfile(S.BACKGROUND_FILE)
            end)
            if not exists then return nil,nil end

            local okRead, content = pcall(function()
                return readfile(S.BACKGROUND_FILE)
            end)
            if not okRead or not content or content == "" then return nil,nil end

            local okDecode, data = pcall(function()
                return S.HttpService:JSONDecode(content)
            end)
            if not okDecode or type(data) ~= "table" then return nil,nil end

            local id = tostring(data.background or ""):gsub("%s+", "")
            id = id:gsub("rbxassetid://", "")
            if id == "" or not tonumber(id) then return nil,nil end

            return id, tostring(data.backgroundName or "Custom")
        end

        function S.applyBackground(id,displayName)
            local cleanId = tostring(id or ""):gsub("%s+", "")
            cleanId = cleanId:gsub("rbxassetid://", "")
            if cleanId == "" or not tonumber(cleanId) then return false end

            local name = tostring(displayName or "Custom")
            S.settingState.background = cleanId
            S.settingState.backgroundId = cleanId
            S.settingState.backgroundName = name
            S.selectedBgName = name

            local imageId = "rbxassetid://" .. cleanId
            S.selectedBgImage.Image = imageId
            S.selectedBgImage.Visible = true
            S.bgPreview.Image = imageId
            S.bgButton.Text = "Background: " .. name .. "  ▼"

            -- حفظ آخر خلفية اختارها المستخدم في ملف مستقل.
            S.saveLastBackground(name,cleanId)
            return true
        end
        
        -- الملف المستقل هو المصدر الأساسي للخلفية المحفوظة.
        local savedBgId, savedBgName = S.loadLastBackground()
        if not savedBgId then
            savedBgId = S.settingState.background or S.settingState.backgroundId
            savedBgName = S.settingState.backgroundName or "Custom"
        end

        if savedBgId then
            local cleanSavedId = tostring(savedBgId):gsub("%s+", ""):gsub("rbxassetid://", "")
            if cleanSavedId ~= "" and tonumber(cleanSavedId) then
                S.settingState.background = cleanSavedId
                S.settingState.backgroundId = cleanSavedId
                S.settingState.backgroundName = tostring(savedBgName or "Custom")
                S.selectedBgName = S.settingState.backgroundName
                S.selectedBgImage.Image = "rbxassetid://" .. cleanSavedId
                S.selectedBgImage.Visible = true
                S.bgPreview.Image = "rbxassetid://" .. cleanSavedId
                S.bgButton.Text = "Background: " .. S.selectedBgName .. "  ▼"
            end
        end

        -- مهم: نستخدم TextButton الحقيقي هنا لأن ScrollingFrame على الهاتف
        -- يحتاج عنصرًا تفاعليًا حقيقيًا حتى يسمح بالسحب والاختيار بدون تعطيل التمرير.
        for _,item in ipairs(S.settingsData) do
            local row = Instance.new("TextButton")
            row.Size = UDim2.new(1,-8,0,35)
            row.BackgroundColor3 = Color3.fromRGB(30,34,45)
            row.BackgroundTransparency = 0.1
            row.BorderSizePixel = 0
            row.Text = ""
            row.AutoButtonColor = false
            row.ZIndex = 21
            row.Parent = S.bgList
            Instance.new("UICorner",row).CornerRadius = UDim.new(0,8)
            
            local rowImage = Instance.new("ImageLabel")
            rowImage.Size = UDim2.new(1,0,1,0)
            rowImage.BackgroundTransparency = 1
            rowImage.BorderSizePixel = 0
            rowImage.ScaleType = Enum.ScaleType.Crop
            rowImage.Image = "rbxassetid://" .. item[2]
            rowImage.ImageTransparency = 0.48
            rowImage.ZIndex = 21
            rowImage.Parent = row
            
            local shade = Instance.new("Frame")
            shade.Size = UDim2.new(1,0,1,0)
            shade.BackgroundColor3 = Color3.fromRGB(0,0,0)
            shade.BackgroundTransparency = 0.35
            shade.BorderSizePixel = 0
            shade.BorderSizePixel = 0
            shade.ZIndex = 22
            shade.Parent = row
            
            local rowText = Instance.new("TextLabel")
            rowText.Size = UDim2.new(1,-16,1,0)
            rowText.Position = UDim2.fromOffset(8,0)
            rowText.BackgroundTransparency = 1
            rowText.Text = item[1]
            rowText.TextColor3 = Color3.fromRGB(255,255,255)
            rowText.TextSize = 12
            rowText.Font = Enum.Font.GothamBold
            rowText.TextXAlignment = Enum.TextXAlignment.Left
            rowText.ZIndex = 23
            rowText.Parent = row
            
            row.Activated:Connect(function()
                S.applyBackground(item[2],item[1])
                S.bgList.Visible = false
            end)
        end
        
        S.bgLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            S.bgList.CanvasSize = UDim2.new(0,0,0,math.max(1,S.bgLayout.AbsoluteContentSize.Y+16))
        end)
        
        
        S.bgButton.Activated:Connect(function()
            S.bgList.Visible = not S.bgList.Visible
            S.bgButton.Text = "Background: " .. (S.selectedBgName or "Choose") .. (S.bgList.Visible and "  ▲" or "  ▼")
        end)
        
        S.sectionLabel("Asset ID", 470)
        S.assetIdBox = S.makeBox("ضع Asset ID للصورة",10,492,205)
        S.applyAssetButton = S.makeButton("Apply",220,492,110)
        S.applyAssetButton.Activated:Connect(function()
            local id = tostring(S.assetIdBox.Text or ""):gsub("%s+", "")
            id = id:gsub("rbxassetid://", "")
            if id ~= "" and tonumber(id) then
                S.applyBackground(id, "Custom")
                S.bgList.Visible = false
            end
        end)

        -- تم إلغاء شريط الشفافية: الخلفية تظهر بجودتها الطبيعية دائماً.
        if S.selectedBgImage then
            S.selectedBgImage.ImageTransparency = 0
        end
        S.settingState.transparency = 0

        -- شريط حجم الواجهة
        S.sectionLabel("UI Size", 615)
        S.uiScaleValueLabel = S.makeButton("Size: 100%",10,637,90)
        S.uiScaleValueLabel.TextSize = 12
        S.uiScaleSlider = Instance.new("Frame")
        S.uiScaleSlider.Size = UDim2.fromOffset(220,10)
        S.uiScaleSlider.Position = UDim2.fromOffset(110,650)
        S.uiScaleSlider.BackgroundColor3 = Color3.fromRGB(60,60,80)
        S.uiScaleSlider.BorderSizePixel = 0
        S.uiScaleSlider.Active = true
        S.uiScaleSlider.ZIndex = 5
        S.uiScaleSlider.Parent = sfSetting
        Instance.new("UICorner",S.uiScaleSlider).CornerRadius = UDim.new(1,0)

        S.uiScaleFill = Instance.new("Frame")
        S.uiScaleFill.Size = UDim2.new(0.5,0,1,0)
        S.uiScaleFill.BackgroundColor3 = Color3.fromRGB(0,200,255)
        S.uiScaleFill.BorderSizePixel = 0
        S.uiScaleFill.Parent = S.uiScaleSlider
        Instance.new("UICorner",S.uiScaleFill).CornerRadius = UDim.new(1,0)

        S.uiScaleKnob = Instance.new("Frame")
        S.uiScaleKnob.Size = UDim2.fromOffset(18,18)
        S.uiScaleKnob.Position = UDim2.new(0.5,-9,0.5,-9)
        S.uiScaleKnob.BackgroundColor3 = Color3.fromRGB(0,200,255)
        S.uiScaleKnob.BorderSizePixel = 0
        S.uiScaleKnob.ZIndex = 6
        S.uiScaleKnob.Parent = S.uiScaleSlider
        Instance.new("UICorner",S.uiScaleKnob).CornerRadius = UDim.new(1,0)

        function S.applyUIScale(percent)
            percent = math.clamp(tonumber(percent) or 100,70,130)
            local t = (percent-70)/60
            local scale = percent/100
            S.settingState.uiScale = scale

            -- UIScale يعمل فقط على الرسم الداخلي؛ نعوّض Position حتى يكبر من المنتصف.
            if S.uiScale then
                S.uiScale.Scale = scale
            end
            if S.mainFrame then
                S.mainFrame.Position = UDim2.new(0.5, -300 * scale, 0.5, -200 * scale)
            end

            S.uiScaleFill.Size = UDim2.new(t,0,1,0)
            S.uiScaleKnob.Position = UDim2.new(t,-9,0.5,-9)
            S.uiScaleValueLabel.Text = "Size: "..math.floor(percent).."%"
        end

        local uiScaleDragging = false
        local function updateUIScaleSlider(inputX)
            local left=S.uiScaleSlider.AbsolutePosition.X
            local width=S.uiScaleSlider.AbsoluteSize.X
            local t=math.clamp((inputX-left)/math.max(width,1),0,1)
            S.applyUIScale(70 + t*60)
        end
        S.uiScaleSlider.InputBegan:Connect(function(input)
            if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then
                uiScaleDragging=true
                updateUIScaleSlider(input.Position.X)
            end
        end)
        S.UserInputService.InputChanged:Connect(function(input)
            if uiScaleDragging and (input.UserInputType==Enum.UserInputType.MouseMovement or input.UserInputType==Enum.UserInputType.Touch) then
                updateUIScaleSlider(input.Position.X)
            end
        end)
        S.UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then
                if uiScaleDragging then
                    uiScaleDragging=false
                    S.saveSettings()
                end
            end
        end)

        function S.clampBox(box,default)
            local n = tonumber(box.Text) or default
            n = math.clamp(n,1,1000)
            box.Text = tostring(math.floor(n))
            return n
        end
        
        S.speedBox.FocusLost:Connect(function()
            S.settingState.walkSpeed = S.clampBox(S.speedBox,16)
            local hum = S.player.Character and S.player.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.WalkSpeed = S.settingState.walkSpeed end
            S.saveSettings()
        end)
        
        S.flySpeedBox.FocusLost:Connect(function()
            S.settingState.flySpeed = S.clampBox(S.flySpeedBox,50)
            S.saveSettings()
        end)

        -- تأكيد حفظ القيم النصية حتى لو تغيرت من سكربت آخر أثناء التشغيل.
        S.speedBox:GetPropertyChangedSignal("Text"):Connect(function()
            local n = tonumber(S.speedBox.Text)
            if n then S.settingState.walkSpeed = math.clamp(n,1,1000); S.saveSettings() end
        end)
        S.flySpeedBox:GetPropertyChangedSignal("Text"):Connect(function()
            local n = tonumber(S.flySpeedBox.Text)
            if n then S.settingState.flySpeed = math.clamp(n,1,1000); S.saveSettings() end
        end)
        
        function S.getHumanoid()
            return S.player.Character and S.player.Character:FindFirstChildOfClass("Humanoid")
        end
        
        function S.applyWalkSpeed()
            local hum = S.getHumanoid()
            if hum then hum.WalkSpeed = math.clamp(tonumber(S.settingState.walkSpeed) or 16,1,1000) end
        end
        
        S.freezeBtn.Activated:Connect(function()
            local root = S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart")
            if not root then return end
            S.settingState.frozen = not S.settingState.frozen
            root.Anchored = S.settingState.frozen
            S.freezeBtn.Text = S.settingState.frozen and "Unfreeze" or "Freeze"
            S.freezeBtn.BackgroundColor3 = S.settingState.frozen and Color3.fromRGB(180,60,60) or Color3.fromRGB(35,40,55)
            S.saveSettings()
        end)
        
        S.flyConnection = nil
        S.flyVelocity = nil
        S.flyGyro = nil
        S.flyHumanoid = nil
        S.flyOldAutoRotate = nil
        S.flyRespawnConnection = S.flyRespawnConnection or nil
        
        function S.stopFly()
            S.settingState.flying = false
            if S.flyConnection then S.flyConnection:Disconnect(); S.flyConnection = nil end
            if S.flyVelocity then S.flyVelocity:Destroy(); S.flyVelocity = nil end
            if S.flyGyro then S.flyGyro:Destroy(); S.flyGyro = nil end
            if S.flyHumanoid and S.flyHumanoid.Parent then
                S.flyHumanoid.AutoRotate = (S.flyOldAutoRotate == nil) and true or S.flyOldAutoRotate
            end
            S.flyHumanoid = nil
            S.flyOldAutoRotate = nil
            local root = S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart")
            if root then root.AssemblyLinearVelocity = Vector3.zero end
            S.flyBtn.Text = "Fly"
            S.flyBtn.BackgroundColor3 = Color3.fromRGB(35,40,55)
        end
        
        function S.startFly()
            local char = S.player.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if not root or not hum then return end
            S.stopFly()
            S.settingState.flying = true
            S.flyHumanoid = hum
            S.flyOldAutoRotate = hum.AutoRotate
            hum.AutoRotate = false

            S.flyVelocity = Instance.new("BodyVelocity")
            S.flyVelocity.MaxForce = Vector3.new(1e7,1e7,1e7)
            S.flyVelocity.P = 10000
            S.flyVelocity.Velocity = Vector3.zero
            S.flyVelocity.Parent = root

            S.flyGyro = Instance.new("BodyGyro")
            S.flyGyro.MaxTorque = Vector3.new(1e7,1e7,1e7)
            S.flyGyro.P = 10000
            S.flyGyro.D = 500
            S.flyGyro.CFrame = root.CFrame
            S.flyGyro.Parent = root

            S.flyConnection = S.RunService.RenderStepped:Connect(function()
                if not S.settingState.flying or not root.Parent or not hum.Parent then
                    S.stopFly()
                    return
                end
                local cam = S.Workspace.CurrentCamera
                if not cam then return end

                -- يدعم الكيبورد والـ Mobile joystick معاً.
                local move = hum.MoveDirection
                local look = cam.CFrame.LookVector
                local right = cam.CFrame.RightVector
                local horizontalLook = Vector3.new(look.X, 0, look.Z)
                local horizontalRight = Vector3.new(right.X, 0, right.Z)
                if horizontalLook.Magnitude > 0 then horizontalLook = horizontalLook.Unit end
                if horizontalRight.Magnitude > 0 then horizontalRight = horizontalRight.Unit end

                local keyboardMove = Vector3.zero
                if S.UIS:IsKeyDown(Enum.KeyCode.W) then keyboardMove = keyboardMove + horizontalLook end
                if S.UIS:IsKeyDown(Enum.KeyCode.S) then keyboardMove = keyboardMove - horizontalLook end
                if S.UIS:IsKeyDown(Enum.KeyCode.D) then keyboardMove = keyboardMove + horizontalRight end
                if S.UIS:IsKeyDown(Enum.KeyCode.A) then keyboardMove = keyboardMove - horizontalRight end
                if keyboardMove.Magnitude > 0 then move = keyboardMove end

                local vertical = 0
                if S.UIS:IsKeyDown(Enum.KeyCode.Space) or hum.Jump then vertical = vertical + 1 end
                if S.UIS:IsKeyDown(Enum.KeyCode.LeftControl) or S.UIS:IsKeyDown(Enum.KeyCode.LeftShift) then vertical = vertical - 1 end

                local direction = Vector3.new(move.X, vertical, move.Z)
                local speed = math.clamp(tonumber(S.settingState.flySpeed) or 50,1,1000)
                S.flyVelocity.Velocity = direction.Magnitude > 0 and direction.Unit * speed or Vector3.zero

                local face = Vector3.new(look.X, 0, look.Z)
                if face.Magnitude > 0 then
                    S.flyGyro.CFrame = CFrame.lookAt(root.Position, root.Position + face.Unit)
                end
            end)
            S.flyBtn.Text = "Unfly"
            S.flyBtn.BackgroundColor3 = Color3.fromRGB(40,150,90)
        end

        -- Re-apply WalkSpeed and Fly after a character replacement.
        if not S.settingRespawnConnection then
            S.settingRespawnConnection=S.player.CharacterAdded:Connect(function()
                task.wait(0.15)
                pcall(function() S.applyWalkSpeed() end)
                if S.settingState.flying then
                    pcall(function() S.startFly() end)
                end
            end)
        end
        
        S.flyBtn.Activated:Connect(function()
            if S.settingState.flying then S.stopFly() else S.startFly() end
            S.saveSettings()
        end)
        
        S.cameraConnection = nil
        S.savedCameraCFrame = nil
        
        S.cameraBtn.Activated:Connect(function()
            S.settingState.cameraFrozen = not S.settingState.cameraFrozen
            local cam = S.Workspace.CurrentCamera
            if S.settingState.cameraFrozen then
                if cam then S.savedCameraCFrame = cam.CFrame end
                if S.cameraConnection then S.cameraConnection:Disconnect() end
                S.cameraConnection = S.RunService.RenderStepped:Connect(function()
                    if S.settingState.cameraFrozen and S.Workspace.CurrentCamera and S.savedCameraCFrame then
                        S.Workspace.CurrentCamera.CFrame = S.savedCameraCFrame
                    end
                end)
                S.cameraBtn.Text = "Camera Frozen"
                S.cameraBtn.BackgroundColor3 = Color3.fromRGB(180,60,60)
            else
                if S.cameraConnection then S.cameraConnection:Disconnect(); S.cameraConnection = nil end
                if S.Workspace.CurrentCamera then
                    local hum = S.getHumanoid()
                    if hum then S.Workspace.CurrentCamera.CameraSubject = hum end
                end
                S.cameraBtn.Text = "Freeze Camera"
                S.cameraBtn.BackgroundColor3 = Color3.fromRGB(35,40,55)
            end
            S.saveSettings()
        end)
        
        function S.setCharacterHidden(hidden)
            if not S.player.Character then return end
            for _,obj in ipairs(S.player.Character:GetDescendants()) do
                if obj:IsA("BasePart") or obj:IsA("Decal") or obj:IsA("Texture") then
                    obj.LocalTransparencyModifier = hidden and 1 or 0
                elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") or obj:IsA("Smoke") or obj:IsA("Fire") or obj:IsA("Sparkles") then
                    obj.Enabled = not hidden
                end
            end
        end
        
        S.hideBtn.Activated:Connect(function()
            S.settingState.hidden = not S.settingState.hidden
            S.setCharacterHidden(S.settingState.hidden)
            S.hideBtn.Text = S.settingState.hidden and "Show Character" or "Hide Character"
            S.hideBtn.BackgroundColor3 = S.settingState.hidden and Color3.fromRGB(180,60,60) or Color3.fromRGB(35,40,55)
            S.saveSettings()
        end)
        
        S.respawnConnection = nil
        S.respawnDiedConnection = nil
        S.respawnRemovingConnection = nil
        S.autoRespawnTask = nil
        
        function S.forceAutoRespawn()
            if not S.settingState.autoRespawn then return end
            local LocalPlayer = game:GetService("Players").LocalPlayer
            if not LocalPlayer then return end

            -- طريقة الـ Respawn التي طلبتها بالضبط.
            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
                LocalPlayer.Character.Humanoid.Health = 0
                return
            end

            -- احتياط فقط إذا لم توجد الشخصية أصلًا.
            pcall(function()
                LocalPlayer:LoadCharacter()
            end)
        end

        function S.bindAutoRespawnCharacter(char)
            task.wait(0.2)
            if not S.settingState.autoRespawn then return end
            S.applyWalkSpeed()
            if S.settingState.hidden then S.setCharacterHidden(true) end
            if S.settingState.frozen then
                local root = char and char:FindFirstChild("HumanoidRootPart")
                if root then root.Anchored = true end
            end
        end
        
        function S.startAutoRespawn()
            if S.respawnConnection then return end
            S.settingState.autoRespawn = true

            S.respawnConnection = S.player.CharacterAdded:Connect(function(char)
                S.bindAutoRespawnCharacter(char)
            end)

            -- مراقبة الشخصية حتى يبقى Auto Respawn فعالًا بعد كل موت.
            S.autoRespawnTask = task.spawn(function()
                while S.settingState.autoRespawn do
                    local LocalPlayer = S.player
                    local char = LocalPlayer and LocalPlayer.Character
                    local hum = char and char:FindFirstChild("Humanoid")
                    if not char or not hum then
                        task.wait(0.2)
                    elseif hum.Health <= 0 then
                        pcall(function() LocalPlayer:LoadCharacter() end)
                        task.wait(0.5)
                    else
                        task.wait(0.2)
                    end
                end
            end)

            if S.player.Character then
                S.bindAutoRespawnCharacter(S.player.Character)
            end
        end
        
        function S.stopAutoRespawn()
            S.settingState.autoRespawn = false
            if S.respawnConnection then S.respawnConnection:Disconnect(); S.respawnConnection = nil end
            if S.autoRespawnTask then pcall(function() S.safeCancel(S.autoRespawnTask) end); S.autoRespawnTask = nil end
        end
        
        S.respawnBtn.Activated:Connect(function()
            if S.settingState.autoRespawn then
                -- OFF = إيقاف Auto Respawn بالكامل فوراً.
                S.settingState.autoRespawn = false
                S.stopAutoRespawn()
                S.respawnBtn.Text = "Auto Respawn: OFF"
                S.respawnBtn.BackgroundColor3 = Color3.fromRGB(35,40,55)
            else
                S.settingState.autoRespawn = true
                S.startAutoRespawn()
                -- تشغيل طريقة الـ Respawn المطلوبة عند تفعيل الزر.
                task.defer(function()
                    if S.settingState.autoRespawn then
                        local LocalPlayer = game:GetService("Players").LocalPlayer
                        if LocalPlayer and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
                            LocalPlayer.Character.Humanoid.Health = 0
                        end
                    end
                end)
                S.respawnBtn.Text = "Auto Respawn: ON"
                S.respawnBtn.BackgroundColor3 = Color3.fromRGB(40,150,90)
            end
            S.saveSettings()
        end)
        
        S.player.CharacterAdded:Connect(function(char)
            wait(0.25)
            S.applyWalkSpeed()
            if S.settingState.frozen then
                local root = char:FindFirstChild("HumanoidRootPart")
                if root then root.Anchored = true end
            end
            if S.settingState.hidden then S.setCharacterHidden(true) end
        end)
        
        S.applyWalkSpeed()
        local restoreBackgroundId = S.settingState.background or S.settingState.backgroundId
        if restoreBackgroundId and tostring(restoreBackgroundId) ~= "" then
            restoreBackgroundId = tostring(restoreBackgroundId):gsub("%s+", ""):gsub("rbxassetid://", "")
            S.settingState.background = restoreBackgroundId
            S.settingState.backgroundId = restoreBackgroundId
            local restoredName = S.settingState.backgroundName or "Custom"
            for _,item in ipairs(S.settingsData) do
                if tostring(item[2]) == tostring(restoreBackgroundId) then restoredName=item[1]; break end
            end
            local savedName = S.settingState.backgroundName or restoredName
            S.applyBackground(restoreBackgroundId,savedName)
            S.assetIdBox.Text=tostring(restoreBackgroundId)
        end
        if S.selectedBgImage then S.selectedBgImage.ImageTransparency = 0 end
        S.applyUIScale((tonumber(S.settingState.uiScale) or 1)*100)
        
        if S.settingState.frozen then
            local root = S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart")
            if root then root.Anchored = true end
            S.freezeBtn.Text = "Unfreeze"
            S.freezeBtn.BackgroundColor3 = Color3.fromRGB(180,60,60)
        end
        if S.settingState.hidden then
            S.setCharacterHidden(true)
            S.hideBtn.Text = "Show Character"
            S.hideBtn.BackgroundColor3 = Color3.fromRGB(180,60,60)
        end
        
        -- استرجاع كل إعداد محفوظ بعد إنشاء عناصر الواجهة.
        if S.settingState.flying then
            task.defer(function()
                if S.startFly then S.startFly() end
            end)
        else
            S.flyBtn.Text = "Fly"
            S.flyBtn.BackgroundColor3 = Color3.fromRGB(35,40,55)
        end

        if S.settingState.cameraFrozen then
            local cam = S.Workspace.CurrentCamera
            if cam then S.savedCameraCFrame = cam.CFrame end
            if S.cameraConnection then S.cameraConnection:Disconnect() end
            S.cameraConnection = S.RunService.RenderStepped:Connect(function()
                if S.settingState.cameraFrozen and S.Workspace.CurrentCamera and S.savedCameraCFrame then
                    S.Workspace.CurrentCamera.CFrame = S.savedCameraCFrame
                end
            end)
            S.cameraBtn.Text = "Camera Frozen"
            S.cameraBtn.BackgroundColor3 = Color3.fromRGB(180,60,60)
        else
            S.cameraBtn.Text = "Freeze Camera"
            S.cameraBtn.BackgroundColor3 = Color3.fromRGB(35,40,55)
        end

        if S.settingState.autoRespawn then
            S.startAutoRespawn()
            S.respawnBtn.Text = "Auto Respawn: ON"
            S.respawnBtn.BackgroundColor3 = Color3.fromRGB(40,150,90)
        else
            S.respawnBtn.Text = "Auto Respawn: OFF"
            S.respawnBtn.BackgroundColor3 = Color3.fromRGB(35,40,55)
        end
        S.saveSettings()
    end
end

-- ============================================================
-- SCOPE 26: 3dad SECTION
-- ============================================================
do
    local S = _G.SQR
    local sf3dad = S.sectionFrames[table.find(S.sections, "3dad")]

    if sf3dad then
        sf3dad.ClipsDescendants = true

        S.countAllButton = Instance.new("TextButton")
        S.countAllButton.Size = UDim2.fromOffset(50,35)
        S.countAllButton.Position = UDim2.fromOffset(10,30)
        S.countAllButton.BackgroundColor3 = Color3.fromRGB(35,40,55)
        S.countAllButton.BorderSizePixel = 0
        S.countAllButton.Text = "ALL"
        S.countAllButton.TextColor3 = Color3.fromRGB(255,255,255)
        S.countAllButton.TextSize = 12
        S.countAllButton.Font = Enum.Font.GothamBold
        S.countAllButton.Active = true
        S.countAllButton.AutoButtonColor = true
        S.countAllButton.ZIndex = 80
        S.countAllButton.Parent = sf3dad
        Instance.new("UICorner",S.countAllButton).CornerRadius = UDim.new(0,9)
        S.addButtonFeedback(S.countAllButton)

        S.countSearchBox = Instance.new("TextBox")
        S.countSearchBox.Size = UDim2.new(1, -170, 0, 35)
        S.countSearchBox.Position = UDim2.fromOffset(66, 30)
        S.countSearchBox.BackgroundColor3 = Color3.fromRGB(35,40,55)
        S.countSearchBox.BackgroundTransparency = 0.05
        S.countSearchBox.BorderSizePixel = 0
        S.countSearchBox.TextColor3 = Color3.fromRGB(255,255,255)
        S.countSearchBox.PlaceholderText = "Username..."
        S.countSearchBox.Text = ""
        S.countSearchBox.ClearTextOnFocus = false
        S.countSearchBox.Font = Enum.Font.Gotham
        S.countSearchBox.TextSize = 13
        S.countSearchBox.Active = true
        S.countSearchBox.ZIndex = 80
        S.countSearchBox.Parent = sf3dad
        Instance.new("UICorner",S.countSearchBox).CornerRadius = UDim.new(0,9)

        S.countTrackButton = Instance.new("TextButton")
        S.countTrackButton.Size = UDim2.fromOffset(90,35)
        S.countTrackButton.Position = UDim2.new(1,-100,0,30)
        S.countTrackButton.BackgroundColor3 = Color3.fromRGB(0,200,0)
        S.countTrackButton.BorderSizePixel = 0
        S.countTrackButton.Text = "Select"
        S.countTrackButton.TextColor3 = Color3.fromRGB(255,255,255)
        S.countTrackButton.TextSize = 13
        S.countTrackButton.Font = Enum.Font.GothamBold
        S.countTrackButton.Active = true
        S.countTrackButton.AutoButtonColor = true
        S.countTrackButton.ZIndex = 80
        S.countTrackButton.Parent = sf3dad
        Instance.new("UICorner",S.countTrackButton).CornerRadius = UDim.new(0,9)
        S.addButtonFeedback(S.countTrackButton)

        S.countInfoLabel = Instance.new("TextLabel")
        S.countInfoLabel.Size = UDim2.new(1,-20,0,25)
        S.countInfoLabel.Position = UDim2.fromOffset(10,72)
        S.countInfoLabel.BackgroundTransparency = 1
        S.countInfoLabel.Text = "اكتب اليوزر ثم اضغط Select — ALL لتحديد الجميع"
        S.countInfoLabel.TextColor3 = Color3.fromRGB(200,210,230)
        S.countInfoLabel.TextSize = 12
        S.countInfoLabel.Font = Enum.Font.GothamMedium
        S.countInfoLabel.TextXAlignment = Enum.TextXAlignment.Left
        S.countInfoLabel.Parent = sf3dad

        S.ownerStayLabel = Instance.new("TextLabel")
        S.ownerStayLabel.Size = UDim2.new(1,-20,0,20)
        S.ownerStayLabel.Position = UDim2.fromOffset(10,7)
        S.ownerStayLabel.BackgroundTransparency = 1
        S.ownerStayLabel.TextColor3 = Color3.fromRGB(120,220,255)
        S.ownerStayLabel.TextSize = 12
        S.ownerStayLabel.Font = Enum.Font.GothamBold
        S.ownerStayLabel.TextXAlignment = Enum.TextXAlignment.Center
        S.ownerStayLabel.ZIndex = 6
        S.ownerStayLabel.Parent = sf3dad

        local ownerSessionStart = os.clock()
        local function formatOwnerStay(seconds)
            seconds = math.max(0, math.floor(seconds or 0))
            local h = math.floor(seconds / 3600)
            local m = math.floor((seconds % 3600) / 60)
            local sec = seconds % 60
            return string.format("مدة بقاء صاحب السكربت: %02dh %02dm %02ds", h, m, sec)
        end
        S.ownerStayLabel.Text = formatOwnerStay(0)
        task.spawn(function()
            while S.remoteCommandAlive and S.ownerStayLabel do
                if S.ownerStayLabel.Parent then
                    local ownerStay = math.max(0, math.floor(os.clock() - ownerSessionStart))
                    S.ownerStayLabel.Text = formatOwnerStay(ownerStay)
                    S.ownerStayLabel.TextColor3 = (ownerStay % 2 == 1)
                        and Color3.fromRGB(255,255,255)
                        or Color3.fromRGB(0,0,0)
                else
                    break
                end
                task.wait(1)
            end
        end)

        S.countScrollFrame = Instance.new("ScrollingFrame")
        S.countScrollFrame.Size = UDim2.new(1,-20,1,-108)
        S.countScrollFrame.Position = UDim2.fromOffset(10,100)
        S.countScrollFrame.BackgroundColor3 = Color3.fromRGB(18,21,30)
        S.countScrollFrame.BackgroundTransparency = 0.15
        S.countScrollFrame.BorderSizePixel = 0
        S.countScrollFrame.ScrollBarThickness = 7
        S.countScrollFrame.ScrollingEnabled = true
        S.countScrollFrame.Active = true
        S.countScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
        S.countScrollFrame.CanvasSize = UDim2.new(0,0,0,0)
        S.countScrollFrame.ZIndex = 20
        S.countScrollFrame.Parent = sf3dad
        Instance.new("UICorner",S.countScrollFrame).CornerRadius = UDim.new(0,10)

        S.countLayout = Instance.new("UIListLayout")
        S.countLayout.Padding = UDim.new(0,6)
        S.countLayout.SortOrder = Enum.SortOrder.LayoutOrder
        S.countLayout.Parent = S.countScrollFrame

        S.countPadding = Instance.new("UIPadding")
        S.countPadding.PaddingTop = UDim.new(0,6)
        S.countPadding.PaddingBottom = UDim.new(0,6)
        S.countPadding.PaddingLeft = UDim.new(0,6)
        S.countPadding.PaddingRight = UDim.new(0,6)
        S.countPadding.Parent = S.countScrollFrame

        S.countTracked = {}
        S.countRows = {}
        S.countConnections = {}
        S.countAllEnabled = false
        local MAX_STAY_SECONDS = 400 * 60 * 60

        local function formatStay(seconds)
            seconds = math.clamp(math.floor(seconds or 0),0,MAX_STAY_SECONDS)
            local hours = math.floor(seconds / 3600)
            local minutes = math.floor((seconds % 3600) / 60)
            local secs = seconds % 60
            return string.format("%02dh %02dm %02ds", hours, minutes, secs)
        end

        local function getPlayerKey(plr)
            return tostring(plr.UserId)
        end

        local function canTrackPlayer(plr)
            if not plr or plr == S.player then return false end
            if S.isPlayerSafe and S.isPlayerSafe(plr) then return false end
            if S.isPlayerOwner(plr) then return false end
            if S.isVIP and S.isPlayerVIP(plr) then return false end
            return true
        end

        local function updateRow(data)
            if not data or not data.row then return end
            local row = data.row
            local currentSession = data.inServer
                and math.max(0,os.clock()-(data.joinedAt or os.clock()))
                or 0
            local current = math.min(MAX_STAY_SECONDS,math.max(0,(data.totalStay or 0)+currentSession))
            if data.stayLabel then
                data.stayLabel.Text = "Stay :  " .. formatStay(current)
                data.stayLabel.TextSize = 14
                -- يبدأ أسود، ثم يتناوب أسود/أبيض كل ثانية بدون توقف.
                data.stayLabel.TextColor3 = (math.floor(current)%2==1)
                    and Color3.fromRGB(255,255,255) or Color3.fromRGB(0,0,0)
            end
            if data.leftLabel then data.leftLabel.Text = "Left :  " .. tostring(data.leftCount or 0)
                data.leftLabel.TextSize = 14 end
            if data.statusLabel then
                data.statusLabel.Text = data.inServer and "Online" or "Offline"
                data.statusLabel.TextColor3 = data.inServer and Color3.fromRGB(80,220,120) or Color3.fromRGB(220,170,80)
                data.statusLabel.TextSize = 12
            end
        end

        local function createCountRow(data)
            local key = data.key
            -- لا ننشئ صفاً جديداً إذا كان الصف موجوداً؛ نحدّث نفس الصف فقط.
            if S.countRows[key] and S.countRows[key].Parent then
                data.row = S.countRows[key]
                updateRow(data)
                return
            end
            S.countRows[key] = nil

            local row = Instance.new("Frame")
            row.Size = UDim2.new(1,-14,0,78)
            row.BackgroundColor3 = Color3.fromRGB(30,34,45)
            row.BackgroundTransparency = 0.08
            row.BorderSizePixel = 0
            row.ZIndex = 60
            row.Active = true
            row.LayoutOrder = data.order or 0
            row.Parent = S.countScrollFrame
            Instance.new("UICorner",row).CornerRadius = UDim.new(0,9)

            local avatar = Instance.new("ImageLabel")
            avatar.Size = UDim2.fromOffset(52,52)
            avatar.Position = UDim2.fromOffset(8,13)
            avatar.BackgroundTransparency = 1
            avatar.BorderSizePixel = 0
            avatar.ScaleType = Enum.ScaleType.Fit
            avatar.ZIndex = 65
            avatar.Visible = true
            avatar.Parent = row
            task.spawn(function()
                local ok,image=pcall(function()
                    local content,isReady=S.Players:GetUserThumbnailAsync(data.userId,Enum.ThumbnailType.AvatarBust,Enum.ThumbnailSize.Size150x150)
                    return content
                end)
                if ok and avatar and avatar.Parent then
                    avatar.Image=image or ""
                    avatar.Visible=true
                end
            end)

            local nameLabel = Instance.new("TextLabel")
            nameLabel.Size = UDim2.new(0.34,0,0,22)
            nameLabel.Position = UDim2.fromOffset(68,7)
            nameLabel.BackgroundTransparency = 1
            nameLabel.Text = data.displayName or data.name or "Unknown"
            nameLabel.TextColor3 = Color3.fromRGB(255,255,255)
            nameLabel.TextSize = 13
            nameLabel.Font = Enum.Font.GothamBold
            nameLabel.TextXAlignment = Enum.TextXAlignment.Left
            nameLabel.ZIndex = 65
            nameLabel.Visible = true
            nameLabel.Parent = row

            local userLabel = Instance.new("TextLabel")
            userLabel.Size = UDim2.new(0.34,0,0,18)
            userLabel.Position = UDim2.fromOffset(68,29)
            userLabel.BackgroundTransparency = 1
            userLabel.Text = "@" .. tostring(data.name or "")
            userLabel.TextColor3 = Color3.fromRGB(175,185,205)
            userLabel.TextSize = 10
            userLabel.Font = Enum.Font.Gotham
            userLabel.TextXAlignment = Enum.TextXAlignment.Left
            userLabel.ZIndex = 65
            userLabel.Visible = true
            userLabel.Parent = row

            local stayLabel = Instance.new("TextLabel")
            stayLabel.Size = UDim2.new(0.30,0,0,20)
            stayLabel.Position = UDim2.new(0.52,0,0,8)
            stayLabel.BackgroundTransparency = 1
            stayLabel.TextColor3 = Color3.fromRGB(210,220,240)
            stayLabel.TextSize = 11
            stayLabel.Font = Enum.Font.GothamBold
            stayLabel.TextXAlignment = Enum.TextXAlignment.Left
            stayLabel.ZIndex = 65
            stayLabel.Visible = true
            stayLabel.Parent = row

            local leftLabel = Instance.new("TextLabel")
            leftLabel.Size = UDim2.new(0.25,0,0,18)
            leftLabel.Position = UDim2.new(0.52,0,0,32)
            leftLabel.BackgroundTransparency = 1
            leftLabel.TextColor3 = Color3.fromRGB(210,220,240)
            leftLabel.TextSize = 11
            leftLabel.Font = Enum.Font.GothamBold
            leftLabel.TextXAlignment = Enum.TextXAlignment.Left
            leftLabel.ZIndex = 65
            leftLabel.Visible = true
            leftLabel.Parent = row

            local statusLabel = Instance.new("TextLabel")
            statusLabel.Size = UDim2.fromOffset(58,18)
            statusLabel.Position = UDim2.new(1,-125,0,20)
            statusLabel.BackgroundTransparency = 1
            statusLabel.TextSize = 10
            statusLabel.Font = Enum.Font.GothamBold
            statusLabel.TextXAlignment = Enum.TextXAlignment.Right
            statusLabel.ZIndex = 65
            statusLabel.Visible = true
            statusLabel.Parent = row

            local xButton = Instance.new("TextButton")
            xButton.Size = UDim2.fromOffset(28,28)
            xButton.Position = UDim2.new(1,-35,0,43)
            xButton.BackgroundColor3 = Color3.fromRGB(190,55,55)
            xButton.BorderSizePixel = 0
            xButton.Text = "X"
            xButton.TextColor3 = Color3.fromRGB(255,255,255)
            xButton.TextSize = 11
            xButton.Font = Enum.Font.GothamBold
            xButton.ZIndex = 100
            xButton.Active = true
            xButton.Parent = row
            Instance.new("UICorner",xButton).CornerRadius = UDim.new(0,7)

            data.row = row
            data.stayLabel = stayLabel
            data.leftLabel = leftLabel
            data.statusLabel = statusLabel
            S.countRows[key] = row
            updateRow(data)
            task.defer(function()
                if row and row.Parent then
                    avatar.Visible=true
                    stayLabel.Visible=true
                    leftLabel.Visible=true
                    statusLabel.Visible=true
                end
            end)

            xButton.Activated:Connect(function()
                S.countTracked[key] = nil
                if S.countRows[key] then S.countRows[key]:Destroy(); S.countRows[key] = nil end
            end)
        end

        -- إشعارات خروج 3dad: طابور متسلسل حتى لو خرج عدة لاعبين في نفس اللحظة.
        S.countLeaveQueue = S.countLeaveQueue or {}
        S.countLeavePopupRunning = false

        local function enqueueLeaveNotification(item)
            table.insert(S.countLeaveQueue,item)
            if S.countLeavePopupRunning then return end
            S.countLeavePopupRunning=true
            task.spawn(function()
                while #S.countLeaveQueue>0 do
                    local info=table.remove(S.countLeaveQueue,1)
                    local popupGui=Instance.new("ScreenGui")
                    popupGui.Name="SQR_3dadLeavePopup"
                    popupGui.ResetOnSpawn=false
                    popupGui.IgnoreGuiInset=true
                    popupGui.DisplayOrder=100002
                    popupGui.Parent=PlayerGui

                    local frame=Instance.new("Frame")
                    frame.Size=UDim2.fromOffset(330,105)
                    frame.Position=UDim2.new(1,10,0.22,0)
                    frame.BackgroundColor3=Color3.fromRGB(25,25,30)
                    frame.BackgroundTransparency=0.05
                    frame.BorderSizePixel=0
                    frame.ZIndex=100
                    frame.Parent=popupGui
                    Instance.new("UICorner",frame).CornerRadius=UDim.new(0,10)

                    local avatar=Instance.new("ImageLabel")
                    avatar.Size=UDim2.fromOffset(72,72)
                    avatar.Position=UDim2.fromOffset(10,16)
                    avatar.BackgroundTransparency=1
                    avatar.ZIndex=101
                    avatar.Parent=frame
                    pcall(function()
                        avatar.Image=S.Players:GetUserThumbnailAsync(info.userId,Enum.ThumbnailType.AvatarBust,Enum.ThumbnailSize.Size150x150)
                    end)

                    local title=Instance.new("TextLabel")
                    title.Size=UDim2.new(1,-95,0,25)
                    title.Position=UDim2.fromOffset(90,8)
                    title.BackgroundTransparency=1
                    title.Text='The "'..tostring(info.name)..'" is left'
                    title.TextColor3=Color3.fromRGB(255,255,255)
                    title.TextSize=15
                    title.Font=Enum.Font.GothamBold
                    title.TextXAlignment=Enum.TextXAlignment.Left
                    title.ZIndex=101
                    title.Parent=frame

                    local timeLabel=Instance.new("TextLabel")
                    timeLabel.Size=UDim2.new(1,-95,0,22)
                    timeLabel.Position=UDim2.fromOffset(90,37)
                    timeLabel.BackgroundTransparency=1
                    timeLabel.Text="Time   :  "..formatStay(info.sessionDuration)
                    timeLabel.TextColor3=Color3.fromRGB(230,230,230)
                    timeLabel.TextSize=13
                    timeLabel.Font=Enum.Font.GothamMedium
                    timeLabel.TextXAlignment=Enum.TextXAlignment.Left
                    timeLabel.ZIndex=101
                    timeLabel.Parent=frame

                    local timesLabel=Instance.new("TextLabel")
                    timesLabel.Size=UDim2.new(1,-95,0,22)
                    timesLabel.Position=UDim2.fromOffset(90,62)
                    timesLabel.BackgroundTransparency=1
                    timesLabel.Text="The Times :  "..tostring(info.times)
                    timesLabel.TextColor3=Color3.fromRGB(230,230,230)
                    timesLabel.TextSize=13
                    timesLabel.Font=Enum.Font.GothamMedium
                    timesLabel.TextXAlignment=Enum.TextXAlignment.Left
                    timesLabel.ZIndex=101
                    timesLabel.Parent=frame

                    local inPos=UDim2.new(1,-340,0.22,0)
                    S.TweenService:Create(frame,TweenInfo.new(0.45,Enum.EasingStyle.Quad,Enum.EasingDirection.Out),{Position=inPos}):Play()
                    task.wait(5)
                    local outPos=UDim2.new(1,10,0.22,0)
                    local tween=S.TweenService:Create(frame,TweenInfo.new(0.45,Enum.EasingStyle.Quad,Enum.EasingDirection.In),{Position=outPos})
                    tween:Play()
                    task.wait(0.5)
                    pcall(function() popupGui:Destroy() end)
                end
                S.countLeavePopupRunning=false
            end)
        end

        local function trackPlayer(plr)
            if not canTrackPlayer(plr) then return false end
            local key = getPlayerKey(plr)
            local data = S.countTracked[key]
            if not data then
                data = {
                    key=key, userId=plr.UserId, name=plr.Name, displayName=plr.DisplayName,
                    player=plr, totalStay=0, joinedAt=os.clock(), leftCount=0, inServer=true,
                    order=#S.countTracked+1
                }
                S.countTracked[key] = data
            else
                if data.inServer then
                    data.player=plr
                    data.name=plr.Name
                    data.displayName=plr.DisplayName
                    if data.row and data.row.Parent then updateRow(data) else createCountRow(data) end
                    return true
                end
                data.player=plr
                data.name=plr.Name
                data.displayName=plr.DisplayName
                data.joinedAt=os.clock()
                data.inServer=true
            end
            createCountRow(data)
            return true
        end

        local function findMatches(query)
            query=tostring(query or ""):lower():gsub("^%s+",""):gsub("%s+$","")
            if query=="" then return {} end
            local matches={}
            for _,plr in ipairs(S.Players:GetPlayers()) do
                if plr~=S.player and (plr.Name:lower()==query or plr.Name:lower():find(query,1,true) or (plr.DisplayName and plr.DisplayName:lower():find(query,1,true))) then
                    table.insert(matches,plr)
                end
            end
            return matches
        end

        local function selectAllCurrentPlayers()
            for _,plr in ipairs(S.Players:GetPlayers()) do trackPlayer(plr) end
        end

        S.countTrackButton.Activated:Connect(function()
            local matches=findMatches(S.countSearchBox.Text)
            if #matches==0 then
                S.countInfoLabel.Text="لم يتم العثور على شخص يمكن تحديده"
                return
            end
            local added=0
            for _,plr in ipairs(matches) do if trackPlayer(plr) then added=added+1 end end
            S.countInfoLabel.Text="تم تحديد "..tostring(added).." شخص"
            S.countSearchBox.Text=""
        end)

        S.countAllButton.Activated:Connect(function()
            S.countAllEnabled=not S.countAllEnabled
            if S.countAllEnabled then
                S.countAllButton.Text="ALL ON"
                S.countAllButton.BackgroundColor3=Color3.fromRGB(0,200,0)
                selectAllCurrentPlayers()
                S.countInfoLabel.Text="ALL ON — سيتم تحديد كل من يدخل تلقائياً"
            else
                S.countAllButton.Text="ALL"
                S.countAllButton.BackgroundColor3=Color3.fromRGB(35,40,55)
                S.countInfoLabel.Text="ALL OFF"
            end
        end)

        S.countConnections = S.countConnections or {}
        table.insert(S.countConnections,S.Players.PlayerRemoving:Connect(function(plr)
            local key=getPlayerKey(plr)
            local data=S.countTracked[key]
            if not data then return end
            -- حماية من تكرار حدث الخروج: يُحسب Left مرة واحدة لكل خروج فعلي.
            if not data.inServer then return end
            local sessionDuration=math.min(MAX_STAY_SECONDS,math.max(0,os.clock()-(data.joinedAt or os.clock())))
            data.totalStay=math.min(MAX_STAY_SECONDS,(data.totalStay or 0)+sessionDuration)
            data.lastSessionDuration=sessionDuration
            data.inServer=false
            data.joinedAt=nil
            data.leftCount=(data.leftCount or 0)+1
            data.player=nil
            updateRow(data)
            enqueueLeaveNotification({
                userId=data.userId,
                name=data.name,
                sessionDuration=sessionDuration,
                times=data.leftCount
            })
        end))

        table.insert(S.countConnections,S.Players.PlayerAdded:Connect(function(plr)
            local key=getPlayerKey(plr)
            local data=S.countTracked[key]
            -- إذا كان محدداً يدوياً من قبل، يعود تلقائياً ويكمل نفس المؤقت.
            if data then
                data.player=plr
                data.name=plr.Name
                data.displayName=plr.DisplayName
                data.inServer=true
                data.joinedAt=os.clock()
                -- إعادة استخدام نفس الصف الموجود بدون تكراره.
                if data.row and data.row.Parent then
                    updateRow(data)
                else
                    createCountRow(data)
                end
            elseif S.countAllEnabled then
                trackPlayer(plr)
            end
        end))

        table.insert(S.countConnections,S.RunService.Heartbeat:Connect(function()
            for _,data in pairs(S.countTracked) do updateRow(data) end
        end))

        table.insert(S.countConnections,S.countLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            S.countScrollFrame.CanvasSize=UDim2.new(0,0,0,S.countLayout.AbsoluteContentSize.Y+20)
        end))
        task.defer(function()
            S.countScrollFrame.CanvasSize=UDim2.new(0,0,0,S.countLayout.AbsoluteContentSize.Y+20)
        end)
    end
end

-- ============================================================
-- ============================================================
-- SCOPE 28: REAL JSONSTORAGE RANK / COMMAND / LOG SYSTEM - FINAL
-- ============================================================
do
    local S=_G.SQR
    S.HttpService = S.HttpService or game:GetService("HttpService")
    S.SUPABASE_URL=S.SUPABASE_URL or "https://elierhbtttlzmuuhswjj.supabase.co"
    S.SUPABASE_KEY=S.SUPABASE_KEY or (S._sqrProtectedKey and S._sqrProtectedKey() or "")
    S.SUPABASE_STATE_URL=S.SUPABASE_STATE_URL or (S.SUPABASE_URL.."/rest/v1/sqr_state")
    S.JSON_POLL=10
    S.SCRIPT_USER_TIMEOUT=120
    S.COMMAND_TIMEOUT=45
    S.VIP_DAILY_LIMIT=5
    S.remoteCommandAlive=true
    S.scriptUsers={}
    S.cloudRecord={}
    S.processedCommands={}

    local function requestFn()
        if typeof(request)=="function" then return request end
        if typeof(http_request)=="function" then return http_request end
        if typeof(syn)=="table" and typeof(syn.request)=="function" then return syn.request end
        return nil
    end

    local function jsRequest(method,body)
        local req=requestFn()
        if not req then return false,nil,"request function not found" end
        local actualMethod=(method=="PUT" and "PATCH" or method)
        local url=S.SUPABASE_STATE_URL
        if actualMethod=="GET" then
            url=url.."?id=eq.1&select=data"
        else
            url=url.."?id=eq.1"
        end
        local options={
            Url=url,
            Method=actualMethod,
            Headers={
                ["apikey"]=S.SUPABASE_KEY,
                ["Authorization"]="Bearer "..S.SUPABASE_KEY,
                ["Content-Type"]="application/json",
                ["Prefer"]="return=representation"
            }
        }
        if actualMethod~="GET" then options.Body=body end
        local ok,res=pcall(function() return req(options) end)
        if not ok or not res then return false,nil,tostring(res or "request failed") end
        local code=tonumber(res.StatusCode) or 0
        local responseBody=tostring(res.Body or "")
        if code<200 or code>=300 then return false,responseBody,"HTTP "..tostring(code).." "..responseBody end
        return true,responseBody,nil
    end

    local function jsGet()
        local ok,body,err=jsRequest("GET")
        if not ok then return nil,err end
        local good,data=pcall(function() return S.HttpService:JSONDecode(body or "") end)
        if not good or type(data)~="table" or not data[1] then return {},nil end
        local record=data[1].data
        if type(record)~="table" then return {},nil end
        return record,nil
    end

    local function jsPut(record)
        if type(record)~="table" then return false,"invalid record" end
        local payload=S.HttpService:JSONEncode({data=record,updated_at=os.date("!%Y-%m-%dT%H:%M:%SZ")})
        local ok,_,err=jsRequest("PUT",payload)
        return ok,err
    end

    S.getCloudRecord=jsGet
    S.putCloudRecord=jsPut

    local function keyName(name) return tostring(name or ""):lower() end
    local function hasName(list,name)
        local q=keyName(name)
        for k,v in pairs(list or {}) do
            if tostring(k):lower()==q and v then return true end
            if type(v)=="string" and v:lower()==q then return true end
        end
        return false
    end

    local function normalizeRecord(r)
        if type(r)~="table" then r={} end
        r.activeUsers=type(r.activeUsers)=="table" and r.activeUsers or {}
        r.owners=type(r.owners)=="table" and r.owners or {}
        r.vips=type(r.vips)=="table" and r.vips or {}
        r.bannedUsers=type(r.bannedUsers)=="table" and r.bannedUsers or {}
        r.vipKicks=type(r.vipKicks)=="table" and r.vipKicks or {}
        r.logs=type(r.logs)=="table" and r.logs or {}
        r.commands=type(r.commands)=="table" and r.commands or {}
        r.removedUsers=type(r.removedUsers)=="table" and r.removedUsers or {}
        r.safeUsers=type(r.safeUsers)=="table" and r.safeUsers or {}
        r.day=tostring(r.day or os.date("!%Y-%m-%d"))
        r.owners.nanMeWellDoit=true
        r.owners.nanNeverLet=true
        if r.day~=os.date("!%Y-%m-%d") then
            r.day=os.date("!%Y-%m-%d")
            r.vipKicks={}
        end
        return r
    end

    local function activeClean(active,removedUsers)
        local out={}; local now=os.time()
        for k,v in pairs(active or {}) do
            if type(v)=="table" and now-(tonumber(v.lastSeen) or 0)<=S.SCRIPT_USER_TIMEOUT then
                local id=tostring(v.userId or k)
                if not (removedUsers and removedUsers[id]) then
                    v.userId=tonumber(v.userId) or tonumber(id) or 0
                    v.name=tostring(v.name or "Unknown")
                    v.displayName=tostring(v.displayName or v.name)
                    v.lastSeen=tonumber(v.lastSeen) or now
                    out[id]=v
                end
            end
        end
        return out
    end

    local function applyRecord(r)
        r=normalizeRecord(r)
        S.cloudRecord=r
        S.rankState={owners={},vips={},bans={},vipKicks=r.vipKicks,day=r.day}
        S.OWNER_LIST={}; S.VIP_LIST={}; S.BAN_LIST={}
        for n,v in pairs(r.owners) do
            if v then S.rankState.owners[keyName(n)]=true; table.insert(S.OWNER_LIST,tostring(n)) end
        end
        for _,n in ipairs({"nanMeWellDoit","nanNeverLet"}) do
            S.rankState.owners[keyName(n)]=true
            local found=false
            for _,x in ipairs(S.OWNER_LIST) do if keyName(x)==keyName(n) then found=true break end end
            if not found then table.insert(S.OWNER_LIST,n) end
        end
        for n,v in pairs(r.vips) do
            if v and not hasName(r.owners,n) then
                S.rankState.vips[keyName(n)]=true; table.insert(S.VIP_LIST,tostring(n))
            end
        end
        for n,v in pairs(r.bannedUsers) do
            if v then S.rankState.bans[keyName(n)]=true; table.insert(S.BAN_LIST,tostring(n)) end
        end
        S.isOwner=hasName(r.owners,S.player.Name) or keyName(S.player.Name)=="nanmewelldoit" or keyName(S.player.Name)=="nanneverlet"
        S.isVIP=(not S.isOwner) and hasName(r.vips,S.player.Name)
        S.isBanned=S.rankState.bans[keyName(S.player.Name)]==true
        S.scriptUsers=r.activeUsers
        return r
    end
    S.applyCloudRecord=applyRecord

    local initial=jsGet()
    if initial then applyRecord(initial) else
        applyRecord({activeUsers={},owners={nanMeWellDoit=true,nanNeverLet=true},vips={},bannedUsers={},safeUsers={},vipKicks={},logs={},day=os.date("!%Y-%m-%d")})
    end

    function S.isPlayerOwner(plr)
        return plr and (hasName((S.cloudRecord or {}).owners,plr.Name) or keyName(plr.Name)=="nanmewelldoit" or keyName(plr.Name)=="nanneverlet") or false
    end
    function S.isPlayerVIP(plr)
        return plr and not S.isPlayerOwner(plr) and hasName(S.cloudRecord.vips,plr.Name) or false
    end
    function S.isPlayerBanned(plr)
        return plr and hasName(S.cloudRecord.bannedUsers,plr.Name) or false
    end
    function S.isPlayerSafe(plr)
        return plr and hasName(S.cloudRecord.safeUsers,plr.Name) or false
    end
    function S.getPlayerRank(plr)
        if S.isPlayerOwner(plr) then return "Owner" end
        if S.isPlayerVIP(plr) then return "VIP" end
        return "Free"
    end
    function S.canTargetPlayer(plr)
        if not plr or plr==S.player then return false end
        if S.isPlayerSafe(plr) then return false end
        local targetRank=S.getPlayerRank(plr)
        if targetRank=="Owner" then return false end
        if S.isVIP and targetRank=="VIP" then return false end
        return true
    end

    function S.getActiveScriptUsers()
        local out={}
        for _,info in pairs(S.scriptUsers or {}) do
            if type(info)=="table" then
                local copy={}
                for k,v in pairs(info) do copy[k]=v end
                copy.userId=tonumber(copy.userId)
                copy.name=tostring(copy.name or "Unknown")
                copy.displayName=tostring(copy.displayName or copy.name)
                copy.rank=S.getPlayerRankByName and S.getPlayerRankByName(copy.name) or (hasName(S.cloudRecord.owners,copy.name) and "Owner" or (hasName(S.cloudRecord.vips,copy.name) and "VIP" or "Free"))
                table.insert(out,copy)
            end
        end
        table.sort(out,function(a,b) return keyName(a.name)<keyName(b.name) end)
        return out
    end

    function S.getPlayerRankByName(name)
        if hasName(S.cloudRecord.owners,name) then return "Owner" end
        if hasName(S.cloudRecord.vips,name) then return "VIP" end
        return "Free"
    end

    function S.findActiveScriptUser(name,active)
        local q=keyName(name)
        for _,info in pairs(active or S.scriptUsers or {}) do
            if type(info)=="table" and (keyName(info.name)==q or keyName(info.displayName)==q) then return info end
        end
    end

    local function addLog(record,action,target,reason,extra)
        record.logs=record.logs or {}
        table.insert(record.logs,1,{time=os.time(),issuer=S.player.Name,issuerUserId=S.player.UserId,issuerRank=S.isOwner and "Owner" or "VIP",action=tostring(action or ""),target=tostring(target or ""),reason=tostring(reason or ""),extra=tostring(extra or "")})
        while #record.logs>300 do table.remove(record.logs) end
    end
    S.addAdminLog=addLog

    local function newId()
        return tostring(os.time()).."-"..tostring(math.random(100000,999999)).."-"..tostring(S.player.UserId).."-"..tostring(math.random(1000,9999))
    end

    local function findUser(record,text)
        local q=keyName(tostring(text or ""):gsub("^%s+",""):gsub("%s+$",""))
        if q=="" then return nil end
        for _,info in pairs(record.activeUsers or {}) do
            if type(info)=="table" and (keyName(info.name)==q or keyName(info.displayName)==q) then return info end
        end
        for _,listName in ipairs({"owners","vips","bannedUsers"}) do
            for n,on in pairs(record[listName] or {}) do
                if on and keyName(n)==q then return {name=tostring(n),userId=0} end
            end
        end
    end

    local function issuerAllowed(command)
        if type(command)~="table" then return false end
        local issuer=tostring(command.issuer or "")
        local rank=tostring(command.issuerRank or "")
        if rank=="Owner" then return hasName(S.cloudRecord.owners,issuer) end
        if rank=="VIP" then return hasName(S.cloudRecord.vips,issuer) end
        return false
    end

    local function executeRemote(command)
        if type(command)~="table" or tonumber(command.targetUserId)~=S.player.UserId or not issuerAllowed(command) then return end
        local cmd=keyName(command.command)
        local args=type(command.args)=="table" and command.args or {}
        if cmd=="kick" then
            S.player:Kick(tostring(args.reason or "Kicked by SQR"))
        elseif cmd=="ban" then
            S.player:Kick(tostring(args.reason or "Banned by SQR"))
        elseif cmd=="freeze" then
            local root=S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart")
            if root then root.Anchored=true; task.delay(math.clamp(tonumber(args.seconds) or 5,1,60),function() if root and root.Parent then root.Anchored=false end end) end
        elseif cmd=="unfreeze" then
            local root=S.player.Character and S.player.Character:FindFirstChild("HumanoidRootPart")
            if root then root.Anchored=false end
        elseif cmd=="lag" and S.ApplyLag then
            S.ApplyLag(S.player,args.seconds)
        elseif cmd=="respawn" then
            pcall(function() S.player:LoadCharacter() end)
        elseif cmd=="kill" then
            local hum=S.player.Character and S.player.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.Health=0 end
        end
    end

    local function publishCommand(targetInfo,cmd,reason)
        local record=normalizeRecord(jsGet() or S.cloudRecord or {})
        local rank=S.isOwner and "Owner" or (S.isVIP and "VIP" or "Free")
        if rank=="Free" then return false,"No permission" end
        local targetName=tostring(targetInfo.name or "")
        local targetId=tonumber(targetInfo.userId) or 0
        if targetName=="" or targetId<=0 then return false,"Target is not available" end
        local targetRank=hasName(record.owners,targetName) and "Owner" or (hasName(record.vips,targetName) and "VIP" or "Free")
        if hasName(record.safeUsers,targetName) then return false,"Target is Safe" end
        if targetRank=="Owner" then return false,"Cannot target Owner" end
        if rank=="VIP" and targetRank~="Free" then return false,"VIP can target Free only" end
        if rank=="VIP" and keyName(cmd)=="kick" then
            if record.day~=os.date("!%Y-%m-%d") then record.day=os.date("!%Y-%m-%d"); record.vipKicks={} end
            local used=tonumber(record.vipKicks[S.player.Name]) or 0
            if used>=S.VIP_DAILY_LIMIT then return false,"VIP Kick limit: 5/5" end
            record.vipKicks[S.player.Name]=used+1
        end
        local now=os.time()
        record.commands=type(record.commands)=="table" and record.commands or {}
        local command={id=newId(),createdAt=now,expiresAt=now+S.COMMAND_TIMEOUT,issuer=S.player.Name,issuerUserId=S.player.UserId,issuerRank=rank,target=targetName,targetUserId=targetId,command=keyName(cmd),args={reason=tostring(reason or "")}}
        table.insert(record.commands,command)
        while #record.commands>80 do table.remove(record.commands,1) end
        record.command=command
        record.removedUsers=record.removedUsers or {}
        record.removedUsers[tostring(targetId)]=now
        record.activeUsers=record.activeUsers or {}
        record.activeUsers[tostring(targetId)]=nil
        addLog(record,cmd,targetName,reason)
        local ok,err=jsPut(record)
        if not ok then return false,err end
        return true,nil
    end
    S.publishRemoteCommand=publishCommand

    function S.setVIPByName(name,enabled)
        local record=normalizeRecord(jsGet() or {})
        local info=findUser(record,name)
        if not info then return false,"User not found" end
        local realName=tostring(info.name)
        if hasName(record.owners,realName) then return false,"Owner cannot be VIP" end
        if enabled then
            record.vips[realName]=true; record.bannedUsers[realName]=nil
            addLog(record,"VIP",realName,"Promoted to VIP")
        else
            record.vips[realName]=nil
            addLog(record,"UnVIP",realName,"VIP removed")
        end
        local ok,err=jsPut(record)
        if ok then applyRecord(record) end
        return ok,err
    end

    function S.setBanByName(name,enabled,reason)
        local record=normalizeRecord(jsGet() or {})
        local info=findUser(record,name)
        if not info then return false,"User not found" end
        local realName=tostring(info.name)
        if hasName(record.owners,realName) then return false,"Cannot ban Owner" end
        if enabled then
            record.bannedUsers[realName]=true
            addLog(record,"Ban",realName,reason)
            local targetId=tonumber(info.userId) or 0
            if targetId>0 then
                local now=os.time()
                record.commands=type(record.commands)=="table" and record.commands or {}
                local command={id=newId(),createdAt=now,expiresAt=now+S.COMMAND_TIMEOUT,issuer=S.player.Name,issuerUserId=S.player.UserId,issuerRank="Owner",target=realName,targetUserId=targetId,command="ban",args={reason=tostring(reason or "Banned by SQR")}}
                table.insert(record.commands,command)
                while #record.commands>80 do table.remove(record.commands,1) end
                record.command=command
                record.removedUsers=record.removedUsers or {}
                record.removedUsers[tostring(targetId)]=now
                record.activeUsers=record.activeUsers or {}
                record.activeUsers[tostring(targetId)]=nil
            end
        else
            record.bannedUsers[realName]=nil
            addLog(record,"UnBan",realName,reason or "Ban removed")
        end
        local ok,err=jsPut(record)
        if ok then applyRecord(record) end
        return ok,err
    end

    function S.setOwnerByName(name,enabled)
        local record=normalizeRecord(jsGet() or {})
        local info=findUser(record,name)
        if not info then return false,"User not found" end
        local realName=tostring(info.name)
        local q=keyName(realName)
        local issuerKey=keyName(S.player.Name)
        local originalOwner=issuerKey=="nanmewelldoit" or issuerKey=="nanneverlet"
        if not originalOwner then return false,"Only original Owners can manage Owner rank" end
        if not enabled and (q=="nanmewelldoit" or q=="nanneverlet") then return false,"Original Owner cannot be removed" end
        if enabled then
            record.owners[realName]=true; record.vips[realName]=nil
            addLog(record,"Owner",realName,"Promoted to Owner")
        else
            record.owners[realName]=nil; record.vips[realName]=nil
            addLog(record,"UnOwner",realName,"Owner removed")
        end
        local ok,err=jsPut(record)
        if ok then applyRecord(record) end
        return ok,err
    end

    function S.setSafeByName(name,enabled)
        local record=normalizeRecord(jsGet() or {})
        local info=findUser(record,name)
        if not info then return false,"User not found" end
        local realName=tostring(info.name)
        if keyName(realName)==keyName(S.player.Name) then return false,"Cannot protect yourself" end
        if enabled then record.safeUsers[realName]=true else record.safeUsers[realName]=nil end
        addLog(record,enabled and "Safe" or "UnSafe",realName,enabled and "Target protection enabled" or "Target protection removed")
        local ok,err=jsPut(record)
        if ok then applyRecord(record) end
        return ok,err
    end

    function S.resetVIPKicks(name)
        local record=normalizeRecord(jsGet() or {})
        local realName=tostring(name or "")
        record.vipKicks[realName]=0
        addLog(record,"Reset Kick",realName,"Daily Kick counter reset")
        local ok,err=jsPut(record)
        if ok then applyRecord(record) end
        return ok,err
    end

    function S.ShowRankMessage(message)
        pcall(function() game:GetService("StarterGui"):SetCore("SendNotification",{Title="SQR",Text=tostring(message),Duration=3}) end)
    end

    -- Unified cloud worker: one read cycle handles commands, presence and ranks.
    -- This replaces three independent 10-second polling loops with one request cycle.
    S.cloudWorkerLoop=task.spawn(function()
        while S.remoteCommandAlive do
            local record=jsGet()
            if record then
                record=normalizeRecord(record)
                applyRecord(record)
                local now=os.time()
                local commands=type(record.commands)=="table" and record.commands or {}
                if #commands==0 and type(record.command)=="table" then commands={record.command} end
                for _,cmd in ipairs(commands) do
                    local id=tostring(cmd.id or "")
                    local created=tonumber(cmd.createdAt) or 0
                    local expires=tonumber(cmd.expiresAt) or (created+S.COMMAND_TIMEOUT)
                    if id~="" and not S.processedCommands[id] and created>=S.scriptStartTime and now<=expires then
                        S.processedCommands[id]=now
                        pcall(function() executeRemote(cmd) end)
                    end
                end
                if S.isBanned then
                    S.player:Kick("Reason : Banned from using this script")
                    break
                end

                record.activeUsers=activeClean(record.activeUsers,record.removedUsers)
                record.removedUsers=record.removedUsers or {}
                record.removedUsers[tostring(S.player.UserId)]=nil
                record.activeUsers[tostring(S.player.UserId)]={userId=S.player.UserId,name=S.player.Name,displayName=S.player.DisplayName,lastSeen=now}
                applyRecord(record)
                jsPut(record)
            end
            task.wait(S.JSON_POLL)
        end
    end)
end

-- ============================================================
-- SCOPE 28B: FINAL VIP / OWNER UI
-- ============================================================
do
    local S=_G.SQR
    local function section(name)
        local i=table.find(S.sections,name)
        return i and S.sectionFrames[i] or nil
    end
    local function clear(parent)
        for _,o in ipairs(parent:GetChildren()) do
            if not o:IsA("UIListLayout") and not o:IsA("UIPadding") then pcall(function() o:Destroy() end) end
        end
    end
    local function label(parent,text,y,h,size)
        local l=Instance.new("TextLabel")
        l.Size=UDim2.new(1,-20,0,h or 24); l.Position=UDim2.fromOffset(10,y); l.BackgroundTransparency=1
        l.Text=tostring(text); l.TextColor3=Color3.fromRGB(225,230,240); l.TextSize=size or 13; l.Font=Enum.Font.GothamBold
        l.TextXAlignment=Enum.TextXAlignment.Left; l.Parent=parent; return l
    end
    local function box(parent,placeholder,y)
        local b=Instance.new("TextBox")
        b.Size=UDim2.new(1,-20,0,36); b.Position=UDim2.fromOffset(10,y); b.BackgroundColor3=Color3.fromRGB(38,42,55); b.BorderSizePixel=0
        b.TextColor3=Color3.new(1,1,1); b.PlaceholderText=placeholder; b.Text=""; b.ClearTextOnFocus=false; b.TextSize=13; b.Font=Enum.Font.Gotham; b.Parent=parent
        Instance.new("UICorner",b).CornerRadius=UDim.new(0,9); return b
    end
    local function mini(parent,text,x,y,w,bg)
        local b=Instance.new("TextButton")
        b.Size=UDim2.fromOffset(w or 70,34); b.Position=UDim2.fromOffset(x,y); b.BackgroundColor3=bg or Color3.fromRGB(45,50,65)
        b.BorderSizePixel=0; b.Text=text; b.TextColor3=Color3.new(1,1,1); b.TextSize=11; b.Font=Enum.Font.GothamBold; b.Parent=parent
        Instance.new("UICorner",b).CornerRadius=UDim.new(0,8); S.addButtonFeedback(b); return b
    end

    local sfVip=section("VIP")
    if sfVip then
        sfVip.Visible = S.isVIP or S.isOwner
        sfVip.Active = true
        sfVip.ZIndex = 20
        sfVip.ClipsDescendants=true
        S.vipStatus=label(sfVip,"VIP • Kick: 0/5",8,24,15)
        S.vipUsersFrame=Instance.new("ScrollingFrame"); S.vipUsersFrame.Size=UDim2.new(1,-20,1,-45); S.vipUsersFrame.Position=UDim2.fromOffset(10,38); S.vipUsersFrame.BackgroundTransparency=0.15; S.vipUsersFrame.BackgroundColor3=Color3.fromRGB(18,21,30); S.vipUsersFrame.BorderSizePixel=0; S.vipUsersFrame.ScrollBarThickness=5; S.vipUsersFrame.Parent=sfVip
        Instance.new("UICorner",S.vipUsersFrame).CornerRadius=UDim.new(0,10)
        S.vipLayout=Instance.new("UIListLayout",S.vipUsersFrame); S.vipLayout.Padding=UDim.new(0,6)
        S.vipItems={}

        function S.vipRefresh()
            local active=S.getActiveScriptUsers()
            if type(active)~="table" then active={} end
            local present={}
            local sigParts={}
            for _,info in ipairs(active) do
                if info.userId and info.userId~=S.player.UserId then
                    present[tostring(info.userId)]=true
                    S.vipKnownUsers[tostring(info.userId)]=info
                    if info.rank=="Free" then
                        sigParts[#sigParts+1]=tostring(info.userId)..":"..tostring(info.name)..":"..tostring(info.rank)
                    end
                end
            end
            for uid in pairs(S.vipKnownUsers) do
                if not present[uid] and not (S.cloudRecord.activeUsers or {})[uid] then
                    S.vipKnownUsers[uid]=nil
                end
            end
            table.sort(sigParts)
            local sig=table.concat(sigParts,"|")
            local used=tonumber((S.cloudRecord.vipKicks or {})[S.player.Name]) or 0
            S.vipStatus.Text="VIP • Kick: 5/"..math.min(used,5)
            if sig==S.vipUiSignature and S.vipUsersFrame:FindFirstChildWhichIsA("Frame") then return end
            S.vipUiSignature=sig
            clear(S.vipUsersFrame); S.vipItems={}
            local count=0
            for _,info in pairs(S.vipKnownUsers) do
                if info.userId and info.userId~=S.player.UserId and info.rank=="Free" then
                    count=count+1
                    local row=Instance.new("Frame"); row.Size=UDim2.new(1,-8,0,72); row.BackgroundColor3=Color3.fromRGB(35,39,52); row.Parent=S.vipUsersFrame
                    Instance.new("UICorner",row).CornerRadius=UDim.new(0,9)
                    local av=Instance.new("ImageLabel"); av.Size=UDim2.fromOffset(54,54); av.Position=UDim2.fromOffset(7,9); av.BackgroundTransparency=1; av.ScaleType=Enum.ScaleType.Fit; av.Parent=row
                    av.Image="rbxthumb://type=AvatarHeadShot&id="..tostring(info.userId).."&w=150&h=150"
                    task.spawn(function() pcall(function() local img=S.Players:GetUserThumbnailAsync(tonumber(info.userId),Enum.ThumbnailType.AvatarBust,Enum.ThumbnailSize.Size150x150); if av and av.Parent and img and img~="" then av.Image=img end end) end)
                    local tx=Instance.new("TextLabel"); tx.Size=UDim2.new(1,-155,0,25); tx.Position=UDim2.fromOffset(70,9); tx.BackgroundTransparency=1; tx.Text="@"..info.name; tx.TextColor3=Color3.new(1,1,1); tx.TextSize=13; tx.Font=Enum.Font.GothamBold; tx.TextXAlignment=Enum.TextXAlignment.Left; tx.Parent=row
                    local uid=Instance.new("TextLabel"); uid.Size=UDim2.new(1,-155,0,20); uid.Position=UDim2.fromOffset(70,34); uid.BackgroundTransparency=1; uid.Text="UserId: "..tostring(info.userId); uid.TextColor3=Color3.fromRGB(165,170,180); uid.TextSize=10; uid.Font=Enum.Font.Gotham; uid.TextXAlignment=Enum.TextXAlignment.Left; uid.Parent=row
                    local kick=mini(row,"KICK",0,19,68,Color3.fromRGB(180,55,55)); kick.Position=UDim2.new(1,-76,0,19)
                    kick.Activated:Connect(function()
                        if not S.isOwner then
                            local n=tonumber((S.cloudRecord.vipKicks or {})[S.player.Name]) or 0
                            if n>=5 then S.ShowRankMessage("VIP Kick limit: 5/5"); return end
                        end
                        local ok,err=S.publishRemoteCommand({name=info.name,userId=info.userId},"kick","")
                        if ok then S.vipKnownUsers[tostring(info.userId)]=nil; S.ownerKnownUsers[tostring(info.userId)]=nil end
                        S.ShowRankMessage(ok and ("Kick sent to @"..info.name) or tostring(err or "Kick failed"))
                    end)
                end
            end
            if count==0 then label(S.vipUsersFrame,"لا يوجد Free مستخدم للسكربت حالياً",8,30,12) end
            S.vipUsersFrame.CanvasSize=UDim2.fromOffset(0,math.max(45,count*78+12))
        end
    end

    local sfOwner=section("Owner")
    if sfOwner then
        sfOwner.Visible = S.isOwner
        sfOwner.Active = true
        sfOwner.ZIndex = 20
        sfOwner.ClipsDescendants=true
        S.ownerLogsButton=mini(sfOwner,"LOGS",0,8,72,Color3.fromRGB(55,90,145)); S.ownerLogsButton.Position=UDim2.new(1,-82,0,8)
        S.ownerBannedButton=mini(sfOwner,"BANNED",0,8,82,Color3.fromRGB(125,65,65)); S.ownerBannedButton.Position=UDim2.new(1,-174,0,8)
        S.ownerVipListButton=mini(sfOwner,"VIP",0,8,52,Color3.fromRGB(45,140,75)); S.ownerVipListButton.Position=UDim2.new(1,-230,0,8)
        S.ownerOwnerListButton=mini(sfOwner,"OWNER",0,8,64,Color3.fromRGB(45,140,75)); S.ownerOwnerListButton.Position=UDim2.new(1,-300,0,8)
        S.ownerManageStatus=label(sfOwner,"",42,28,11); S.ownerManageStatus.Size=UDim2.new(1,-20,0,28)
        S.ownerPlayersLabel=label(sfOwner,"PLAYERS",72,22,14)
        S.ownerUserCount=label(sfOwner,"Script Users: 0",96,22,12)
        S.ownerPlayerFrame=Instance.new("ScrollingFrame"); S.ownerPlayerFrame.Size=UDim2.new(1,-20,0,190); S.ownerPlayerFrame.Position=UDim2.fromOffset(10,122); S.ownerPlayerFrame.BackgroundColor3=Color3.fromRGB(18,21,30); S.ownerPlayerFrame.BackgroundTransparency=0.12; S.ownerPlayerFrame.BorderSizePixel=0; S.ownerPlayerFrame.ScrollBarThickness=5; S.ownerPlayerFrame.Parent=sfOwner
        Instance.new("UICorner",S.ownerPlayerFrame).CornerRadius=UDim.new(0,10)
        S.ownerPlayerLayout=Instance.new("UIListLayout",S.ownerPlayerFrame); S.ownerPlayerLayout.Padding=UDim.new(0,6)
        S.ownerItems={}
        S.ownerUiSignature=""
        S.vipUiSignature=""
        S.ownerKnownUsers={}
        S.vipKnownUsers={}
        S.ownerKickCounter=label(sfOwner,"VIP KICK COUNTERS",322,22,13)
        S.ownerResetFrame=Instance.new("ScrollingFrame"); S.ownerResetFrame.Size=UDim2.new(1,-20,0,70); S.ownerResetFrame.Position=UDim2.fromOffset(10,348); S.ownerResetFrame.BackgroundColor3=Color3.fromRGB(18,21,30); S.ownerResetFrame.BorderSizePixel=0; S.ownerResetFrame.ScrollBarThickness=5; S.ownerResetFrame.Parent=sfOwner
        Instance.new("UICorner",S.ownerResetFrame).CornerRadius=UDim.new(0,9)
        S.ownerResetLayout=Instance.new("UIListLayout",S.ownerResetFrame); S.ownerResetLayout.Padding=UDim.new(0,4)
        S.ownerResetItems={}

        local logsFrame=nil
        S.ownerLogsButton.Activated:Connect(function()
            if logsFrame then logsFrame:Destroy(); logsFrame=nil; return end
            logsFrame=Instance.new("Frame"); logsFrame.Size=UDim2.new(1,-10,1,-10); logsFrame.Position=UDim2.fromOffset(5,5); logsFrame.BackgroundColor3=Color3.fromRGB(18,21,30); logsFrame.ZIndex=60; logsFrame.Parent=sfOwner
            Instance.new("UICorner",logsFrame).CornerRadius=UDim.new(0,10)
            local close=mini(logsFrame,"X",0,5,36,Color3.fromRGB(180,55,55)); close.Position=UDim2.new(1,-43,0,5); close.ZIndex=62
            close.Activated:Connect(function() logsFrame:Destroy(); logsFrame=nil end)
            local title=label(logsFrame,"SQR LOGS",10,28,17); title.ZIndex=62
            local scroll=Instance.new("ScrollingFrame"); scroll.Size=UDim2.new(1,-20,1,-50); scroll.Position=UDim2.fromOffset(10,45); scroll.BackgroundTransparency=1; scroll.BorderSizePixel=0; scroll.ScrollBarThickness=5; scroll.Active=true; scroll.ScrollingEnabled=true; scroll.AutomaticCanvasSize=Enum.AutomaticSize.Y; scroll.ZIndex=61; scroll.Parent=logsFrame
            local lay=Instance.new("UIListLayout",scroll); lay.Padding=UDim.new(0,5)
            local function refresh()
                for _,o in ipairs(scroll:GetChildren()) do if not o:IsA("UIListLayout") then o:Destroy() end end
                for _,e in ipairs(S.cloudRecord.logs or {}) do
                    local l=Instance.new("TextLabel"); l.Size=UDim2.new(1,-8,0,58); l.BackgroundColor3=Color3.fromRGB(34,38,50); l.TextColor3=Color3.fromRGB(235,235,240); l.TextSize=11; l.Font=Enum.Font.Gotham; l.TextXAlignment=Enum.TextXAlignment.Left; l.TextYAlignment=Enum.TextYAlignment.Center; l.TextWrapped=true; l.ZIndex=62; l.Text=tostring(e.issuer).." : "..tostring(e.action).." User"; l.Parent=scroll
                    Instance.new("UICorner",l).CornerRadius=UDim.new(0,7)
                end
                scroll.CanvasSize=UDim2.fromOffset(0,math.max(lay.AbsoluteContentSize.Y+10, scroll.AbsoluteWindowSize.Y+1))
            end
            refresh()
            task.spawn(function() while logsFrame and logsFrame.Parent do task.wait(2); if logsFrame and logsFrame.Parent then refresh() end end end)
        end)

        local rankListFrame=nil
        local function openRankList(which)
            if rankListFrame then rankListFrame:Destroy(); rankListFrame=nil end
            rankListFrame=Instance.new("Frame"); rankListFrame.Size=UDim2.new(1,-10,1,-10); rankListFrame.Position=UDim2.fromOffset(5,5); rankListFrame.BackgroundColor3=Color3.fromRGB(18,21,30); rankListFrame.ZIndex=80; rankListFrame.Parent=sfOwner
            Instance.new("UICorner",rankListFrame).CornerRadius=UDim.new(0,10)
            local close=mini(rankListFrame,"X",0,5,36,Color3.fromRGB(180,55,55)); close.Position=UDim2.new(1,-43,0,5); close.ZIndex=82
            close.Activated:Connect(function() if rankListFrame then rankListFrame:Destroy(); rankListFrame=nil end end)
            local title=label(rankListFrame,which=="VIP" and "VIP USERS" or "OWNER USERS",10,28,17); title.ZIndex=82
            local scroll=Instance.new("ScrollingFrame"); scroll.Size=UDim2.new(1,-20,1,-50); scroll.Position=UDim2.fromOffset(10,45); scroll.BackgroundTransparency=1; scroll.BorderSizePixel=0; scroll.ScrollBarThickness=5; scroll.Active=true; scroll.ScrollingEnabled=true; scroll.AutomaticCanvasSize=Enum.AutomaticSize.Y; scroll.ZIndex=81; scroll.Parent=rankListFrame
            local lay=Instance.new("UIListLayout",scroll); lay.Padding=UDim.new(0,5)
            local source=which=="VIP" and (S.cloudRecord.vips or {}) or (S.cloudRecord.owners or {})
            local count=0
            for n,on in pairs(source) do
                if on then
                    count=count+1
                    local row=Instance.new("Frame"); row.Size=UDim2.new(1,-8,0,44); row.BackgroundColor3=Color3.fromRGB(34,38,50); row.ZIndex=82; row.Parent=scroll; Instance.new("UICorner",row).CornerRadius=UDim.new(0,7)
                    local l=Instance.new("TextLabel"); l.Size=UDim2.new(1,-100,1,0); l.Position=UDim2.fromOffset(8,0); l.BackgroundTransparency=1; l.Text="@"..tostring(n).." • "..which; l.TextColor3=Color3.new(1,1,1); l.TextSize=12; l.Font=Enum.Font.GothamBold; l.TextXAlignment=Enum.TextXAlignment.Left; l.ZIndex=83; l.Parent=row
                    local removable=(which=="VIP" or (keyName(n)~="nanmewelldoit" and keyName(n)~="nanneverlet"))
                    if removable then
                        local un=mini(row,which=="VIP" and "UnVIP" or "UnOwner",0,5,78,Color3.fromRGB(190,55,55)); un.Position=UDim2.new(1,-84,0,5); un.ZIndex=84
                        un.Activated:Connect(function()
                            local ok,err
                            if which=="VIP" then ok,err=S.setVIPByName(n,false) else ok,err=S.setOwnerByName(n,false) end
                            S.ShowRankMessage(ok and ((which=="VIP" and "UnVIP: @" or "UnOwner: @")..tostring(n)) or tostring(err or "Failed"))
                            if ok then openRankList(which); if S.ownerRefresh then S.ownerRefresh() end end
                        end)
                    end
                end
            end
            if count==0 then label(scroll,which=="VIP" and "لا يوجد VIP" or "لا يوجد Owner",8,30,12) end
            scroll.CanvasSize=UDim2.fromOffset(0,math.max(48,count*49+8))
        end
        S.ownerVipListButton.Activated:Connect(function() openRankList("VIP") end)
        S.ownerOwnerListButton.Activated:Connect(function() openRankList("OWNER") end)

        local bannedFrame=nil
        S.ownerBannedButton.Activated:Connect(function()
            if bannedFrame then bannedFrame:Destroy(); bannedFrame=nil; return end
            bannedFrame=Instance.new("Frame"); bannedFrame.Size=UDim2.new(1,-10,1,-10); bannedFrame.Position=UDim2.fromOffset(5,5); bannedFrame.BackgroundColor3=Color3.fromRGB(18,21,30); bannedFrame.ZIndex=70; bannedFrame.Parent=sfOwner
            Instance.new("UICorner",bannedFrame).CornerRadius=UDim.new(0,10)
            local close=mini(bannedFrame,"X",0,5,36,Color3.fromRGB(180,55,55)); close.Position=UDim2.new(1,-43,0,5); close.ZIndex=72
            close.Activated:Connect(function() bannedFrame:Destroy(); bannedFrame=nil end)
            local title=label(bannedFrame,"BANNED USERS",10,28,17); title.ZIndex=72
            local scroll=Instance.new("ScrollingFrame"); scroll.Size=UDim2.new(1,-20,1,-50); scroll.Position=UDim2.fromOffset(10,45); scroll.BackgroundTransparency=1; scroll.BorderSizePixel=0; scroll.ScrollBarThickness=5; scroll.Active=true; scroll.ScrollingEnabled=true; scroll.AutomaticCanvasSize=Enum.AutomaticSize.Y; scroll.ZIndex=71; scroll.Parent=bannedFrame
            local lay=Instance.new("UIListLayout",scroll); lay.Padding=UDim.new(0,5)
            local function refreshBanned()
                for _,o in ipairs(scroll:GetChildren()) do if not o:IsA("UIListLayout") then o:Destroy() end end
                local count=0
                for n,on in pairs(S.cloudRecord.bannedUsers or {}) do
                    if on then
                        count=count+1
                        local row=Instance.new("Frame"); row.Size=UDim2.new(1,-8,0,42); row.BackgroundColor3=Color3.fromRGB(34,38,50); row.ZIndex=72; row.Parent=scroll
                        local l=Instance.new("TextLabel"); l.Size=UDim2.new(1,-90,1,0); l.Position=UDim2.fromOffset(8,0); l.BackgroundTransparency=1; l.Text="@"..tostring(n); l.TextColor3=Color3.new(1,1,1); l.TextSize=12; l.Font=Enum.Font.GothamBold; l.TextXAlignment=Enum.TextXAlignment.Left; l.ZIndex=73; l.Parent=row
                        local un=mini(row,"UnBan",0,5,68,Color3.fromRGB(55,130,85)); un.Position=UDim2.new(1,-75,0,5); un.ZIndex=74
                        un.Activated:Connect(function()
                            local ok,err=S.setBanByName(n,false,"UnBan from Banned list")
                            S.ShowRankMessage(ok and ("UnBan: @"..tostring(n)) or tostring(err or "UnBan failed"))
                            if ok then refreshBanned(); if S.ownerRefresh then S.ownerRefresh() end end
                        end)
                        Instance.new("UICorner",row).CornerRadius=UDim.new(0,7)
                    end
                end
                if count==0 then label(scroll,"لا يوجد أشخاص مبندين",8,30,12) end
                scroll.CanvasSize=UDim2.fromOffset(0,math.max(48,count*47+8))
            end
            refreshBanned()
        end)

        local function avatarFor(id)
            id=tonumber(id)
            if not id or id<=0 then return "" end
            return "rbxthumb://type=AvatarHeadShot&id="..tostring(id).."&w=150&h=150"
        end

        local function buildPlayerRow(info)
            local row=Instance.new("Frame"); row.Size=UDim2.new(1,-8,0,118); row.BackgroundColor3=Color3.fromRGB(35,39,52); row.Parent=S.ownerPlayerFrame
            Instance.new("UICorner",row).CornerRadius=UDim.new(0,9)
            local av=Instance.new("ImageLabel"); av.Size=UDim2.fromOffset(54,54); av.Position=UDim2.fromOffset(7,8); av.BackgroundTransparency=1; av.ScaleType=Enum.ScaleType.Fit; av.Image=avatarFor(info.userId); av.Parent=row
            task.spawn(function() pcall(function() local img=S.Players:GetUserThumbnailAsync(tonumber(info.userId),Enum.ThumbnailType.AvatarBust,Enum.ThumbnailSize.Size150x150); if av and av.Parent and img and img~="" then av.Image=img end end) end)
            local name=Instance.new("TextLabel"); name.Size=UDim2.new(1,-75,0,20); name.Position=UDim2.fromOffset(68,8); name.BackgroundTransparency=1; name.Text="@"..tostring(info.name); name.TextColor3=Color3.new(1,1,1); name.TextSize=13; name.Font=Enum.Font.GothamBold; name.TextXAlignment=Enum.TextXAlignment.Left; name.Parent=row
            local rank=Instance.new("TextLabel"); rank.Size=UDim2.new(1,-75,0,18); rank.Position=UDim2.fromOffset(68,31); rank.BackgroundTransparency=1; rank.Text="Rank: "..tostring(info.rank); rank.TextColor3=Color3.fromRGB(175,180,190); rank.TextSize=11; rank.Font=Enum.Font.Gotham; rank.TextXAlignment=Enum.TextXAlignment.Left; rank.Parent=row
            local reason=Instance.new("TextBox"); reason.Size=UDim2.fromOffset(100,27); reason.Position=UDim2.new(1,-330,0,58); reason.BackgroundColor3=Color3.fromRGB(28,32,43); reason.TextColor3=Color3.new(1,1,1); reason.PlaceholderText="Reason"; reason.Text=""; reason.TextSize=10; reason.Font=Enum.Font.Gotham; reason.ClearTextOnFocus=false; reason.Parent=row; Instance.new("UICorner",reason).CornerRadius=UDim.new(0,6)
            local kick=mini(row,"KICK",0,64,48,Color3.fromRGB(180,55,55)); kick.Position=UDim2.new(1,-313,0,75)
            local ban=mini(row,"BAN",0,64,48,Color3.fromRGB(150,75,75)); ban.Position=UDim2.new(1,-260,0,75)
            local rankBtn=mini(row,info.rank=="VIP" and "UnVIP" or "VIP",0,64,62,info.rank=="VIP" and Color3.fromRGB(190,55,55) or Color3.fromRGB(45,140,75)); rankBtn.Position=UDim2.new(1,-207,0,75)
            local safeBtn=mini(row,(S.isPlayerSafe and S.isPlayerSafe({Name=info.name}) and "UnSafe" or "Safe"),0,64,58,Color3.fromRGB(70,100,145)); safeBtn.Position=UDim2.new(1,-140,0,75)
            local ownerBtn=mini(row,info.rank=="Owner" and "UnOwner" or "Owner",0,43,72,info.rank=="Owner" and Color3.fromRGB(190,55,55) or Color3.fromRGB(45,140,75)); ownerBtn.Position=UDim2.new(1,-80,0,43)
            kick.Activated:Connect(function()
                local targetName=info.name
                local targetId=info.userId
                local targetReason=reason.Text
                kick.Active=false
                kick.Text="..."
                task.spawn(function()
                    local ok,err=S.publishRemoteCommand({name=targetName,userId=targetId},"kick",targetReason)
                    pcall(function()
                        if kick and kick.Parent then
                            kick.Active=true
                            kick.Text="KICK"
                        end
                    end)
                    if ok then
                        S.vipKnownUsers[tostring(targetId)]=nil
                        S.ownerKnownUsers[tostring(targetId)]=nil
                    end
                    S.ShowRankMessage(ok and ("Kick sent to @"..targetName) or tostring(err or "Kick failed"))
                end)
            end)
            ban.Activated:Connect(function()
                local ok,err=S.setBanByName(info.name,true,reason.Text)
                S.ShowRankMessage(ok and ("Ban sent to @"..info.name) or tostring(err or "Ban failed"))
            end)
            rankBtn.Activated:Connect(function()
                if info.rank=="Owner" then return end
                local ok,err=S.setVIPByName(info.name,info.rank~="VIP")
                S.ShowRankMessage(ok and "Rank updated" or tostring(err or "Failed"))
            end)
            safeBtn.Activated:Connect(function()
                local nowSafe=S.isPlayerSafe and S.isPlayerSafe({Name=info.name}) or false
                local ok,err=S.setSafeByName(info.name,not nowSafe)
                if ok then safeBtn.Text=not nowSafe and "UnSafe" or "Safe" end
                S.ShowRankMessage(ok and "Safe updated" or tostring(err or "Safe failed"))
            end)
            ownerBtn.Activated:Connect(function()
                -- استجابة واجهة سريعة: لا نجمّد زر Owner أثناء طلب Supabase.
                ownerBtn.Active=false
                local enabled=(info.rank~="Owner")
                ownerBtn.Text=enabled and "..." or "..."
                task.spawn(function()
                    local ok,err=S.setOwnerByName(info.name,enabled)
                    pcall(function()
                        if ownerBtn and ownerBtn.Parent then
                            ownerBtn.Active=true
                            ownerBtn.Text=enabled and "Owner" or "UnOwner"
                        end
                    end)
                    S.ShowRankMessage(ok and (enabled and "Owner added" or "Owner removed") or tostring(err or "Owner update failed"))
                    task.defer(function()
                        pcall(function() S.ownerRefresh() end)
                    end)
                end)
            end)
            return row
        end

        function S.ownerRefresh()
            sfOwner.Visible=S.isOwner==true
            sfOwner.Active=true
            sfOwner.ZIndex=20
            local active=S.getActiveScriptUsers()
            local present={}
            for _,info in ipairs(active) do
                if tonumber(info.userId)~=S.player.UserId then
                    present[tostring(info.userId)]=true
                    S.ownerKnownUsers[tostring(info.userId)]=info
                end
            end
            for uid in pairs(S.ownerKnownUsers) do
                -- Keep users that are still present in the shared SQR presence
                -- record, even when they are in another Roblox server.
                if not present[uid] and not (S.cloudRecord.activeUsers or {})[uid] then
                    S.ownerKnownUsers[uid]=nil
                end
            end
            local sigParts={}
            for uid,info in pairs(S.ownerKnownUsers) do sigParts[#sigParts+1]=tostring(uid)..":"..tostring(info.name)..":"..tostring(info.rank) end
            table.sort(sigParts)
            local sig=table.concat(sigParts,"|")
            S.ownerUserCount.Text="Script Users: "..tostring(math.max(#active,table.getn(S.ownerKnownUsers)))
            local count=0
            for _,info in pairs(S.ownerKnownUsers) do
                if tonumber(info.userId)~=S.player.UserId then count=count+1 end
            end
            if sig~=S.ownerUiSignature then
                S.ownerUiSignature=sig
                clear(S.ownerPlayerFrame)
                for _,info in pairs(S.ownerKnownUsers) do
                    if tonumber(info.userId)~=S.player.UserId then buildPlayerRow(info) end
                end
                if count==0 then label(S.ownerPlayerFrame,"لا يوجد مستخدمون آخرون للسكربت حالياً",8,30,12) end
                S.ownerPlayerFrame.CanvasSize=UDim2.fromOffset(0,math.max(45,count*124+10))
            end
            clear(S.ownerResetFrame)
            local vipCount=0
            for _,info in pairs(S.ownerKnownUsers) do
                if info.rank=="VIP" then
                    vipCount=vipCount+1
                    local row=Instance.new("Frame"); row.Size=UDim2.new(1,-8,0,36); row.BackgroundColor3=Color3.fromRGB(34,38,50); row.Parent=S.ownerResetFrame; Instance.new("UICorner",row).CornerRadius=UDim.new(0,7)
                    local used=tonumber((S.cloudRecord.vipKicks or {})[info.name]) or 0
                    local t=Instance.new("TextLabel"); t.Size=UDim2.new(1,-85,1,0); t.Position=UDim2.fromOffset(7,0); t.BackgroundTransparency=1; t.Text="@"..info.name.." • VIP • Kick: 5/"..math.min(used,5); t.TextColor3=Color3.new(1,1,1); t.TextSize=11; t.Font=Enum.Font.GothamBold; t.TextXAlignment=Enum.TextXAlignment.Left; t.Parent=row
                    local r=mini(row,"RESET",0,1,65,Color3.fromRGB(45,130,85)); r.Position=UDim2.new(1,-72,0,1); r.Activated:Connect(function() S.resetVIPKicks(info.name) end)
                end
            end
            if vipCount==0 then label(S.ownerResetFrame,"لا يوجد VIP حالياً",8,30,12) end
            S.ownerResetFrame.CanvasSize=UDim2.fromOffset(0,math.max(42,vipCount*40+8))
            sfOwner.CanvasSize=UDim2.fromOffset(0,430+math.max(0,count*124))
        end
    end

    task.spawn(function()
        task.wait(0.05)
        pcall(function() if S.refreshSectionVisibility then S.refreshSectionVisibility() end; if S.vipRefresh then S.vipRefresh() end; if S.ownerRefresh then S.ownerRefresh() end end)
        while S.remoteCommandAlive do
            task.wait(5)
            pcall(function()
                if S.refreshSectionVisibility then S.refreshSectionVisibility() end
                if S.vipRefresh then S.vipRefresh() end
                if S.ownerRefresh then S.ownerRefresh() end
                if S._finalUISafetySync then S._finalUISafetySync() end
            end)
        end
    end)

    task.spawn(function()
        while S.remoteCommandAlive do
            task.wait(2)
            pcall(function() S.repairAllSectionControls() end)
            pcall(function() S.repairNamedControls() end)
        end
    end)
end

-- ============================================================
-- SCOPE 29A: FINAL UI SAFETY
-- Force the active UI tree to stay visible/usable; never force a closed main window open.
-- ============================================================
do
    local S=_G.SQR
    local function sync()
        if S.refreshSectionVisibility then pcall(S.refreshSectionVisibility) end
        if S.screenGui and S.screenGui.Enabled and S.mainFrame then S.mainFrame.Visible=true end
        if S.sidebar and S.screenGui and S.screenGui.Enabled then S.sidebar.Visible=true end
        local current=S.sectionFrames[S.currentSection]
        if current and S.sectionAllowed(S.sections[S.currentSection]) then
            current.Visible=true
            current.ZIndex=20
            if current:IsA("ScrollingFrame") then
                current.ScrollBarThickness=(S.sections[S.currentSection]=="Anti") and 4 or 5
                current.Active=true
                current.ScrollingEnabled=true
                current.AutomaticCanvasSize=Enum.AutomaticSize.Y
            end
            -- Some executor/UI combinations leave controls hidden or non-interactive.
            -- Normalize the whole active section while respecting explicit hidden markers.
            pcall(function() S.normalizeSectionUI(current) end)
        end
    end

    S._finalUISafetySync = sync

    -- Explicit repair list for controls that were reported as invisible/non-clickable.
    -- These are references to the existing controls; no duplicate buttons are created.
    function S.repairNamedControls()
        local names = {
            "searchBox","nameLabel","userLabel","viewButton","teleportButton",
            "bangButton","headbangButton","nvButton","reButton","copy1Button","copy2Button",
            "distanceLabel","distanceSlider","speedLabel","speedSlider","cuffButton","autoCuffButton",
            "hdSendButton","hdSpamButton","hdPlusButton","hdMinusButton","hdCountBox","hdStartButton","cmdBarButton","hdMessageCommandBox",
            "nearCmdBox","nearSpeedBox","nearSendButton","nearSpamButton","nearAuraButton","nearReButton","nearStatus",
            "discordBtn","countAllButton","countSearchBox","countTrackButton",
            "ownerLogsButton","ownerBannedButton","ownerVipListButton","ownerOwnerListButton","ownerPlayersLabel","ownerUserCount",
            "ownerPlayerFrame","ownerKickCounter","ownerResetFrame","vipStatus","vipUsersFrame"
        }
        for _,name in ipairs(names) do
            local obj=S[name]
            if obj and obj.Parent then
                obj.Visible=true
                obj.Active=true
                if obj:IsA("GuiButton") or obj:IsA("TextBox") then
                    obj.ZIndex=100
                    if obj:IsA("TextBox") then
                        obj.Active=true
                        obj.Selectable=true
                        obj.TextEditable=true
                    end
                end
            end
        end
        if S.hdSpamControls then S.hdSpamControls.Visible=true; S.hdSpamControls.ZIndex=90 end
        if S.hdSpamList then S.hdSpamList.Visible=true; S.hdSpamList.ZIndex=70 end
        if S.nearCmdBox then S.nearCmdBox.ZIndex=100 end
        if S.protectionGui and S.protectionGui.Parent then
            S.protectionGui.Enabled=true
        end
        if S.protectionFrame and S.protectionFrame.Parent then
            S.protectionFrame.Visible=true
            S.protectionFrame.Active=true
            S.protectionFrame.ZIndex=100
        end
        if S.fpsLabel and S.fpsLabel.Parent then
            S.fpsLabel.Visible=true
            S.fpsLabel.TextTransparency=0
        end
    end

    -- V35: final input/render repair. This does not replace existing callbacks;
    -- it only restores visibility/Active/ZIndex and installs feedback once.
    function S.repairAllSectionControls()
        for i,sf in ipairs(S.sectionFrames or {}) do
            if sf then
                pcall(function()
                    sf.Active = true
                    sf.ScrollingEnabled = true
                    sf.ScrollingDirection = Enum.ScrollingDirection.Y
                    sf.AutomaticCanvasSize = Enum.AutomaticSize.Y
                    sf.ZIndex = 20
                end)
                for _,obj in ipairs(sf:GetDescendants()) do
                    if obj:IsA("GuiObject") and obj:GetAttribute("SQRKeepHidden") ~= true and obj:GetAttribute("SQRDeferred") ~= true then
                        obj.Visible = true
                        if obj:IsA("GuiButton") or obj:IsA("TextBox") then
                            obj.Active = true
                            obj.ZIndex = math.max(obj.ZIndex, 50)
                            if obj:IsA("GuiButton") then
                                pcall(function() S.addButtonFeedback(obj) end)
                            end
                        end
                    end
                end
            end
        end
    end

    function S.repairSection(sectionName)
        local idx=table.find(S.sections,sectionName)
        local sf=idx and S.sectionFrames[idx]
        if sf then pcall(function() S.normalizeSectionUI(sf) end) end
    end

    S.repairAllSectionControls()

    function S.setScriptAvatar(imageObject,userId)
        local id=tonumber(userId)
        if not imageObject or not id or id<=0 then return end
        imageObject.Image="rbxthumb://type=AvatarHeadShot&id="..tostring(id).."&w=150&h=150"
        task.spawn(function()
            pcall(function()
                local img=S.Players:GetUserThumbnailAsync(id,Enum.ThumbnailType.AvatarBust,Enum.ThumbnailSize.Size150x150)
                if imageObject and imageObject.Parent and img and img~="" then imageObject.Image=img end
            end)
        end)
    end

    sync()
    pcall(function()
        if S.screenGui and not S.screenGui.Parent and S.playerGui then
            S.screenGui.Parent=S.playerGui
        end
        if S.screenGui and S.screenGui.Enabled and S.mainFrame and not S.mainFrame.Parent then
            S.mainFrame.Parent=S.screenGui
        end
        for _,sf in ipairs(S.sectionFrames) do
            if sf and not sf.Parent and S.mainFrame then
                sf.Parent=S.mainFrame
            end
        end
    end)
end

-- ============================================================
-- SCOPE 29B: REMOVED
-- ============================================================
-- لا نجبر GuiObjects المخفية على Visible=true؛ هذا كان سبب ظهور
-- Owner/VIP فوق الأقسام الأخرى وبقاء Owner ملتصقاً بالشاشة.
-- ============================================================
-- SCOPE 29: FINAL SETUP
-- ============================================================
do
    local S = _G.SQR
    
    S.installButtonFeedback(S.gui)
    S.installButtonFeedback(S.screenGui)
    
    S.closeBtn = Instance.new("TextButton")
    S.closeBtn.Size = UDim2.new(0, 28, 0, 28)
    S.closeBtn.Position = UDim2.new(1, -35, 0, 2)
    S.closeBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
    S.closeBtn.BorderSizePixel = 0
    S.closeBtn.Text = "X"
    S.closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    S.closeBtn.TextScaled = true
    S.closeBtn.Font = Enum.Font.SourceSansBold
    S.closeBtn.Parent = S.mainFrame
    Instance.new("UICorner", S.closeBtn).CornerRadius = UDim.new(0, 8)
    
    S.closeBtn.MouseButton1Click:Connect(function()
        pcall(function() S.saveSettings() end)
        S.screenGui.Enabled = false
        pcall(function()
            local tween = S.TweenService:Create(S.uiScale, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Scale = 0.01})
            tween:Play()
        end)
        wait(0.27)
        local savedScale = math.clamp(tonumber(S.settingState.uiScale) or 1, 0.7, 1.3)
        S.uiScale.Scale = savedScale
        S.mainFrame.Position = UDim2.new(0.5, -300 * savedScale, 0.5, -200 * savedScale)
        S.mainFrame.Visible = false
    end)
    
    pcall(function()
        S.gameBindToClose = game:BindToClose(function()
            pcall(function() S.saveSettings() end)
        end)
    end)

    pcall(function()
    end)
    print("SQR UI loaded successfully!")
end